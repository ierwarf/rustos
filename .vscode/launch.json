{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "KVM parallel boot",
      "type": "node-terminal",
      "request": "launch",
      "preLaunchTask": "Build OS",
      "command": "exec cargo xtask kvm-smoke --expect 'runtimed: bootstrap ui done'",
      "cwd": "${workspaceFolder}"
    }
  ]
}
