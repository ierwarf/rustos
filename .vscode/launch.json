{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "KVM DVM: verified atomic desktop (no manual pointer grab)",
      "type": "node-terminal",
      "request": "launch",
      "preLaunchTask": "Prepare KVM DVM",
      "command": "exec cargo xtask kvm-run",
      "cwd": "${workspaceFolder}"
    }
  ]
}
