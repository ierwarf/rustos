{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "QEMU",
      "type": "node-terminal",
      "request": "launch",
      "preLaunchTask": "Build OS",
      "command": "cargo xtask run --profile nvme --usb-input --debugcon stdio",
      "cwd": "${workspaceFolder}"
    },
    {
      "name": "KVM",
      "type": "node-terminal",
      "request": "launch",
      "preLaunchTask": "Build OS",
      "command": "cargo xtask run --profile nvme --accel-profile kvm --usb-input --debugcon stdio",
      "cwd": "${workspaceFolder}"
    },
    {
      "name": "G14",
      "type": "node-terminal",
      "request": "launch",
      "preLaunchTask": "Build OS",
      "command": "cargo xtask run --profile g14 --usb-input --debugcon stdio",
      "cwd": "${workspaceFolder}"
    }
  ]
}
