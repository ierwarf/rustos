{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "KVM NVMe",
      "type": "node-terminal",
      "request": "launch",
      "preLaunchTask": "Build OS",
      "command": "exec cargo xtask run --profile nvme --accel-profile kvm --usb-input --usb-input-device tablet --debugcon file --summarize-log",
      "cwd": "${workspaceFolder}"
    },
    {
      "name": "QEMU NVMe",
      "type": "node-terminal",
      "request": "launch",
      "preLaunchTask": "Build OS",
      "command": "exec cargo xtask run --profile nvme --usb-input --usb-input-device tablet --debugcon file --summarize-log",
      "cwd": "${workspaceFolder}"
    }
  ]
}
