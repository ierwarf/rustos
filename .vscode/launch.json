{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Release",
            "type": "node-terminal",
            "request": "launch",
            "preLaunchTask": "Build OS",
            "command": "exec /usr/bin/qemu-system-x86_64 -bios OVMF.fd -drive if=pflash,format=raw,readonly=on,file=OVMF.fd -drive file=fat:rw:build,format=raw -net none -m 1G -monitor none -debugcon stdio -global isa-debugcon.iobase=0xe9 -d int -D qemu_interrupt.log",
            "cwd": "${workspaceFolder}"
        }
    ]
}
