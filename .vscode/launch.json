{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "RustOS: verified KVM desktop",
      "type": "node-terminal",
      "request": "launch",
      "preLaunchTask": "F5: Build signed RustOS image",
      "command": "cargo xtask kvm-run",
      "cwd": "${workspaceFolder}"
    }
  ]
}
