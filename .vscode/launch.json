{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "RustOS: verified KVM desktop",
      "type": "node-terminal",
      "request": "launch",
      "command": "exec cargo xtask kvm-run --build --rustos-vcpus 8",
      "cwd": "${workspaceFolder}"
    }
  ]
}
