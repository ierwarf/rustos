{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "F5: Build signed RustOS image",
      "type": "process",
      "command": "cargo",
      "args": [
        "xtask",
        "build"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": [
        "$rustc"
      ],
      "group": "build"
    }
  ]
}
