[package]
name = "bootloader"
version = "0.1.0"
edition = "2021"

[dependencies]
boot-protocol = { path = "boot-protocol" }
storage-core = { path = "../../libs/storage-core" }
uefi = { version = "0.36.1", features = ["alloc"] }
xmas-elf = "0.10.0"
object = { version = "0.38.1", default-features = false, features = ["read_core", "elf"] }
raw-cpuid = "11.6.0"
