[package]
name = "inputd"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
libc = "0.2"
observability-client = { path = "../../libs/observability-client" }
rustos-user-abi = { path = "../../libs/rustos-user-abi" }
