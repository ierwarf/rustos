[workspace]

members = [
    "bootloader",
    "bootloader/boot-protocol",
    "driver-abi",
    "drivers/input/keyboard",
    "kernel",
    "module-tests",
    "prekernel",
    "uiserver",
]
resolver = "3"

[profile.release]
panic = "abort"

[profile.dev]
panic = "abort"
