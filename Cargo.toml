[workspace]

members = [
    "bootloader",
    "bootloader/boot-protocol",
    "driver-abi",
    "drivers/display/amdgpu",
    "drivers/input/keyboard",
    "kernel",
    "module-tests",
    "prekernel",
    "uiserver",
    "xtask",
]
resolver = "3"

[profile.release]
panic = "abort"

[profile.dev]
panic = "abort"
