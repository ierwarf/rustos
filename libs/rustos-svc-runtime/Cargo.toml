[package]
name = "rustos-svc-runtime"
version = "0.1.0"
edition = "2021"
publish = false

[features]
default = ["global-allocator"]
global-allocator = []

[dependencies]
rustos-user-abi = { path = "../rustos-user-abi" }
