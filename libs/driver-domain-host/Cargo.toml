[package]
name = "rustos-driver-domain-host"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
anyhow = "1"
driver-domain-protocol = { path = "../driver-domain-protocol", features = ["std"] }
libc = "0.2"
