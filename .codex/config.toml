# Project-scoped Codex configuration for RustOS.
#
# Loaded in addition to ~/.codex/config.toml when the agent is run with
# CWD inside this repo. Only applies because this project is marked
# trust_level = "trusted" in the user config.
#
# Keep this file project-specific: hooks, MCP servers, and any
# overrides that only make sense for RustOS work belong here. Personal
# style settings (model, reasoning effort, plugins, memories) stay in
# the user config.

[features]
hooks = true

# --------------------------------------------------------------------------
# Hooks
#
# Scripts live in .codex/hooks/ and read a JSON event from stdin, returning
# a Codex hook decision on stdout. Each script computes the repo root from its
# own location so this config is portable across clones.
# --------------------------------------------------------------------------

[[hooks.PreToolUse]]
matcher = "^(Read|mcp__.*read.*file|mcp__serena__read_file)$"

[[hooks.PreToolUse.hooks]]
type = "command"
command = '"$(git rev-parse --show-toplevel)/.codex/hooks/pre_read_large_file.sh"'
timeout = 5
statusMessage = "Checking file size / allow-list"

[[hooks.PreToolUse]]
matcher = "^(apply_patch|Edit|Write|mcp__serena__(create_text_file|insert_after_symbol|insert_before_symbol|rename_symbol|replace_content|replace_in_files|replace_symbol_body|safe_delete_symbol))$"

[[hooks.PreToolUse.hooks]]
type = "command"
command = '"$(git rev-parse --show-toplevel)/.codex/hooks/pre_edit_policy.sh"'
timeout = 5
statusMessage = "Checking edit path policy"

[[hooks.PreToolUse]]
matcher = "^(Bash|exec_command|mcp__serena__execute_shell_command)$"

[[hooks.PreToolUse.hooks]]
type = "command"
command = '"$(git rev-parse --show-toplevel)/.codex/hooks/pre_bash_destructive.sh"'
timeout = 5
statusMessage = "Checking for destructive shell"

[[hooks.PreToolUse.hooks]]
type = "command"
command = '"$(git rev-parse --show-toplevel)/.codex/hooks/pre_commit.sh"'
timeout = 45
statusMessage = "Pre-commit policy / fmt checks"

[[hooks.PostToolUse]]
matcher = "^(apply_patch|Edit|Write|mcp__serena__(create_text_file|insert_after_symbol|insert_before_symbol|rename_symbol|replace_content|replace_in_files|replace_symbol_body|safe_delete_symbol))$"

[[hooks.PostToolUse.hooks]]
type = "command"
command = '"$(git rev-parse --show-toplevel)/.codex/hooks/post_edit_rust.sh"'
timeout = 120
statusMessage = "cargo xtask check"

[[hooks.UserPromptSubmit]]

[[hooks.UserPromptSubmit.hooks]]
type = "command"
command = '"$(git rev-parse --show-toplevel)/.codex/hooks/user_prompt_policy.sh"'
timeout = 5
statusMessage = "Checking prompt safety"

[[hooks.SessionStart]]
matcher = "^(startup|resume)$"

[[hooks.SessionStart.hooks]]
type = "command"
command = '"$(git rev-parse --show-toplevel)/.codex/hooks/session_context.sh"'
timeout = 5
statusMessage = "Loading RustOS hook context"

# --------------------------------------------------------------------------
# MCP servers (project-scoped)
# Project-declared servers are exact-version inputs. Session-provided app or
# plugin connectors are configured outside this repository.
#   ripgrep  — mcp-ripgrep 0.4.0 (npm)
#   serena   — serena-agent 1.6.0 (PyPI through uvx)
# --------------------------------------------------------------------------

[mcp_servers.ripgrep]
command = "npx"
args = ["-y", "mcp-ripgrep@0.4.0"]

[mcp_servers.ripgrep.tools.search]
approval_mode = "approve"

[mcp_servers.serena]
command = "uvx"
args = ["--from", "serena-agent==1.6.0", "serena", "start-mcp-server"]
