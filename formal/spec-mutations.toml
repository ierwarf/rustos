# Mutation-adequacy corpus for the high-risk SMP and interactive-boot models.
#
# Each mutant is a single, exact alteration in a temporary TLA+ copy.  A
# passing baseline followed by a TLC invariant counterexample is required.
# Parser/type failures, timeout, and a surviving mutant are all gate failures.
# `property-perturbation` intentionally changes an assertion to a nearby wrong
# one; the other kinds model omitted guards, ordering, effect, or revoke steps.
# The required_models set is deliberately risk-scoped, not a claim that finite
# mutation testing can enumerate all faults in every TLA+ model.
schema = "rustos-tla-mutation-corpus-v1"

required_models = [
    "acceptance-profile-publication/AcceptanceProfilePublication",
    "atomic-process-activation-batch/AtomicProcessActivationBatch",
    "capability-derivation-lifecycle/CapabilityDerivationLifecycle",
    "clocksource-deadline/ClocksourceDeadline",
    "cpu-online-lifecycle/CpuOnlineLifecycle",
    "cross-cpu-task-retirement/CrossCpuTaskRetirement",
    "dvm-block-startup/DvmBlockStartup",
    "dvm-input-ring/DvmInputRing",
    "dvm-network-ring/DvmNetworkRing",
    "dvm-transport-lifecycle/DvmTransportLifecycle",
    "exception-retirement-lifecycle/ExceptionRetirementLifecycle",
    "exec-address-space-transaction/ExecAddressSpaceTransaction",
    "gpu-submit-transaction/GpuSubmitTransaction",
    "input-ingestion-worker/InputIngestionWorker",
    "ipc-priority-inheritance/IpcPriorityInheritance",
    "ipc-priority-queue/IpcPriorityQueue",
    "ipc-reply-deadline/IpcReplyDeadline",
    "ipc-reply-recv-transaction/IpcReplyRecvTransaction",
    "ipc-transfer-authority/IpcTransferAuthority",
    "msi-vector-lifecycle/MsiVectorLifecycle",
    "netd-deferred-reply/NetdDeferredReply",
    "page-cache-lifecycle/PageCacheLifecycle",
    "page-table-map-transaction/PageTableMapTransaction",
    "pager-fault-slot-lifecycle/PagerFaultSlotLifecycle",
    "pager-frame-grant-lifecycle/PagerFrameGrantLifecycle",
    "pager-vma-publication/PagerVmaPublication",
    "per-cpu-clockevent-lifecycle/PerCpuClockeventLifecycle",
    "process-lifecycle-transaction/ProcessLifecycleTransaction",
    "product-boot/ProductBoot",
    "robust-futex-owner-death/RobustFutexOwnerDeath",
    "scheduler-active-balance/SchedulerActiveBalance",
    "scheduler-cpu-distribution/SchedulerCpuDistribution",
    "scheduler-cpu-ownership/SchedulerCpuOwnership",
    "scheduler-thread-demotion/SchedulerThreadDemotion",
    "scheduler-wakeup/SchedulerWakeup",
    "smp-reschedule-ipi/SmpRescheduleIpi",
    "smp-ring3-qualification/SmpRing3Qualification",
    "synchronous-ipc-handoff-concurrency/SynchronousIpcHandoffConcurrency",
    "synchronous-ipc-handoff/SynchronousIpcHandoff",
    "tlb-shootdown-lifecycle/TlbShootdownLifecycle",
    "user-stack-growth/UserStackGrowth",
    "userspace-wait-set/UserspaceWaitSet",
    "userspace-wait-set/WaitSetRegistry",
    "wayland-accept-isolation/WaylandAcceptIsolation",
]

[[mutation]]
id = "acceptance-initial-miss-negative-cached"
kind = "transition-effect"
severity = "high"
model = "acceptance-profile-publication/AcceptanceProfilePublication"
flow = "commercial-product-boot"
find = "    /\\ phase' = Watching /\\ watching' = TRUE"
replace = "    /\\ phase' = Watching /\\ watching' = FALSE"
occurrence = 1
invariant = "InitialMissRetainsBoundedObserver"
min_counterexample_states = 2

[[mutation]]
id = "activation-authority-revoke-removed"
kind = "transition-revocation"
severity = "critical"
model = "atomic-process-activation-batch/AtomicProcessActivationBatch"
flow = "atomic-process-activation-batch"
find = "IF task \\in BatchSet THEN Revoked ELSE authority[task]"
replace = "IF task \\in BatchSet THEN Live ELSE authority[task]"
occurrence = 1
invariant = "ExitedRequesterCannotLeaveActivatableOrphans"
min_counterexample_states = 2

[[mutation]]
id = "activation-consume-publish-order-collapsed"
kind = "transition-order"
severity = "critical"
model = "atomic-process-activation-batch/AtomicProcessActivationBatch"
flow = "atomic-process-activation-batch"
find = "phase' = AuthorityConsumed"
replace = "phase' = Committed"
occurrence = 1
invariant = "NoPartialPublication"
min_counterexample_states = 2

[[mutation]]
id = "activation-suspension-property-perturbed"
kind = "property-perturbation"
severity = "critical"
model = "atomic-process-activation-batch/AtomicProcessActivationBatch"
flow = "atomic-process-activation-batch"
find = '''AuthorityConsumptionRetainsSuspension ==
    phase = AuthorityConsumed =>
'''
replace = '''AuthorityConsumptionRetainsSuspension ==
    phase = Committed =>
'''
occurrence = 1
invariant = "AuthorityConsumptionRetainsSuspension"
min_counterexample_states = 3

[[mutation]]
id = "activation-target-generation-preflight-removed"
kind = "transition-guard-removal"
severity = "critical"
model = "atomic-process-activation-batch/AtomicProcessActivationBatch"
flow = "atomic-process-activation-batch"
find = '    /\ TargetIdentityIsExact'
replace = '    /\ TRUE'
occurrence = 1
invariant = "TargetIdentityReplacementCannotActivate"
min_counterexample_states = 3

[[mutation]]
id = "activation-target-identity-pid-only"
kind = "transition-guard-removal"
severity = "critical"
model = "atomic-process-activation-batch/AtomicProcessActivationBatch"
flow = "atomic-process-activation-batch"
find = "    TargetIdentity(currentGeneration) = TargetIdentity(snapshotGeneration)"
replace = "    TargetIdentity(currentGeneration).pid = TargetIdentity(snapshotGeneration).pid"
occurrence = 1
invariant = "TargetIdentityReplacementCannotActivate"
min_counterexample_states = 2

[[mutation]]
id = "capability-derivation-lease-drift"
kind = "transition-guard-removal"
severity = "critical"
model = "capability-derivation-lifecycle/CapabilityDerivationLifecycle"
flow = "capability-derivation-lifecycle"
find = "    /\\ lease = parentLease"
replace = "    /\\ TRUE"
occurrence = 1
invariant = "LiveChildIsAttenuatedAndExact"
min_counterexample_states = 2

[[mutation]]
id = "capability-derivation-parent-revoke-leaks-child"
kind = "transition-revocation"
severity = "critical"
model = "capability-derivation-lifecycle/CapabilityDerivationLifecycle"
flow = "capability-derivation-lifecycle"
find = '''RevokeParent ==
    /\ parentState = Live
    /\ parentState' = Revoked
    /\ childState' = IF childState = Live THEN Revoked ELSE childState'''
replace = '''RevokeParent ==
    /\ parentState = Live
    /\ parentState' = Revoked
    /\ childState' = childState'''
occurrence = 1
invariant = "LiveChildIsAttenuatedAndExact"
min_counterexample_states = 2

[[mutation]]
id = "capability-derivation-rights-broadened"
kind = "transition-guard-removal"
severity = "critical"
model = "capability-derivation-lifecycle/CapabilityDerivationLifecycle"
flow = "capability-derivation-lifecycle"
find = "    /\\ rights \\subseteq parentRights"
replace = "    /\\ TRUE"
occurrence = 1
invariant = "LiveChildIsAttenuatedAndExact"
min_counterexample_states = 2

[[mutation]]
id = "clock-raw-tsc-smp-admission-guard-removed"
kind = "transition-guard-removal"
severity = "critical"
model = "clocksource-deadline/ClocksourceDeadline"
flow = "monotonic-deadline-lifecycle"
find = "    /\\ clockCpuCount = 1"
replace = "    /\\ TRUE"
occurrence = 1
invariant = "RawTscRequiresUniprocessorTopology"
min_counterexample_states = 2

[[mutation]]
id = "cpu-online-ap-cache-enable-before-mtrr-restore"
kind = "transition-guard-removal"
severity = "critical"
model = "cpu-online-lifecycle/CpuOnlineLifecycle"
flow = "cpu-online-lifecycle"
find = '''EnableApCache(cpu) ==
    /\ cpu # Bsp
    /\ state[cpu] = Starting
    /\ cacheState[cpu] = NoFillCache
    /\ mtrrBaselineRestored[cpu]'''
replace = '''EnableApCache(cpu) ==
    /\ cpu # Bsp
    /\ state[cpu] = Starting
    /\ cacheState[cpu] = NoFillCache
    /\ TRUE'''
occurrence = 1
invariant = "ApCacheEnableRequiresExactRestoredMemoryTypes"
min_counterexample_states = 4

[[mutation]]
id = "cpu-online-ap-no-fill-transition-bypassed"
kind = "transition-effect"
severity = "critical"
model = "cpu-online-lifecycle/CpuOnlineLifecycle"
flow = "cpu-online-lifecycle"
find = "    /\\ cacheState' = [cacheState EXCEPT ![cpu] = NoFillCache]"
replace = "    /\\ cacheState' = [cacheState EXCEPT ![cpu] = CacheEnabled]"
occurrence = 1
invariant = "ApCacheEnableRequiresExactRestoredMemoryTypes"
min_counterexample_states = 3

[[mutation]]
id = "cpu-online-ap-restore-before-bsp-baseline"
kind = "transition-guard-removal"
severity = "critical"
model = "cpu-online-lifecycle/CpuOnlineLifecycle"
flow = "cpu-online-lifecycle"
find = '''RestoreApMemoryTypeBaseline(cpu) ==
    /\ cpu # Bsp
    /\ state[cpu] = Starting
    /\ cacheState[cpu] = NoFillCache
    /\ bspMemoryTypeBaselineCaptured'''
replace = '''RestoreApMemoryTypeBaseline(cpu) ==
    /\ cpu # Bsp
    /\ state[cpu] = Starting
    /\ cacheState[cpu] = NoFillCache
    /\ TRUE'''
occurrence = 1
invariant = "BspBaselinePrecedesEveryApRestore"
min_counterexample_states = 4

[[mutation]]
id = "cpu-online-pat-readback-before-private-ready-removed"
kind = "transition-guard-removal"
severity = "critical"
model = "cpu-online-lifecycle/CpuOnlineLifecycle"
flow = "cpu-online-lifecycle"
find = '''PublishPrivateState(cpu) ==
    /\ state[cpu] = Starting
    /\ generation[cpu] = 1
    /\ CpuMemoryTypeContractReady(cpu)'''
replace = '''PublishPrivateState(cpu) ==
    /\ state[cpu] = Starting
    /\ generation[cpu] = 1
    /\ patSlot0Validated[cpu]'''
occurrence = 1
invariant = "BspAndEveryApReadBackExactPatBeforePrivateReady"
min_counterexample_states = 3

[[mutation]]
id = "cpu-online-pat-slot0-readback-corrupted"
kind = "transition-effect"
severity = "critical"
model = "cpu-online-lifecycle/CpuOnlineLifecycle"
flow = "cpu-online-lifecycle"
find = "    /\\ patReadback' = [patReadback EXCEPT ![cpu] = PatKernelCacheContract]"
replace = '''    /\ patReadback' = [patReadback EXCEPT ![cpu] =
        [slot0 |-> "uc", slot2 |-> "uc", slot4 |-> "wc"]]'''
occurrence = 1
invariant = "ExactPatReadbackRequiresWbUcWcSelectors"
min_counterexample_states = 5

[[mutation]]
id = "cpu-online-pat-slot2-readback-corrupted"
kind = "transition-effect"
severity = "critical"
model = "cpu-online-lifecycle/CpuOnlineLifecycle"
flow = "cpu-online-lifecycle"
find = "    /\\ patReadback' = [patReadback EXCEPT ![cpu] = PatKernelCacheContract]"
replace = '''    /\ patReadback' = [patReadback EXCEPT ![cpu] =
        [slot0 |-> "wb", slot2 |-> "wb", slot4 |-> "wc"]]'''
occurrence = 1
invariant = "ExactPatReadbackRequiresWbUcWcSelectors"
min_counterexample_states = 5

[[mutation]]
id = "cpu-online-restart-fails-open"
kind = "transition-effect"
severity = "critical"
model = "cpu-online-lifecycle/CpuOnlineLifecycle"
flow = "cpu-online-lifecycle"
find = '''RejectRestart(cpu) ==
    /\ state[cpu] = Failed
    /\ restartAttempted' = TRUE
    /\ panicRaised' = TRUE
    /\ UNCHANGED <<state, generation, privateReady, schedulerReady,
                   dispatchAuthority, failedSeen, staleGenerationAttempted,
                   initialPatSlot0, patSlot0Validated, patProgrammed,
                   patReadback, sharedMemoryWbMapped,
                   sharedMemorySelector, bspMemoryTypeBaselineCaptured,
                   mtrrBaselineRestored, cacheState>>'''
replace = '''RejectRestart(cpu) ==
    /\ state[cpu] = Failed
    /\ state' = [state EXCEPT ![cpu] = Starting]
    /\ restartAttempted' = TRUE
    /\ panicRaised' = FALSE
    /\ UNCHANGED <<generation, privateReady, schedulerReady,
                   dispatchAuthority, failedSeen, staleGenerationAttempted,
                   initialPatSlot0, patSlot0Validated, patProgrammed,
                   patReadback, sharedMemoryWbMapped,
                   sharedMemorySelector, bspMemoryTypeBaselineCaptured,
                   mtrrBaselineRestored, cacheState>>'''
occurrence = 1
invariant = "FailedStateIsTerminalForBoot"
min_counterexample_states = 3

[[mutation]]
id = "cpu-online-scheduler-stage-skipped"
kind = "transition-order"
severity = "critical"
model = "cpu-online-lifecycle/CpuOnlineLifecycle"
flow = "cpu-online-lifecycle"
find = "state' = [state EXCEPT ![cpu] = OnlineParked]"
replace = "state' = [state EXCEPT ![cpu] = Online]"
occurrence = 1
invariant = "OnlineRequiresCompleteCpu"
min_counterexample_states = 2

[[mutation]]
id = "cpu-online-stale-generation-fails-open"
kind = "transition-effect"
severity = "critical"
model = "cpu-online-lifecycle/CpuOnlineLifecycle"
flow = "cpu-online-lifecycle"
find = '''RejectStaleGeneration ==
    /\ staleGenerationAttempted' = TRUE
    /\ panicRaised' = TRUE'''
replace = '''RejectStaleGeneration ==
    /\ staleGenerationAttempted' = TRUE
    /\ panicRaised' = FALSE'''
occurrence = 1
invariant = "InvalidGenerationFailsClosed"
min_counterexample_states = 2

[[mutation]]
id = "cpu-online-wb-shared-memory-uses-wc-selector"
kind = "transition-effect"
severity = "critical"
model = "cpu-online-lifecycle/CpuOnlineLifecycle"
flow = "cpu-online-lifecycle"
find = "    /\\ sharedMemorySelector' = WbSelector"
replace = "    /\\ sharedMemorySelector' = WcSlot4Selector"
occurrence = 1
invariant = "WbSharedMemoryDoesNotUseWcSelector"
min_counterexample_states = 2

[[mutation]]
id = "cross-cpu-replace-before-sibling-detach"
kind = "transition-guard-removal"
severity = "critical"
model = "cross-cpu-task-retirement/CrossCpuTaskRetirement"
flow = "cross-cpu-task-retirement"
find = '''Replace ==
    /\ phase = Quiescing
    /\ targetQuiesced
    /\ owner["target"] = NoneCpu
    /\ ~siblingAttached
'''
replace = '''Replace ==
    /\ phase = Quiescing
    /\ targetQuiesced
    /\ owner["target"] = NoneCpu
    /\ TRUE
'''
occurrence = 1
invariant = "ReplacementRequiresAllOldExecutionStopped"
min_counterexample_states = 3

[[mutation]]
id = "dvm-block-install-prefetchable-wb-gate-removed"
kind = "transition-guard-removal"
severity = "critical"
model = "dvm-block-startup/DvmBlockStartup"
flow = "commercial-product-boot"
find = '''    /\ barPrefetchable
    /\ rustosApertureCache = "wb"'''
replace = "    /\\ TRUE"
occurrence = 1
invariant = "InstalledTransportHasPrefetchableRustosWbAperture"
min_counterexample_states = 2

[[mutation]]
id = "dvm-block-rustos-wc-alias-admitted"
kind = "transition-effect"
severity = "critical"
model = "dvm-block-startup/DvmBlockStartup"
flow = "commercial-product-boot"
find = "    /\\ rustosApertureCache' = \"wb\""
replace = "    /\\ rustosApertureCache' = \"wc\""
occurrence = 1
invariant = "RustosAtomicApertureNeverUsesWc"
min_counterexample_states = 2

[[mutation]]
id = "dvm-block-successor-exact-admission-bypassed"
kind = "transition-guard-removal"
severity = "critical"
model = "dvm-block-startup/DvmBlockStartup"
flow = "commercial-product-boot"
find = '''    /\ candidateGeneration > generation
    /\ candidateCursorsZero
    /\ candidateSignatureValid'''
replace = '''    /\ TRUE
    /\ TRUE
    /\ TRUE'''
occurrence = 1
invariant = "ReboundEpochIsStrictlyNewerSignedZeroCursor"
min_counterexample_states = 8

[[mutation]]
id = "dvm-input-drain-owner-guard-removed"
kind = "transition-guard-removal"
severity = "critical"
model = "dvm-input-ring/DvmInputRing"
flow = "dvm-input-ingress"
find = '''BeginBrokerTurn(caller) ==
    /\ caller \in Callers
    /\ rustosReady
    /\ brokerOwner = NoOwner
'''
replace = '''BeginBrokerTurn(caller) ==
    /\ caller \in Callers
    /\ rustosReady
    /\ TRUE
'''
occurrence = 1
invariant = "SingleFlightDrainOwnership"
min_counterexample_states = 3

[[mutation]]
id = "dvm-network-install-atomic-control-bypass"
kind = "transition-guard-removal"
severity = "critical"
model = "dvm-network-ring/DvmNetworkRing"
flow = "dvm-network-ingress"
find = '''Install ==
    /\ ~installed
    /\ rawHeader = ValidHeader
    /\ cacheContract.barPrefetchable
    /\ cacheContract.rustos = "wb"
    /\ cacheContract.rustosAtomics'''
replace = '''Install ==
    /\ ~installed
    /\ rawHeader = ValidHeader
    /\ cacheContract.barPrefetchable
    /\ cacheContract.rustos = "wb"
    /\ TRUE'''
occurrence = 1
invariant = "InstalledTransportHasPrefetchableRustosWbAtomics"
min_counterexample_states = 2

[[mutation]]
id = "dvm-network-install-prefetchable-bypass"
kind = "transition-guard-removal"
severity = "critical"
model = "dvm-network-ring/DvmNetworkRing"
flow = "dvm-network-ingress"
find = '''Install ==
    /\ ~installed
    /\ rawHeader = ValidHeader
    /\ cacheContract.barPrefetchable
    /\ cacheContract.rustos = "wb"'''
replace = '''Install ==
    /\ ~installed
    /\ rawHeader = ValidHeader
    /\ TRUE
    /\ cacheContract.rustos = "wb"'''
occurrence = 1
invariant = "InstalledTransportHasPrefetchableRustosWbAtomics"
min_counterexample_states = 2

[[mutation]]
id = "dvm-network-install-rustos-wb-bypass"
kind = "transition-guard-removal"
severity = "critical"
model = "dvm-network-ring/DvmNetworkRing"
flow = "dvm-network-ingress"
find = '''Install ==
    /\ ~installed
    /\ rawHeader = ValidHeader
    /\ cacheContract.barPrefetchable
    /\ cacheContract.rustos = "wb"'''
replace = '''Install ==
    /\ ~installed
    /\ rawHeader = ValidHeader
    /\ cacheContract.barPrefetchable
    /\ TRUE'''
occurrence = 1
invariant = "InstalledTransportHasPrefetchableRustosWbAtomics"
min_counterexample_states = 2

[[mutation]]
id = "dvm-network-linux-atomic-readiness-bypass"
kind = "transition-guard-removal"
severity = "critical"
model = "dvm-network-ring/DvmNetworkRing"
flow = "dvm-network-ingress"
find = '''PublishLinuxReady ==
    /\ installed
    /\ ~linuxReady
    /\ cacheContract.linux = "wb"
    /\ cacheContract.linuxAtomics'''
replace = '''PublishLinuxReady ==
    /\ installed
    /\ ~linuxReady
    /\ cacheContract.linux = "wb"
    /\ TRUE'''
occurrence = 1
invariant = "LinuxDataPlaneRequiresWbAtomics"
min_counterexample_states = 3

[[mutation]]
id = "dvm-network-linux-wb-readiness-bypass"
kind = "transition-guard-removal"
severity = "critical"
model = "dvm-network-ring/DvmNetworkRing"
flow = "dvm-network-ingress"
find = '''PublishLinuxReady ==
    /\ installed
    /\ ~linuxReady
    /\ cacheContract.linux = "wb"
    /\ cacheContract.linuxAtomics'''
replace = '''PublishLinuxReady ==
    /\ installed
    /\ ~linuxReady
    /\ TRUE
    /\ cacheContract.linuxAtomics'''
occurrence = 1
invariant = "LinuxDataPlaneRequiresWbAtomics"
min_counterexample_states = 3

[[mutation]]
id = "dvm-transport-activation-overwrites-drain"
kind = "transition-guard-removal"
severity = "critical"
model = "dvm-transport-lifecycle/DvmTransportLifecycle"
flow = "dvm-input-ingress"
find = '''CommitActivation ==
    /\ state = "Activating"
    /\ mappingPublished
    /\ state' = "Active"
'''
replace = '''CommitActivation ==
    /\ state \in {"Activating", "Draining"}
    /\ mappingPublished
    /\ state' = "Active"
'''
occurrence = 1
invariant = "DrainCancelsActivation"
min_counterexample_states = 5

[[mutation]]
id = "dvm-transport-reset-before-quiescence"
kind = "transition-guard-removal"
severity = "critical"
model = "dvm-transport-lifecycle/DvmTransportLifecycle"
flow = "dvm-input-ingress"
find = '''CompleteDrain ==
    /\ state = "Draining"
    /\ claims = 0
    /\ state' = "Revoked"
    /\ mappingPublished' = FALSE
    /\ resetCount' = resetCount + 1
    /\ resetWithClaim' = (claims # 0)
'''
replace = '''CompleteDrain ==
    /\ state = "Draining"
    /\ claims \in 0..MaxClaims
    /\ state' = "Revoked"
    /\ mappingPublished' = FALSE
    /\ resetCount' = resetCount + 1
    /\ resetWithClaim' = (claims # 0)
'''
occurrence = 1
invariant = "ResetOnlyAfterQuiescence"
min_counterexample_states = 4

[[mutation]]
id = "exception-user-diagnostic-memory-probe-restored"
kind = "transition-effect"
severity = "critical"
model = "exception-retirement-lifecycle/ExceptionRetirementLifecycle"
flow = "exception-retirement"
find = '''RecordScalarDiagnostics ==
    /\ phase = Aligned
    /\ stackAligned
    /\ phase' = Diagnosed
    /\ diagnosticProbeAttempted' = FALSE
    /\ nestedFault' = FALSE
'''
replace = '''RecordScalarDiagnostics ==
    /\ phase = Aligned
    /\ stackAligned
    /\ phase' = Diagnosed
    /\ diagnosticProbeAttempted' = TRUE
    /\ nestedFault' = ~diagnosticMemoryValid
'''
occurrence = 1
invariant = "UserDiagnosticNeverProbesMemory"
min_counterexample_states = 4

[[mutation]]
id = "exec-old-bundle-released-before-scheduler-publication"
kind = "transition-order"
severity = "critical"
model = "exec-address-space-transaction/ExecAddressSpaceTransaction"
flow = "process-address-space-lifecycle"
find = '''    /\ processStateLockHeld' = TRUE
    /\ locksNested' = schedulerLockHeld
    /\ UNCHANGED <<tokenLive, tokenUses, stagedBundle,
                    schedulerPublished, visibleGeneration, exitPending,
                    targetRetired, oldBundleRetained, schedulerLockHeld,
                    publicationFailed>>'''
replace = '''    /\ processStateLockHeld' = TRUE
    /\ oldBundleRetained' = FALSE
    /\ locksNested' = schedulerLockHeld
    /\ UNCHANGED <<tokenLive, tokenUses, stagedBundle,
                    schedulerPublished, visibleGeneration, exitPending,
                    targetRetired, schedulerLockHeld, publicationFailed>>'''
occurrence = 1
invariant = "OldBundleRetainedUntilVisibleCommit"
min_counterexample_states = 5

[[mutation]]
id = "exec-publication-failure-commits-generation"
kind = "transition-effect"
severity = "high"
model = "exec-address-space-transaction/ExecAddressSpaceTransaction"
flow = "process-address-space-lifecycle"
find = '''    /\ publicationFailed' = TRUE
    /\ targetRetired' = exitPending
    /\ UNCHANGED <<tokenUses, schedulerPublished, visibleGeneration,
                    exitPending, oldBundleRetained, processStateLockHeld,
                    locksNested>>'''
replace = '''    /\ publicationFailed' = TRUE
    /\ targetRetired' = exitPending
    /\ visibleGeneration' = NewGeneration
    /\ UNCHANGED <<tokenUses, schedulerPublished,
                    exitPending, oldBundleRetained, processStateLockHeld,
                    locksNested>>'''
occurrence = 1
invariant = "NoVisibleHalfExec"
min_counterexample_states = 5

[[mutation]]
id = "exec-target-immediate-retire-during-transaction"
kind = "transition-effect"
severity = "critical"
model = "exec-address-space-transaction/ExecAddressSpaceTransaction"
flow = "process-address-space-lifecycle"
find = '''    /\ exitPending' = TRUE
    /\ UNCHANGED <<phase, tokenLive, tokenUses, stagedBundle,
                    schedulerPublished, visibleGeneration, targetRetired,
                    oldBundleRetained, processStateLockHeld,
                    schedulerLockHeld, locksNested, publicationFailed>>'''
replace = '''    /\ exitPending' = TRUE
    /\ targetRetired' = TRUE
    /\ UNCHANGED <<phase, tokenLive, tokenUses, stagedBundle,
                    schedulerPublished, visibleGeneration,
                    oldBundleRetained, processStateLockHeld,
                    schedulerLockHeld, locksNested, publicationFailed>>'''
occurrence = 1
invariant = "NoCommitAfterRetire"
min_counterexample_states = 3

[[mutation]]
id = "futex-kernel-wake-forced-private"
kind = "transition-effect"
severity = "critical"
model = "robust-futex-owner-death/RobustFutexOwnerDeath"
flow = "thread-exit-futex-cleanup"
find = "    /\\ wakeKey' = waiterKey"
replace = "    /\\ wakeKey' = PrivateKey"
occurrence = 1
invariant = "KernelGeneratedWakeUsesEquivalentKey"
min_counterexample_states = 4

[[mutation]]
id = "gpu-rejection-rollback-removed"
kind = "transition-effect"
severity = "critical"
model = "gpu-submit-transaction/GpuSubmitTransaction"
flow = "gpu-frame-lifecycle"
find = "    /\\ nextSubmit' = checkpoint /\\ timelineAdmitted' = FALSE"
replace = "    /\\ nextSubmit' = nextSubmit /\\ timelineAdmitted' = FALSE"
occurrence = 1
invariant = "RejectedSubmitRestoresExactCheckpoint"
min_counterexample_states = 4

[[mutation]]
id = "input-session-batch-retention-removed"
kind = "transition-effect"
severity = "critical"
model = "input-ingestion-worker/InputIngestionWorker"
flow = "input-delivery-lifecycle"
find = '''BeginAuthoritySync ==
    /\ workerState = Draining
    /\ workerState' = Syncing
    /\ authorityPending' = TRUE
'''
replace = '''BeginAuthoritySync ==
    /\ workerState = Draining
    /\ workerState' = Syncing
    /\ authorityPending' = FALSE
'''
occurrence = 1
invariant = "AuthorityBatchIsRetained"
min_counterexample_states = 2

[[mutation]]
id = "input-session-deadline-reset-per-attempt"
kind = "transition-effect"
severity = "high"
model = "input-ingestion-worker/InputIngestionWorker"
flow = "input-delivery-lifecycle"
find = '''           sleepBudget == IF afterRemaining < AuthorityRetryBackoff
                          THEN afterRemaining ELSE AuthorityRetryBackoff
       IN authorityElapsed' = afterCall + sleepBudget'''
replace = '''           sleepBudget == AuthorityRetryBackoff
       IN authorityElapsed' = afterCall + sleepBudget'''
occurrence = 1
invariant = "TypeOK"
min_counterexample_states = 7

[[mutation]]
id = "ipc-priority-capacity-admission-removed"
kind = "transition-guard-removal"
severity = "critical"
model = "ipc-priority-inheritance/IpcPriorityInheritance"
flow = "scheduler-dispatch"
find = "    /\\ DonationSlotsUsed < MaxDonations"
replace = "    /\\ TRUE"
occurrence = 1
invariant = "TypeOK"
min_counterexample_states = 3

[[mutation]]
id = "ipc-priority-ordinary-bypasses-system"
kind = "transition-guard-removal"
severity = "critical"
model = "ipc-priority-queue/IpcPriorityQueue"
flow = "scheduler-dispatch"
find = '''DeliverOrdinary ==
    /\ Len(ordinaryQ) > 0
    /\ ~SystemEligible
'''
replace = '''DeliverOrdinary ==
    /\ Len(ordinaryQ) > 0
    /\ TRUE
'''
occurrence = 1
invariant = "SystemBacklogCannotBeBypassed"
min_counterexample_states = 4

[[mutation]]
id = "ipc-priority-process-wide-broker-promotion"
kind = "transition-effect"
severity = "critical"
model = "ipc-priority-inheritance/IpcPriorityInheritance"
flow = "scheduler-dispatch"
find = "              /\\ receiverOf[reply] = task"
replace = "              /\\ receiverOf[reply] \\in BrokerTasks\n              /\\ task \\in BrokerTasks"
occurrence = 1
invariant = "UnselectedBrokerIsNotPromoted"
min_counterexample_states = 3

[[mutation]]
id = "ipc-priority-reservation-blocks-before-publication"
kind = "transition-effect"
severity = "critical"
model = "ipc-priority-inheritance/IpcPriorityInheritance"
flow = "scheduler-dispatch"
find = "    /\\ UNCHANGED taskState\n    /\\ lastPick' = NoTask"
replace = "    /\\ taskState' = [taskState EXCEPT ![caller] = Blocked]\n    /\\ lastPick' = NoTask"
occurrence = 1
invariant = "ReservedCallerIsNotBlocked"
min_counterexample_states = 2

[[mutation]]
id = "ipc-priority-unbound-reply-promotes-without-worker"
kind = "transition-effect"
severity = "critical"
model = "ipc-priority-inheritance/IpcPriorityInheritance"
flow = "scheduler-dispatch"
find = '''PublishCallUnbound(caller, reply) ==
    /\ caller \in Tasks /\ reply \in Replies
    /\ replyState[reply] = Reserved
    /\ callerOf[reply] = caller
    /\ taskState[caller] = Runnable
    /\ taskState' = [taskState EXCEPT ![caller] = Blocked]
    /\ replyState' = [replyState EXCEPT ![reply] = Queued]
    /\ receiverOf' = [receiverOf EXCEPT ![reply] = NoTask]
    /\ donation' = [donation EXCEPT ![reply] = FALSE]'''
replace = '''PublishCallUnbound(caller, reply) ==
    /\ caller \in Tasks /\ reply \in Replies
    /\ replyState[reply] = Reserved
    /\ callerOf[reply] = caller
    /\ taskState[caller] = Runnable
    /\ taskState' = [taskState EXCEPT ![caller] = Blocked]
    /\ replyState' = [replyState EXCEPT ![reply] = Queued]
    /\ receiverOf' = [receiverOf EXCEPT ![reply] = NoTask]
    /\ donation' = [donation EXCEPT ![reply] = TRUE]'''
occurrence = 1
invariant = "DonationHasExactLiveReply"
min_counterexample_states = 3

[[mutation]]
id = "ipc-reply-publication-eagerly-visible"
kind = "transition-effect"
severity = "critical"
model = "ipc-reply-deadline/IpcReplyDeadline"
flow = "ipc-call"
find = "        [publicationState EXCEPT ![reply] = PendingPublication]"
replace = "        [publicationState EXCEPT ![reply] = VisiblePublication]"
occurrence = 1
invariant = "VisiblePublicationRequiresConsumedTimelyReply"
min_counterexample_states = 3

[[mutation]]
id = "ipc-reply-publication-expiry-drop-omitted"
kind = "transition-effect"
severity = "critical"
model = "ipc-reply-deadline/IpcReplyDeadline"
flow = "ipc-call"
find = '''            IF r \in DueCalls /\ publicationState[r] = PendingPublication
            THEN DroppedPublication
            ELSE publicationState[r]'''
replace = '''            IF r \in DueCalls /\ publicationState[r] = PendingPublication
            THEN publicationState[r]
            ELSE publicationState[r]'''
occurrence = 1
invariant = "PendingPublicationIsOwnedByOneLiveReply"
min_counterexample_states = 4

[[mutation]]
id = "ipc-reply-publication-taking-expiry-omitted"
kind = "transition-guard-removal"
severity = "critical"
model = "ipc-reply-deadline/IpcReplyDeadline"
flow = "ipc-call"
find = 'OutstandingReply(r) == replyState[r] \in {Queued, Dequeued, Replied, Taking}'
replace = 'OutstandingReply(r) == replyState[r] \in {Queued, Dequeued, Replied}'
occurrence = 1
invariant = "TakingReplyStillPrecedesDeadline"
min_counterexample_states = 5

[[mutation]]
id = "ipc-reply-publication-timely-take-commit-omitted"
kind = "transition-effect"
severity = "critical"
model = "ipc-reply-deadline/IpcReplyDeadline"
flow = "ipc-call"
find = "                      THEN VisiblePublication"
replace = "                      THEN PendingPublication"
occurrence = 1
invariant = "PendingPublicationIsOwnedByOneLiveReply"
min_counterexample_states = 5

[[mutation]]
id = "ipc-reply-recv-commit-effect-removed"
kind = "transition-effect"
severity = "critical"
model = "ipc-reply-recv-transaction/IpcReplyRecvTransaction"
flow = "scheduler-dispatch"
find = '''ReceiveQueued ==
    /\ phase = ReplyCommitted
    /\ requestPending
    /\ phase' = Delivered
    /\ requestPending' = FALSE
    /\ nextReplyOwned' = TRUE
    /\ resultKind' = Success
    /\ UNCHANGED <<replyLive, callerRunnable, waiterArmed, serverBlocked>>
'''
replace = '''ReceiveQueued ==
    /\ phase = ReplyCommitted
    /\ requestPending
    /\ phase' = Delivered
    /\ requestPending' = FALSE
    /\ nextReplyOwned' = TRUE
    /\ resultKind' = Success
    /\ replyLive' = TRUE
    /\ UNCHANGED <<callerRunnable, waiterArmed, serverBlocked>>
'''
occurrence = 1
invariant = "ReplyCommitPrecedesReceive"
min_counterexample_states = 3

[[mutation]]
id = "ipc-reply-recv-postcommit-error-untagged"
kind = "transition-effect"
severity = "critical"
model = "ipc-reply-recv-transaction/IpcReplyRecvTransaction"
flow = "scheduler-dispatch"
find = "    /\\ resultKind' = TaggedPostError"
replace = "    /\\ resultKind' = PreError"
occurrence = 1
invariant = "PostCommitFailureIsTagged"
min_counterexample_states = 3

[[mutation]]
id = "ipc-reply-recv-preflight-error-mistagged"
kind = "transition-effect"
severity = "critical"
model = "ipc-reply-recv-transaction/IpcReplyRecvTransaction"
flow = "scheduler-dispatch"
find = "    /\\ resultKind' = PreError"
replace = "    /\\ resultKind' = TaggedPostError"
occurrence = 1
invariant = "PreCommitFailurePreservesReply"
min_counterexample_states = 2

[[mutation]]
id = "ipc-reply-wait-arms-without-polling"
kind = "transition-guard-removal"
severity = "high"
model = "ipc-reply-deadline/IpcReplyDeadline"
flow = "ipc-call"
find = "    /\\ turnPolls[caller] > 0\n    /\\ taskState' = [taskState EXCEPT ![caller] = Armed]"
replace = "    /\\ taskState' = [taskState EXCEPT ![caller] = Armed]"
occurrence = 1
invariant = "TimerArmedOnlyAfterAPoll"
min_counterexample_states = 4

[[mutation]]
id = "ipc-reply-wait-poll-budget-omitted"
kind = "transition-guard-removal"
severity = "high"
model = "ipc-reply-deadline/IpcReplyDeadline"
flow = "ipc-call"
find = "    /\\ turnPolls[caller] < PollsPerTurn\n    /\\ turnPolls' = [turnPolls EXCEPT ![caller] = @ + 1]\n    /\\ UNCHANGED <<now, taskState, waitFor, timerDeadline, replyState, callerOf,"
replace = "    /\\ turnPolls' = [turnPolls EXCEPT ![caller] = @ + 1]\n    /\\ UNCHANGED <<now, taskState, waitFor, timerDeadline, replyState, callerOf,"
occurrence = 1
invariant = "WaitTurnPollsAtMostTwice"
min_counterexample_states = 3

[[mutation]]
id = "ipc-reply-wait-turn-not-closed-on-take"
kind = "transition-effect"
severity = "high"
model = "ipc-reply-deadline/IpcReplyDeadline"
flow = "ipc-call"
find = "    /\\ turnPolls' = [turnPolls EXCEPT ![caller] = 0]\n    /\\ turnTimerArms' = [turnTimerArms EXCEPT ![caller] = 0]\n    /\\ UNCHANGED <<now, taskState, timerDeadline, callerOf, serverOf,"
replace = "    /\\ turnPolls' = turnPolls\n    /\\ turnTimerArms' = [turnTimerArms EXCEPT ![caller] = 0]\n    /\\ UNCHANGED <<now, taskState, timerDeadline, callerOf, serverOf,"
occurrence = 1
invariant = "EveryChargedPollBelongsToALiveWait"
min_counterexample_states = 4

[[mutation]]
id = "ipc-transfer-receiver-set-epoch-guard-removed"
kind = "transition-guard-removal"
severity = "critical"
model = "ipc-transfer-authority/IpcTransferAuthority"
flow = "ipc-handle-transfer"
find = "    /\\ receiverEpoch = receiverSetEpoch"
replace = "    /\\ receiverEpoch \\in ReceiverEpochs"
occurrence = 1
invariant = "ReceiverSetCannotRedirect"
min_counterexample_states = 4

[[mutation]]
id = "ipc-transfer-recipient-guard-removed"
kind = "transition-guard-removal"
severity = "critical"
model = "ipc-transfer-authority/IpcTransferAuthority"
flow = "ipc-handle-transfer"
find = "    /\\ receiver = intendedReceiver"
replace = "    /\\ receiver \\in Receivers"
occurrence = 1
invariant = "OnlyIntendedRecipientInstalls"
min_counterexample_states = 5

[[mutation]]
id = "ipc-transfer-requested-bytes-own-frontier"
kind = "transition-effect"
severity = "critical"
model = "ipc-transfer-authority/IpcTransferAuthority"
flow = "ipc-handle-transfer"
find = "    /\\ committedPosition' = committedPosition + accepted"
replace = "    /\\ committedPosition' = committedPosition + requested"
occurrence = 1
invariant = "AcceptedBytesOwnTheFrontier"
min_counterexample_states = 2

[[mutation]]
id = "msi-committed-rollback-handler-retained"
kind = "transition-revocation"
severity = "critical"
model = "msi-vector-lifecycle/MsiVectorLifecycle"
flow = "msi-vector-ingress"
find = '''    /\ leased' = leased \ Owned
    /\ committed' = committed \ Owned
    /\ routed' = routed \ Owned
    /\ handlerOwner' = [v \in Vectors |-> IF v \in Owned THEN NoDevice ELSE handlerOwner[v]]
'''
replace = '''    /\ leased' = leased
    /\ committed' = committed \ Owned
    /\ routed' = routed \ Owned
    /\ handlerOwner' = handlerOwner
'''
occurrence = 1
invariant = "FailedPublicationHasNoAuthority"
min_counterexample_states = 3

[[mutation]]
id = "netd-admission-class-cap-omitted"
kind = "transition-effect"
severity = "critical"
model = "netd-deferred-reply/NetdDeferredReply"
flow = "netd-deferred-reply-lifecycle"
find = "    /\\ effectiveEnd' = [effectiveEnd EXCEPT ![r] = StampedEndAt(now)]"
replace = "    /\\ effectiveEnd' = [effectiveEnd EXCEPT ![r] = WireEnd]"
occurrence = 1
invariant = "StampedEndMatchesAdmissionClamp"
min_counterexample_states = 2

[[mutation]]
id = "netd-admission-expiry-guard-removed"
kind = "transition-guard-removal"
severity = "critical"
model = "netd-deferred-reply/NetdDeferredReply"
flow = "netd-deferred-reply-lifecycle"
find = '''Admit(r) ==
    /\ phase[r] = Free
    /\ now < WireEnd
    /\ Cardinality(reserved) < MaxPending
'''
replace = '''Admit(r) ==
    /\ phase[r] = Free
    /\ TRUE
    /\ Cardinality(reserved) < MaxPending
'''
occurrence = 1
invariant = "AdmittedBeforeWireDeadline"
min_counterexample_states = 4

[[mutation]]
id = "netd-expiry-reservation-release-omitted"
kind = "transition-effect"
severity = "critical"
model = "netd-deferred-reply/NetdDeferredReply"
flow = "netd-deferred-reply-lifecycle"
find = "    /\\ reserved' = {r \\in reserved : ~ExpiresOnTick(r)}"
replace = "    /\\ reserved' = reserved"
occurrence = 1
invariant = "ReservationMatchesLiveDeferredRequest"
min_counterexample_states = 4

[[mutation]]
id = "netd-requeue-refreshes-stamped-deadline"
kind = "transition-effect"
severity = "critical"
model = "netd-deferred-reply/NetdDeferredReply"
flow = "netd-deferred-reply-lifecycle"
find = '''Requeue(r) ==
    /\ phase[r] = Detached
    /\ phase' = [phase EXCEPT ![r] = Queued]
    /\ UNCHANGED <<now, reserved, replyAttempts, outcome, admittedAt,
                   effectiveEnd, rejection>>
'''
replace = '''Requeue(r) ==
    /\ phase[r] = Detached
    /\ phase' = [phase EXCEPT ![r] = Queued]
    /\ effectiveEnd' = [effectiveEnd EXCEPT ![r] = StampedEndAt(now)]
    /\ UNCHANGED <<now, reserved, replyAttempts, outcome, admittedAt,
                   rejection>>
'''
occurrence = 1
invariant = "StampedEndMatchesAdmissionClamp"
min_counterexample_states = 4

[[mutation]]
id = "nmi-acquires-interrupted-lock"
kind = "transition-effect"
severity = "critical"
model = "exception-retirement-lifecycle/ExceptionRetirementLifecycle"
flow = "exception-retirement"
find = '''    /\ interruptedLockHeld' = lockHeld
    /\ nmiTookLock' = FALSE
'''
replace = '''    /\ interruptedLockHeld' = lockHeld
    /\ nmiTookLock' = lockHeld
'''
occurrence = 1
invariant = "NmiTakesNoTrackedLock"
min_counterexample_states = 2

[[mutation]]
id = "page-cache-cow-dirties-shared-source"
kind = "transition-effect"
severity = "critical"
model = "page-cache-lifecycle/PageCacheLifecycle"
flow = "page-cache-lifecycle"
find = "    /\\ cowSourceClean' = TRUE"
replace = "    /\\ cowSourceClean' = FALSE"
occurrence = 1
invariant = "PrivateCowPreservesSharedClean"
min_counterexample_states = 4

[[mutation]]
id = "page-cache-dirty-reclaim-admitted"
kind = "transition-guard-removal"
severity = "critical"
model = "page-cache-lifecycle/PageCacheLifecycle"
flow = "page-cache-lifecycle"
find = '''BeginReclaim ==
    /\ state = Clean
    /\ ~dirty
'''
replace = '''BeginReclaim ==
    /\ state \in {Clean, Dirty}
    /\ TRUE
'''
occurrence = 1
invariant = "ReclaimStartsOnlyFromClean"
min_counterexample_states = 5

[[mutation]]
id = "page-cache-duplicate-load-owner"
kind = "transition-effect"
severity = "critical"
model = "page-cache-lifecycle/PageCacheLifecycle"
flow = "page-cache-lifecycle"
find = '''BeginLoad(g) ==
    /\ state = Absent
    /\ ~backingRevoked
    /\ g > 0
    /\ state' = Loading
    /\ keyGeneration' = g
    /\ loadOwners' = 1
'''
replace = '''BeginLoad(g) ==
    /\ state \in {Absent, Loading}
    /\ ~backingRevoked
    /\ g > 0
    /\ state' = Loading
    /\ keyGeneration' = g
    /\ loadOwners' = loadOwners + 1
'''
occurrence = 1
invariant = "OneLoadOwner"
min_counterexample_states = 3

[[mutation]]
id = "page-cache-reclaim-before-tlb-ack"
kind = "transition-guard-removal"
severity = "critical"
model = "page-cache-lifecycle/PageCacheLifecycle"
flow = "page-cache-lifecycle"
find = '''CompleteReclaim ==
    /\ state = Reclaiming
    /\ tlbGeneration > 0
    /\ tlbAck = tlbGeneration
'''
replace = '''CompleteReclaim ==
    /\ state = Reclaiming
    /\ tlbGeneration > 0
    /\ TRUE
'''
occurrence = 1
invariant = "FreedAfterExactTlbAck"
min_counterexample_states = 5

[[mutation]]
id = "page-cache-revoked-frame-before-tlb-ack"
kind = "transition-guard-removal"
severity = "critical"
model = "page-cache-lifecycle/PageCacheLifecycle"
flow = "page-cache-lifecycle"
find = '''CompleteRevokedReclaim ==
    /\ state = Revoked
    /\ pageGeneration > 0
    /\ ~dirty
    /\ revokeToken = 1
    /\ tlbGeneration > 0
    /\ tlbAck = tlbGeneration
'''
replace = '''CompleteRevokedReclaim ==
    /\ state = Revoked
    /\ pageGeneration > 0
    /\ ~dirty
    /\ revokeToken = 1
    /\ TRUE
    /\ TRUE
'''
occurrence = 1
invariant = "FreedAfterExactTlbAck"
min_counterexample_states = 4

[[mutation]]
id = "page-cache-revoked-generation-reloads"
kind = "transition-guard-removal"
severity = "critical"
model = "page-cache-lifecycle/PageCacheLifecycle"
flow = "page-cache-lifecycle"
find = '''BeginLoad(g) ==
    /\ state = Absent
    /\ ~backingRevoked
'''
replace = '''BeginLoad(g) ==
    /\ state = Absent
    /\ TRUE
'''
occurrence = 1
invariant = "RevokedBackingCannotReload"
min_counterexample_states = 4

[[mutation]]
id = "page-table-publication-log-omitted"
kind = "transition-effect"
severity = "critical"
model = "page-table-map-transaction/PageTableMapTransaction"
flow = "memory-map"
find = "    /\\ commitLog' = Append(commitLog, table)"
replace = "    /\\ commitLog' = commitLog"
occurrence = 1
invariant = "PublishedTopologyHasRollbackCustody"
min_counterexample_states = 3

[[mutation]]
id = "pager-fault-slot-claim-before-block"
kind = "transition-guard-removal"
severity = "critical"
model = "pager-fault-slot-lifecycle/PagerFaultSlotLifecycle"
flow = "pager-fault-slot-lifecycle"
find = '''ClaimReply(replyToken, endpointMatches, requestMatches) ==
    /\ state = Dispatched
    /\ dispatcherOwns
    /\ replyToken = token
'''
replace = '''ClaimReply(replyToken, endpointMatches, requestMatches) ==
    /\ state \in {Blocked, Dispatched}
    /\ replyToken = token
    /\ endpointMatches
    /\ requestMatches
'''
occurrence = 1
invariant = "ReplyClaimRequiresDispatch"
min_counterexample_states = 4

[[mutation]]
id = "pager-fault-slot-dispatch-ownership-bypass"
kind = "transition-effect"
severity = "critical"
model = "pager-fault-slot-lifecycle/PagerFaultSlotLifecycle"
flow = "pager-fault-slot-lifecycle"
find = '''    /\ state' = Dispatched
    /\ dispatcherOwns' = TRUE
'''
replace = '''    /\ state' = Dispatched
    /\ dispatcherOwns' = FALSE
'''
occurrence = 1
invariant = "DispatchedHasExactOwner"
min_counterexample_states = 3

[[mutation]]
id = "pager-fault-slot-endpoint-bypass"
kind = "transition-guard-removal"
severity = "critical"
model = "pager-fault-slot-lifecycle/PagerFaultSlotLifecycle"
flow = "pager-fault-slot-lifecycle"
find = '''    /\ replyToken = token
    /\ endpointMatches
    /\ requestMatches
'''
replace = '''    /\ replyToken = token
    /\ TRUE
    /\ requestMatches
'''
occurrence = 1
invariant = "ReplyClaimRequiresBlocked"
min_counterexample_states = 3

[[mutation]]
id = "pager-fault-slot-token-bypass"
kind = "transition-guard-removal"
severity = "critical"
model = "pager-fault-slot-lifecycle/PagerFaultSlotLifecycle"
flow = "pager-fault-slot-lifecycle"
find = '''    /\ state = Dispatched
    /\ dispatcherOwns
    /\ replyToken = token
    /\ endpointMatches
'''
replace = '''    /\ state = Dispatched
    /\ dispatcherOwns
    /\ TRUE
    /\ endpointMatches
'''
occurrence = 1
invariant = "ReplyClaimRequiresBlocked"
min_counterexample_states = 3

[[mutation]]
id = "pager-frame-grant-binding-bypass"
kind = "transition-guard-removal"
severity = "critical"
model = "pager-frame-grant-lifecycle/PagerFrameGrantLifecycle"
flow = "pager-frame-grant-lifecycle"
find = '''Consume(token, handleGeneration) ==
    /\ state = Granted
    /\ token = boundToken
'''
replace = '''Consume(token, handleGeneration) ==
    /\ state = Granted
    /\ TRUE
'''
occurrence = 1
invariant = "TransferRequiresExactBinding"
min_counterexample_states = 3

[[mutation]]
id = "pager-frame-grant-generation-bypass"
kind = "transition-guard-removal"
severity = "critical"
model = "pager-frame-grant-lifecycle/PagerFrameGrantLifecycle"
flow = "pager-frame-grant-lifecycle"
find = '''    /\ token = boundToken
    /\ handleGeneration = generation
    /\ leafPrepared
    /\ state' = Consumed
'''
replace = '''    /\ token = boundToken
    /\ TRUE
    /\ leafPrepared
    /\ state' = Consumed
'''
occurrence = 1
invariant = "TransferRequiresExactGeneration"
min_counterexample_states = 3

[[mutation]]
id = "pager-frame-grant-leaf-preparation"
kind = "transition-guard-removal"
severity = "critical"
model = "pager-frame-grant-lifecycle/PagerFrameGrantLifecycle"
flow = "pager-frame-grant-lifecycle"
find = '''    /\ wiredReserve > 0
    /\ leafPrepared
    /\ token \in 1..2
'''
replace = '''    /\ wiredReserve > 0
    /\ TRUE
    /\ token \in 1..2
'''
occurrence = 1
invariant = "PublishedGrantRequiresPreparedLeaf"
min_counterexample_states = 2

[[mutation]]
id = "pager-frame-grant-wx-publication"
kind = "transition-guard-removal"
severity = "critical"
model = "pager-frame-grant-lifecycle/PagerFrameGrantLifecycle"
flow = "pager-frame-grant-lifecycle"
find = '''    /\ token \in 1..2
    /\ ~(mayWrite /\ mayExecute)
    /\ state' = Granted
'''
replace = '''    /\ token \in 1..2
    /\ TRUE
    /\ state' = Granted
'''
occurrence = 1
invariant = "PublishedRightsAreWxSafe"
min_counterexample_states = 2

[[mutation]]
id = "pager-vma-publication-identity-bypass"
kind = "transition-guard-removal"
severity = "critical"
model = "pager-vma-publication/PagerVmaPublication"
flow = "pager-vma-publication"
find = '''    /\ sequence % 2 = 0
    /\ identityExact
    /\ permissionAllowed
'''
replace = '''    /\ sequence % 2 = 0
    /\ TRUE
    /\ permissionAllowed
'''
occurrence = 1
invariant = "LookupRequiresExactIdentity"
min_counterexample_states = 4

[[mutation]]
id = "pager-vma-publication-odd-commit"
kind = "transition-effect"
severity = "critical"
model = "pager-vma-publication/PagerVmaPublication"
flow = "pager-vma-publication"
find = '''    /\ state' = Published
    /\ sequence' = sequence + 1
'''
replace = '''    /\ state' = Published
    /\ sequence' = sequence
'''
occurrence = 1
invariant = "PublishedSequenceIsStable"
min_counterexample_states = 3

[[mutation]]
id = "pager-vma-publication-overlap-bypass"
kind = "transition-guard-removal"
severity = "critical"
model = "pager-vma-publication/PagerVmaPublication"
flow = "pager-vma-publication"
find = '''    /\ ~(mayWrite /\ mayExecute)
    /\ ~hasOverlap
    /\ state' = Writing
'''
replace = '''    /\ ~(mayWrite /\ mayExecute)
    /\ TRUE
    /\ state' = Writing
'''
occurrence = 1
invariant = "PublishedVmaIsNonoverlapping"
min_counterexample_states = 3

[[mutation]]
id = "pager-vma-publication-permission-bypass"
kind = "transition-guard-removal"
severity = "critical"
model = "pager-vma-publication/PagerVmaPublication"
flow = "pager-vma-publication"
find = '''    /\ identityExact
    /\ permissionAllowed
    /\ lookupAdmitted' = TRUE
'''
replace = '''    /\ identityExact
    /\ TRUE
    /\ lookupAdmitted' = TRUE
'''
occurrence = 1
invariant = "LookupRequiresAllowedAccess"
min_counterexample_states = 4

[[mutation]]
id = "per-cpu-clockevent-deadline-before-lvt"
kind = "transition-guard-removal"
severity = "critical"
model = "per-cpu-clockevent-lifecycle/PerCpuClockeventLifecycle"
flow = "per-cpu-clockevent-lifecycle"
find = '''    /\ state[cpu] = LvtReady
    /\ lvtUnmasked[cpu]
'''
replace = '''    /\ state[cpu] = Offline
'''
occurrence = 1
invariant = "ArmedDeadlineRequiresUnmaskedLvt"
min_counterexample_states = 2

[[mutation]]
id = "per-cpu-clockevent-online-before-programming"
kind = "transition-order"
severity = "critical"
model = "per-cpu-clockevent-lifecycle/PerCpuClockeventLifecycle"
flow = "per-cpu-clockevent-lifecycle"
find = '''ConfigureLvt(cpu) ==
    /\ cpu \in ApCpus
    /\ state[cpu] = Offline
    /\ state' = [state EXCEPT ![cpu] = LvtReady]
    /\ lvtUnmasked' = [lvtUnmasked EXCEPT ![cpu] = TRUE]
    /\ UNCHANGED <<now, deadline, schedulerTurns>>
'''
replace = '''ConfigureLvt(cpu) ==
    /\ cpu \in ApCpus
    /\ state[cpu] = Offline
    /\ state' = [state EXCEPT ![cpu] = Online]
    /\ UNCHANGED <<now, deadline, lvtUnmasked, schedulerTurns>>
'''
occurrence = 1
invariant = "ApOnlineRequiresProgrammedDeadline"
min_counterexample_states = 2

[[mutation]]
id = "process-lifecycle-partial-frame-rollback-dropped"
kind = "transition-effect"
severity = "critical"
model = "process-lifecycle-transaction/ProcessLifecycleTransaction"
flow = "process-lifecycle-transaction"
find = '''    /\ phase' = Idle
    /\ freeFrames' = freeFrames + unpublishedFrames'''
replace = '''    /\ phase' = Idle
    /\ freeFrames' = freeFrames'''
occurrence = 1
invariant = "FrameConservation"
min_counterexample_states = 3

[[mutation]]
id = "process-lifecycle-reap-with-live-task"
kind = "transition-guard-removal"
severity = "critical"
model = "process-lifecycle-transaction/ProcessLifecycleTransaction"
flow = "process-lifecycle-transaction"
find = '''QueueReap ==
    /\ phase = Exiting
    /\ tasks = 0
    /\ references = 0'''
replace = '''QueueReap ==
    /\ phase = Exiting
    /\ TRUE'''
occurrence = 1
invariant = "ReapRequiresNoAuthority"
min_counterexample_states = 5

[[mutation]]
id = "process-lifecycle-transaction-id-reuse"
kind = "transition-effect"
severity = "critical"
model = "process-lifecycle-transaction/ProcessLifecycleTransaction"
flow = "process-lifecycle-transaction"
find = '''ReserveExec ==
    /\ phase = Running
    /\ tasks = 1
    /\ CanAllocateTxn
    /\ phase' = ExecReserved
    /\ activeTxn' = nextTxn
    /\ nextTxn' = nextTxn + 1'''
replace = '''ReserveExec ==
    /\ phase = Running
    /\ tasks = 1
    /\ CanAllocateTxn
    /\ phase' = ExecReserved
    /\ activeTxn' = nextTxn
    /\ nextTxn' = nextTxn'''
occurrence = 1
invariant = "GenerationNeverAliases"
min_counterexample_states = 5

[[mutation]]
id = "product-boot-display-authority-guard-removed"
kind = "transition-guard-removal"
severity = "critical"
model = "product-boot/ProductBoot"
flow = "commercial-product-boot"
find = '''PresentFirstFrame ==
    /\ mode = Interactive
    /\ phase = WaylandReady
    /\ displayProven
'''
replace = '''PresentFirstFrame ==
    /\ mode = Interactive
    /\ phase = WaylandReady
    /\ TRUE
'''
occurrence = 1
invariant = "PresentedHasCompleteAuthorityChain"
min_counterexample_states = 2

[[mutation]]
id = "scheduler-active-balance-due-guard-removed"
kind = "transition-guard-removal"
severity = "critical"
model = "scheduler-active-balance/SchedulerActiveBalance"
flow = "scheduler-dispatch"
find = '''TransferOne(task) ==
    /\ Due
    /\ Migratable(task)'''
replace = '''TransferOne(task) ==
    /\ TRUE
    /\ Migratable(task)'''
occurrence = 1
invariant = "EveryTransferOccursOnDueOpportunity"
min_counterexample_states = 4

[[mutation]]
id = "scheduler-active-balance-execution-owner-admitted"
kind = "transition-guard-removal"
severity = "critical"
model = "scheduler-active-balance/SchedulerActiveBalance"
flow = "scheduler-dispatch"
find = '''QueueOnlyUnowned(task) ==
    /\ task \in sourceQueue
    /\ ~ExecutionOwned(task)
    /\ task \notin transitioning'''
replace = '''QueueOnlyUnowned(task) == SourceOwned(task)'''
occurrence = 1
invariant = "EachTaskHasExactlyOneCustody"
min_counterexample_states = 2

[[mutation]]
id = "scheduler-active-balance-mailbox-request-omitted"
kind = "transition-effect"
severity = "critical"
model = "scheduler-active-balance/SchedulerActiveBalance"
flow = "scheduler-dispatch"
find = '''    /\ requestTarget' = [requestTarget EXCEPT ![task] = Target]'''
replace = '''    /\ requestTarget' = [requestTarget EXCEPT ![task] = NoCpu]'''
occurrence = 1
invariant = "RemoteMailboxHasExactRequest"
min_counterexample_states = 3

[[mutation]]
id = "scheduler-active-balance-target-redirected"
kind = "transition-effect"
severity = "critical"
model = "scheduler-active-balance/SchedulerActiveBalance"
flow = "scheduler-dispatch"
find = '''    /\ mailbox' = [mailbox EXCEPT ![task] = Target]
    /\ requestTarget' = [requestTarget EXCEPT ![task] = Target]'''
replace = '''    /\ mailbox' = [mailbox EXCEPT ![task] = WrongTarget]
    /\ requestTarget' = [requestTarget EXCEPT ![task] = WrongTarget]'''
occurrence = 1
invariant = "RemoteCustodyUsesEligibleTarget"
min_counterexample_states = 3

[[mutation]]
id = "scheduler-class-burst-cap-removed"
kind = "transition-guard-removal"
severity = "critical"
model = "scheduler-cpu-distribution/SchedulerCpuDistribution"
flow = "scheduler-dispatch"
find = '''    /\ (ready \cap UserTasks = {} \/ systemBurst[cpu] < MaxSystemBurst)'''
replace = '''    /\ TRUE'''
occurrence = 1
invariant = "SystemBurstIsBounded"
min_counterexample_states = 3

[[mutation]]
id = "scheduler-demotion-low-weight-promotion"
kind = "transition-effect"
severity = "critical"
model = "scheduler-thread-demotion/SchedulerThreadDemotion"
flow = "scheduler-dispatch"
find = '''    /\ baseWeight' =
        [baseWeight EXCEPT ![thread] =
            IF @ = Elevated THEN Normal ELSE @]'''
replace = '''    /\ baseWeight' = [baseWeight EXCEPT ![thread] = Normal]'''
occurrence = 1
invariant = "LowWeightNeverPromoted"
min_counterexample_states = 2

[[mutation]]
id = "scheduler-demotion-permanent-weight-cap-removed"
kind = "transition-effect"
severity = "critical"
model = "scheduler-thread-demotion/SchedulerThreadDemotion"
flow = "scheduler-dispatch"
find = '''    /\ baseWeight' =
        [baseWeight EXCEPT ![thread] =
            IF @ = Elevated THEN Normal ELSE @]'''
replace = '''    /\ baseWeight' = baseWeight'''
occurrence = 1
invariant = "DemotionCapsPermanentWeight"
min_counterexample_states = 2

[[mutation]]
id = "scheduler-dispatch-exact-queue-cpu-guard-removed"
kind = "transition-guard-removal"
severity = "critical"
model = "scheduler-cpu-ownership/SchedulerCpuOwnership"
flow = "scheduler-cpu-ownership"
find = '''BeginDispatch(cpu, nextTask) ==
    /\ ~panicked
    /\ preemptDepth[cpu] = 0
    /\ guardTask[cpu] = NoTask
    /\ transitionFrom[cpu] = NoTask
    /\ nextTask \in ready
    /\ nextTask \notin blocked
    /\ nextTask \notin mailbox
    /\ StackTransitionCommitted(nextTask)
    /\ localQueueCpu[nextTask] = cpu'''
replace = '''BeginDispatch(cpu, nextTask) ==
    /\ ~panicked
    /\ preemptDepth[cpu] = 0
    /\ guardTask[cpu] = NoTask
    /\ transitionFrom[cpu] = NoTask
    /\ nextTask \in ready
    /\ nextTask \notin blocked
    /\ nextTask \notin mailbox
    /\ StackTransitionCommitted(nextTask)
    /\ TRUE'''
occurrence = 1
invariant = "RecordedDispatchClaimIsLocalExactRunnable"
min_counterexample_states = 2

[[mutation]]
id = "scheduler-dispatch-queue-custody-retained"
kind = "transition-effect"
severity = "critical"
model = "scheduler-cpu-ownership/SchedulerCpuOwnership"
flow = "scheduler-cpu-ownership"
find = '''       /\ taskCpu' = [taskCpu EXCEPT
            ![nextTask] = cpu]
       /\ ready' = ready \ {nextTask}
       /\ legacyReady' = legacyReady \ {nextTask}
       /\ localQueueCpu' = [localQueueCpu EXCEPT ![nextTask] = NoCpu]'''
replace = '''       /\ taskCpu' = [taskCpu EXCEPT
            ![nextTask] = cpu]
       /\ ready' = ready \ {nextTask}
       /\ legacyReady' = legacyReady \ {nextTask}
       /\ localQueueCpu' = [localQueueCpu EXCEPT ![nextTask] = @]'''
occurrence = 1
invariant = "RunningTasksHaveNoQueueCustody"
min_counterexample_states = 2

[[mutation]]
id = "scheduler-dispatch-runnable-guard-removed"
kind = "transition-guard-removal"
severity = "critical"
model = "scheduler-cpu-ownership/SchedulerCpuOwnership"
flow = "scheduler-cpu-ownership"
find = '''BeginDispatch(cpu, nextTask) ==
    /\ ~panicked
    /\ preemptDepth[cpu] = 0
    /\ guardTask[cpu] = NoTask
    /\ transitionFrom[cpu] = NoTask
    /\ nextTask \in ready
    /\ nextTask \notin blocked
    /\ nextTask \notin mailbox
    /\ StackTransitionCommitted(nextTask)
    /\ localQueueCpu[nextTask] = cpu
    /\ runnable[nextTask]'''
replace = '''BeginDispatch(cpu, nextTask) ==
    /\ ~panicked
    /\ preemptDepth[cpu] = 0
    /\ guardTask[cpu] = NoTask
    /\ transitionFrom[cpu] = NoTask
    /\ nextTask \in ready
    /\ nextTask \notin blocked
    /\ nextTask \notin mailbox
    /\ StackTransitionCommitted(nextTask)
    /\ localQueueCpu[nextTask] = cpu
    /\ TRUE'''
occurrence = 1
invariant = "RecordedDispatchClaimIsLocalExactRunnable"
min_counterexample_states = 3

[[mutation]]
id = "scheduler-fair-user-vruntime-guard-removed"
kind = "transition-guard-removal"
severity = "critical"
model = "scheduler-cpu-distribution/SchedulerCpuDistribution"
flow = "scheduler-dispatch"
find = '''    /\ AdmittedFairPick(task, cpu, UserTasks)'''
replace = '''    /\ TRUE'''
occurrence = 1
invariant = "FairUserSelectionUsesRuntime"
min_counterexample_states = 3

[[mutation]]
id = "scheduler-locality-lag-bound-removed"
kind = "transition-guard-removal"
severity = "critical"
model = "scheduler-cpu-distribution/SchedulerCpuDistribution"
flow = "scheduler-dispatch"
find = '''LocalityAdmissible(task, cpu, classTasks) ==
    /\ lastCpu[task] = cpu
    /\ \A peer \in ready \cap classTasks:
           runtime[task] <= runtime[peer] + MaxLocalityLag'''
replace = '''LocalityAdmissible(task, cpu, classTasks) ==
    /\ lastCpu[task] = cpu
    /\ \A peer \in ready \cap classTasks: TRUE'''
occurrence = 1
invariant = "FairUserSelectionUsesRuntime"
min_counterexample_states = 3

[[mutation]]
id = "scheduler-outgoing-stack-owner-retained"
kind = "transition-effect"
severity = "critical"
model = "scheduler-cpu-ownership/SchedulerCpuOwnership"
flow = "scheduler-cpu-ownership"
find = '''CommitStackSwitch(cpu) ==
    /\ ~panicked
    /\ transitionFrom[cpu] # NoTask
    /\ LET previous == transitionFrom[cpu] IN
       /\ transitionFrom' = [transitionFrom EXCEPT ![cpu] = NoTask]
       /\ taskCpu' = [taskCpu EXCEPT ![previous] = NoCpu]
'''
replace = '''CommitStackSwitch(cpu) ==
    /\ ~panicked
    /\ transitionFrom[cpu] # NoTask
    /\ LET previous == transitionFrom[cpu] IN
       /\ transitionFrom' = [transitionFrom EXCEPT ![cpu] = NoTask]
       /\ taskCpu' = [taskCpu EXCEPT ![previous] = cpu]
'''
occurrence = 1
invariant = "ReadyTasksAreNotRunning"
min_counterexample_states = 3

[[mutation]]
id = "scheduler-ready-authority-lost"
kind = "transition-effect"
severity = "critical"
model = "scheduler-cpu-ownership/SchedulerCpuOwnership"
flow = "scheduler-cpu-ownership"
find = '''       /\ ready' = IF previous \in (blocked \cup ready)
                    THEN ready ELSE ready \cup {previous}'''
replace = '''       /\ ready' = ready'''
occurrence = 1
invariant = "LegacyReadyHasPublishedCustody"
min_counterexample_states = 3

[[mutation]]
id = "scheduler-transition-legacy-ready-before-custody"
kind = "transition-guard-removal"
severity = "critical"
model = "scheduler-cpu-ownership/SchedulerCpuOwnership"
flow = "scheduler-cpu-ownership"
find = '''PublishLegacyReady(task) ==
    /\ ~panicked
    /\ task \in blocked
    /\ task \in mailbox
    /\ mailboxCpu[task] \in Cpus
    /\ task \notin legacyReady'''
replace = '''PublishLegacyReady(task) ==
    /\ ~panicked
    /\ task \in blocked
    /\ TRUE
    /\ task \notin legacyReady'''
occurrence = 1
invariant = "LegacyReadyHasPublishedCustody"
min_counterexample_states = 5

[[mutation]]
id = "scheduler-transition-mailbox-publication-omitted"
kind = "transition-effect"
severity = "critical"
model = "scheduler-cpu-ownership/SchedulerCpuOwnership"
flow = "scheduler-cpu-ownership"
find = "    /\\ mailbox' = mailbox \\cup {task}"
replace = "    /\\ mailbox' = mailbox"
occurrence = 1
invariant = "MailboxCpuMatchesPublishedMailbox"
min_counterexample_states = 5

[[mutation]]
id = "scheduler-transition-precommit-claim"
kind = "transition-guard-removal"
severity = "critical"
model = "scheduler-cpu-ownership/SchedulerCpuOwnership"
flow = "scheduler-cpu-ownership"
find = '''StackTransitionCommitted(task) ==
    \A source \in Cpus: transitionFrom[source] # task'''
replace = '''StackTransitionCommitted(task) ==
    TRUE'''
occurrence = 1
invariant = "ReadyTasksAreNotRunning"
min_counterexample_states = 7

[[mutation]]
id = "scheduler-transition-wake-dropped"
kind = "transition-effect"
severity = "critical"
model = "scheduler-wakeup/SchedulerWakeup"
flow = "scheduler-lifecycle"
find = '''WakeTask(task) ==
    /\ task \in Tasks
    /\ taskState[task] \in {Armed, Blocked}
    /\ taskState' = [taskState EXCEPT ![task] = Ready]
'''
replace = '''WakeTask(task) ==
    /\ task \in Tasks
    /\ taskState[task] \in {Armed, Blocked}
    /\ taskState' = [taskState EXCEPT ![task] =
        IF transition = task THEN Blocked ELSE Ready]
'''
occurrence = 1
invariant = "TransitionWakeSurvives"
min_counterexample_states = 4

[[mutation]]
id = "smp-ipi-claim-drops-request-generation"
kind = "transition-effect"
severity = "critical"
model = "smp-reschedule-ipi/SmpRescheduleIpi"
flow = "smp-reschedule-ipi-lifecycle"
find = "    /\\ claimedSeq' = [claimedSeq EXCEPT ![cpu] = requestSeq[cpu]]"
replace = "    /\\ claimedSeq' = [claimedSeq EXCEPT ![cpu] = consumeSeq[cpu]]"
occurrence = 2
invariant = "NoUnownedClearedRequest"
min_counterexample_states = 3

[[mutation]]
id = "smp-ipi-exact-target-publication-removed"
kind = "transition-effect"
severity = "critical"
model = "smp-reschedule-ipi/SmpRescheduleIpi"
flow = "smp-reschedule-ipi-lifecycle"
find = "    /\\ request' = [request EXCEPT ![target] = TRUE]"
replace = "    /\\ request' = request"
occurrence = 2
invariant = "NoUnownedClearedRequest"
min_counterexample_states = 3

[[mutation]]
id = "smp-ipi-locked-target-custody-not-published"
kind = "transition-effect"
severity = "critical"
model = "smp-reschedule-ipi/SmpRescheduleIpi"
flow = "smp-reschedule-ipi-lifecycle"
find = "[targetCustodyOwed EXCEPT\n          ![target] = IF ~request[target] THEN TRUE ELSE @]"
replace = "[targetCustodyOwed EXCEPT\n          ![target] = FALSE]"
occurrence = 1
invariant = "PendingTargetHasAuthority"
min_counterexample_states = 2

[[mutation]]
id = "smp-ipi-self-notification-restored"
kind = "transition-effect"
severity = "critical"
model = "smp-reschedule-ipi/SmpRescheduleIpi"
flow = "smp-reschedule-ipi-lifecycle"
find = "![target] = IF ~request[target] /\\ source # target THEN source ELSE @"
replace = "![target] = IF ~request[target] THEN source ELSE @"
occurrence = 1
invariant = "RemoteNotifyNeverTargetsFlusher"
min_counterexample_states = 2

[[mutation]]
id = "smp-ring3-activate-without-bind-admitted"
kind = "transition-guard-removal"
severity = "critical"
model = "smp-ring3-qualification/SmpRing3Qualification"
flow = "smp-ring3-qualification"
find = '''Activate ==
    /\ result = Pending
    /\ BindingIsLiveExact'''
replace = '''Activate ==
    /\ result = Pending
    /\ TRUE'''
occurrence = 1
invariant = "BindingLifecycleIsExact"
min_counterexample_states = 2

[[mutation]]
id = "smp-ring3-activation-deadline-stamp-bypassed"
kind = "transition-effect"
severity = "critical"
model = "smp-ring3-qualification/SmpRing3Qualification"
flow = "smp-ring3-qualification"
find = "    /\\ absoluteDeadline' = now + QualifiedDeadline"
replace = "    /\\ absoluteDeadline' = now + QualifiedDeadline + 1"
occurrence = 1
invariant = "ActivationStampsImmutableAbsoluteDeadline"
min_counterexample_states = 4

[[mutation]]
id = "smp-ring3-bind-after-activation-admitted"
kind = "transition-guard-removal"
severity = "critical"
model = "smp-ring3-qualification/SmpRing3Qualification"
flow = "smp-ring3-qualification"
find = '''Bind(owner, target) ==
    /\ result = Pending
    /\ bindingState = Deferred'''
replace = '''Bind(owner, target) ==
    /\ result = Pending
    /\ bindingState \in {Deferred, Active}'''
occurrence = 1
invariant = "BindBeforeActivationAndNoRebind"
min_counterexample_states = 5

[[mutation]]
id = "smp-ring3-bind-owner-authority-bypassed"
kind = "transition-guard-removal"
severity = "critical"
model = "smp-ring3-qualification/SmpRing3Qualification"
flow = "smp-ring3-qualification"
find = '''    /\ owner = SessiondAuthority
    /\ owner = liveOwnerAuthority
    /\ owner = deferredOwnerAuthority'''
replace = '''    /\ TRUE
    /\ TRUE
    /\ TRUE'''
occurrence = 1
invariant = "BindingLifecycleIsExact"
min_counterexample_states = 3

[[mutation]]
id = "smp-ring3-bind-target-generation-bypassed"
kind = "transition-guard-removal"
severity = "critical"
model = "smp-ring3-qualification/SmpRing3Qualification"
flow = "smp-ring3-qualification"
find = '''    /\ target = liveTargetIdentity
    /\ target = deferredTargetIdentity'''
replace = '''    /\ TRUE
    /\ TRUE'''
occurrence = 1
invariant = "BindingLifecycleIsExact"
min_counterexample_states = 3

[[mutation]]
id = "smp-ring3-binding-id-zero-admitted"
kind = "transition-effect"
severity = "critical"
model = "smp-ring3-qualification/SmpRing3Qualification"
flow = "smp-ring3-qualification"
find = '''CorrectRecord(worker) ==
    RecordFor(worker, worker, QualificationPid, worker + 1, QualifiedWork,
              now, absoluteDeadline, bindingId)'''
replace = '''CorrectRecord(worker) ==
    RecordFor(worker, worker, QualificationPid, worker + 1, QualifiedWork,
              now, absoluteDeadline, NoBindingId)'''
occurrence = 1
invariant = "NonzeroUniformKernelBindingId"
min_counterexample_states = 5

[[mutation]]
id = "smp-ring3-complete-before-join-admitted"
kind = "transition-guard-removal"
severity = "critical"
model = "smp-ring3-qualification/SmpRing3Qualification"
flow = "smp-ring3-qualification"
find = '''RecordComplete ==
    /\ ActiveRecordAdmission(0)
    /\ AllJoined'''
replace = '''RecordComplete ==
    /\ ActiveRecordAdmission(0)
    /\ TRUE'''
occurrence = 1
invariant = "WorkerFsmAndJoinBeforeComplete"
min_counterexample_states = 5

[[mutation]]
id = "smp-ring3-complete-omission-admitted"
kind = "transition-guard-removal"
severity = "critical"
model = "smp-ring3-qualification/SmpRing3Qualification"
flow = "smp-ring3-qualification"
find = '''Succeed ==
    /\ result = Pending
    /\ bindingState = Active
    /\ TerminalRecorded
    /\ QualificationEvidenceEventCount = 3 * workerCount + 1'''
replace = '''Succeed ==
    /\ result = Pending
    /\ bindingState = Active
    /\ AllJoined'''
occurrence = 1
invariant = "SuccessfulQualificationHasEveryExactEvidenceRecord"
min_counterexample_states = 5

[[mutation]]
id = "smp-ring3-critical-drop-accounting-bypassed"
kind = "transition-effect"
severity = "critical"
model = "smp-ring3-qualification/SmpRing3Qualification"
flow = "smp-ring3-qualification"
find = '''DropReady(worker) ==
    /\ CriticalOutputDropAdmission(worker)
    /\ worker = 0
    /\ workerPhase[worker] = WorkerInit
    /\ evidence' = [evidence EXCEPT ![Ready][worker] = CorrectRecord(worker)]
    /\ workerPhase' = [workerPhase EXCEPT ![worker] = WorkerReady]
    /\ catalog' = [catalog EXCEPT
                       !.output.criticalDropped = TRUE,
                       !.output.qualificationMilestonesDropped = 1,
                       !.output.qualificationDebugBytesDiscarded = 1]'''
replace = '''DropReady(worker) ==
    /\ CriticalOutputDropAdmission(worker)
    /\ worker = 0
    /\ workerPhase[worker] = WorkerInit
    /\ evidence' = [evidence EXCEPT ![Ready][worker] = CorrectRecord(worker)]
    /\ workerPhase' = [workerPhase EXCEPT ![worker] = WorkerReady]
    /\ catalog' = [catalog EXCEPT !.output.criticalDropped = TRUE]'''
occurrence = 1
invariant = "QualificationCriticalOutputAccountingIsExact"
min_counterexample_states = 6

[[mutation]]
id = "smp-ring3-deadline-admission-gate-bypassed"
kind = "transition-guard-removal"
severity = "critical"
model = "smp-ring3-qualification/SmpRing3Qualification"
flow = "smp-ring3-qualification"
find = '''ActiveRecordAdmission(worker) ==
    /\ result = Pending
    /\ bindingState = Active
    /\ worker \in ExpectedWorkers
    /\ bindingId = KernelBindingId
    /\ liveOwnerAuthority = boundOwnerAuthority
    /\ liveTargetIdentity = boundTargetIdentity
    /\ now < absoluteDeadline'''
replace = '''ActiveRecordAdmission(worker) ==
    /\ result = Pending
    /\ bindingState = Active
    /\ worker \in ExpectedWorkers
    /\ bindingId = KernelBindingId
    /\ liveOwnerAuthority = boundOwnerAuthority
    /\ liveTargetIdentity = boundTargetIdentity
    /\ TRUE'''
occurrence = 1
invariant = "NoAcceptedEvidenceAtOrAfterDeadline"
min_counterexample_states = 5

[[mutation]]
id = "smp-ring3-deadline-refresh-accepted"
kind = "transition-effect"
severity = "critical"
model = "smp-ring3-qualification/SmpRing3Qualification"
flow = "smp-ring3-qualification"
find = '''RejectDeadlineRefresh ==
    /\ result = Pending
    /\ bindingState = Active
    /\ UNCHANGED vars'''
replace = '''RejectDeadlineRefresh ==
    /\ result = Pending
    /\ bindingState = Active
    /\ absoluteDeadline' = absoluteDeadline + 1
    /\ UNCHANGED <<workerCount, evidence, completed, workerPhase,
                    bindingState, liveOwnerAuthority, liveTargetIdentity,
                    deferredOwnerAuthority, deferredTargetIdentity,
                    boundOwnerAuthority, boundTargetIdentity, bindingId,
                    activationCount, activationTick, now,
                    uiReady, catalog, duplicateEvidenceAccepted, expiredEvidenceAccepted, result>>'''
occurrence = 1
invariant = "ActivationStampsImmutableAbsoluteDeadline"
min_counterexample_states = 5

[[mutation]]
id = "smp-ring3-duplicate-evidence-accepted"
kind = "transition-effect"
severity = "critical"
model = "smp-ring3-qualification/SmpRing3Qualification"
flow = "smp-ring3-qualification"
find = '''RejectDuplicateEvidence ==
    /\ result = Pending
    /\ \E worker \in ExpectedWorkers:
          Recorded(Ready, worker) \/ Recorded(Start, worker) \/ Recorded(Finish, worker)
    /\ UNCHANGED vars'''
replace = '''RejectDuplicateEvidence ==
    /\ result = Pending
    /\ \E worker \in ExpectedWorkers:
          Recorded(Ready, worker) \/ Recorded(Start, worker) \/ Recorded(Finish, worker)
    /\ duplicateEvidenceAccepted' = TRUE
    /\ UNCHANGED <<workerCount, evidence, completed, workerPhase,
                    bindingState, liveOwnerAuthority, liveTargetIdentity,
                    deferredOwnerAuthority, deferredTargetIdentity,
                    boundOwnerAuthority, boundTargetIdentity, bindingId,
                    activationCount, activationTick, absoluteDeadline, now,
                    uiReady, catalog, expiredEvidenceAccepted, result>>'''
occurrence = 1
invariant = "MalformedEvidenceIsNeverAccepted"
min_counterexample_states = 5

[[mutation]]
id = "smp-ring3-expired-evidence-accepted"
kind = "transition-effect"
severity = "critical"
model = "smp-ring3-qualification/SmpRing3Qualification"
flow = "smp-ring3-qualification"
find = '''RejectExpiredEvidence ==
    /\ result = Pending
    /\ bindingState = Active
    /\ now >= absoluteDeadline
    /\ UNCHANGED vars'''
replace = '''RejectExpiredEvidence ==
    /\ result = Pending
    /\ bindingState = Active
    /\ now >= absoluteDeadline
    /\ expiredEvidenceAccepted' = TRUE
    /\ UNCHANGED <<workerCount, evidence, completed, workerPhase,
                    bindingState, liveOwnerAuthority, liveTargetIdentity,
                    deferredOwnerAuthority, deferredTargetIdentity,
                    boundOwnerAuthority, boundTargetIdentity, bindingId,
                    activationCount, activationTick, absoluteDeadline, now,
                    uiReady, catalog, duplicateEvidenceAccepted, result>>'''
occurrence = 1
invariant = "MalformedEvidenceIsNeverAccepted"
min_counterexample_states = 5

[[mutation]]
id = "smp-ring3-finish-before-start-admitted"
kind = "transition-guard-removal"
severity = "critical"
model = "smp-ring3-qualification/SmpRing3Qualification"
flow = "smp-ring3-qualification"
find = "    /\\ FinishAdmission(worker)"
replace = "    /\\ TRUE"
occurrence = 1
invariant = "FinishRequiresOwnStartAndGlobalBoundedWork"
min_counterexample_states = 2

[[mutation]]
id = "smp-ring3-foreign-worker-evidence-accepted"
kind = "transition-effect"
severity = "critical"
model = "smp-ring3-qualification/SmpRing3Qualification"
flow = "smp-ring3-qualification"
find = '''RejectForeignWorkerEvidence ==
    /\ result = Pending
    /\ bindingState = Active
    /\ workerCount < MaxWorkers
    /\ UNCHANGED vars'''
replace = '''RejectForeignWorkerEvidence ==
    /\ result = Pending
    /\ bindingState = Active
    /\ workerCount < MaxWorkers
    /\ evidence' = [evidence EXCEPT ![Ready][MaxWorkers - 1] =
        ForeignRecord(now)]
    /\ UNCHANGED <<workerCount, completed, workerPhase, bindingState,
                    liveOwnerAuthority, liveTargetIdentity,
                    deferredOwnerAuthority, deferredTargetIdentity,
                    boundOwnerAuthority, boundTargetIdentity, bindingId,
                    activationCount, activationTick, absoluteDeadline, now,
                    uiReady, catalog, duplicateEvidenceAccepted, expiredEvidenceAccepted, result>>'''
occurrence = 1
invariant = "ExactWorkerCpuBijectionAndStampedIdentity"
min_counterexample_states = 5

[[mutation]]
id = "smp-ring3-owner-revoke-cleanup-bypassed"
kind = "transition-revocation"
severity = "critical"
model = "smp-ring3-qualification/SmpRing3Qualification"
flow = "smp-ring3-qualification"
find = '''    /\ liveOwnerAuthority' = ReusedSessiondAuthority
    /\ bindingState' = Revoked'''
replace = '''    /\ liveOwnerAuthority' = ReusedSessiondAuthority
    /\ bindingState' = Active'''
occurrence = 1
invariant = "ActiveBindingMatchesLiveOwnerAndTarget"
min_counterexample_states = 4

[[mutation]]
id = "smp-ring3-private-admission-dvm-executable-provenance-bypassed"
kind = "transition-guard-removal"
severity = "critical"
model = "smp-ring3-qualification/SmpRing3Qualification"
flow = "smp-ring3-qualification"
find = '''PrivateDeferredChildAdmissionEnabled ==
    /\ result = Pending
    /\ bindingState = NotCreated
    /\ liveOwnerAuthority = SessiondAuthority
    /\ liveTargetIdentity = QualificationIdentity
    /\ ProductionVolumeReady
    /\ ExactPrivateContractVisible
    /\ TrustedQualificationExecutableSelected'''
replace = '''PrivateDeferredChildAdmissionEnabled ==
    /\ result = Pending
    /\ bindingState = NotCreated
    /\ liveOwnerAuthority = SessiondAuthority
    /\ liveTargetIdentity = QualificationIdentity
    /\ ProductionVolumeReady
    /\ ExactPrivateContractVisible'''
occurrence = 1
invariant = "AttemptedDvmExecutableSubstitutionNeverEnablesPrivateAdmission"
min_counterexample_states = 3

[[mutation]]
id = "smp-ring3-private-admission-production-volume-gate-bypassed"
kind = "transition-guard-removal"
severity = "critical"
model = "smp-ring3-qualification/SmpRing3Qualification"
flow = "smp-ring3-qualification"
find = '''PrivateDeferredChildAdmissionEnabled ==
    /\ result = Pending
    /\ bindingState = NotCreated
    /\ liveOwnerAuthority = SessiondAuthority
    /\ liveTargetIdentity = QualificationIdentity
    /\ ProductionVolumeReady
    /\ ExactPrivateContractVisible
    /\ TrustedQualificationExecutableSelected'''
replace = '''PrivateDeferredChildAdmissionEnabled ==
    /\ result = Pending
    /\ bindingState = NotCreated
    /\ liveOwnerAuthority = SessiondAuthority
    /\ liveTargetIdentity = QualificationIdentity
    /\ ExactPrivateContractVisible
    /\ TrustedQualificationExecutableSelected'''
occurrence = 1
invariant = "PrivateDeferredChildAdmissionRequiresReadyExactProductionContract"
min_counterexample_states = 3

[[mutation]]
id = "smp-ring3-private-admission-ui-ready-gate-restored"
kind = "transition-effect"
severity = "critical"
model = "smp-ring3-qualification/SmpRing3Qualification"
flow = "smp-ring3-qualification"
find = '''PrivateDeferredChildAdmissionEnabled ==
    /\ result = Pending
    /\ bindingState = NotCreated
    /\ liveOwnerAuthority = SessiondAuthority
    /\ liveTargetIdentity = QualificationIdentity
    /\ ProductionVolumeReady
    /\ ExactPrivateContractVisible
    /\ TrustedQualificationExecutableSelected'''
replace = '''PrivateDeferredChildAdmissionEnabled ==
    /\ result = Pending
    /\ bindingState = NotCreated
    /\ liveOwnerAuthority = SessiondAuthority
    /\ liveTargetIdentity = QualificationIdentity
    /\ ProductionVolumeReady
    /\ ExactPrivateContractVisible
    /\ TrustedQualificationExecutableSelected
    /\ uiReady'''
occurrence = 1
invariant = "PrivateDeferredChildAdmissionIsUiIndependent"
min_counterexample_states = 2

[[mutation]]
id = "smp-ring3-ready-barrier-bypassed"
kind = "transition-guard-removal"
severity = "critical"
model = "smp-ring3-qualification/SmpRing3Qualification"
flow = "smp-ring3-qualification"
find = '''    /\ AllReady
    /\ worker = NextStartedWorker
    /\ workerPhase[worker] = WorkerReady'''
replace = '''    /\ TRUE
    /\ worker = NextStartedWorker
    /\ workerPhase[worker] = WorkerReady'''
occurrence = 1
invariant = "ReadyBarrierPrecedesEveryStart"
min_counterexample_states = 5

[[mutation]]
id = "smp-ring3-scheduler-measurement-loss-contaminates-qualification-accounting"
kind = "transition-effect"
severity = "critical"
model = "smp-ring3-qualification/SmpRing3Qualification"
flow = "smp-ring3-qualification"
find = '''DropSchedulerMeasurement ==
    /\ ActiveRecordAdmission(0)
    /\ ~output.schedulerMeasurementDropped
    /\ catalog' = [catalog EXCEPT !.output.schedulerMeasurementDropped = TRUE]'''
replace = '''DropSchedulerMeasurement ==
    /\ ActiveRecordAdmission(0)
    /\ ~output.schedulerMeasurementDropped
    /\ catalog' = [catalog EXCEPT
                       !.output.schedulerMeasurementDropped = TRUE,
                       !.output.qualificationMilestonesDropped = 1]'''
occurrence = 1
invariant = "SchedulerMeasurementLossDoesNotTaintQualificationAccounting"
min_counterexample_states = 6

[[mutation]]
id = "smp-ring3-success-output-clean-guard-bypassed"
kind = "transition-guard-removal"
severity = "critical"
model = "smp-ring3-qualification/SmpRing3Qualification"
flow = "smp-ring3-qualification"
find = '''    /\ QualificationEvidenceEventCount = 3 * workerCount + 1
    /\ QualificationOutputClean
    /\ bindingState' = Terminal'''
replace = '''    /\ QualificationEvidenceEventCount = 3 * workerCount + 1
    /\ TRUE
    /\ bindingState' = Terminal'''
occurrence = 1
invariant = "SuccessfulQualificationHasCleanExactOutput"
min_counterexample_states = 10

[[mutation]]
id = "smp-ring3-target-invalidation-cleanup-bypassed"
kind = "transition-revocation"
severity = "critical"
model = "smp-ring3-qualification/SmpRing3Qualification"
flow = "smp-ring3-qualification"
find = '''    /\ liveTargetIdentity' = next
    /\ bindingState' = Revoked'''
replace = '''    /\ liveTargetIdentity' = next
    /\ bindingState' = Active'''
occurrence = 1
invariant = "ActiveBindingMatchesLiveOwnerAndTarget"
min_counterexample_states = 4

[[mutation]]
id = "smp-ring3-transient-volume-unavailable-accepted-as-absent"
kind = "transition-effect"
severity = "critical"
model = "smp-ring3-qualification/SmpRing3Qualification"
flow = "smp-ring3-qualification"
find = '''ObserveTransientProductionVolumeUnavailable ==
    /\ result = Pending
    /\ bindingState = NotCreated
    /\ ~ProductionVolumeReady
    /\ catalog.privateState = PrivateQualificationPending
    /\ catalog.observation # CatalogVolumeTransientlyUnavailable
    /\ catalog' = [catalog EXCEPT
                       !.observation = CatalogVolumeTransientlyUnavailable]
    /\ UNCHANGED <<workerCount, evidence, completed, workerPhase,
                    bindingState, liveOwnerAuthority, liveTargetIdentity,
                    deferredOwnerAuthority, deferredTargetIdentity,
                    boundOwnerAuthority, boundTargetIdentity, bindingId,
                    activationCount, activationTick, absoluteDeadline, now,
                    uiReady, duplicateEvidenceAccepted, expiredEvidenceAccepted, result>>'''
replace = '''ObserveTransientProductionVolumeUnavailable ==
    /\ result = Pending
    /\ bindingState = NotCreated
    /\ ~ProductionVolumeReady
    /\ catalog.privateState = PrivateQualificationPending
    /\ catalog.observation # CatalogVolumeTransientlyUnavailable
    /\ catalog' = [catalog EXCEPT
                       !.privateState = PrivateContractAbsent,
                       !.observation = CatalogVolumeTransientlyUnavailable]
    /\ UNCHANGED <<workerCount, evidence, completed, workerPhase,
                    bindingState, liveOwnerAuthority, liveTargetIdentity,
                    deferredOwnerAuthority, deferredTargetIdentity,
                    boundOwnerAuthority, boundTargetIdentity, bindingId,
                    activationCount, activationTick, absoluteDeadline, now,
                    uiReady, duplicateEvidenceAccepted, expiredEvidenceAccepted, result>>'''
occurrence = 1
invariant = "TransientProductionVolumeUnavailabilityRemainsPending"
min_counterexample_states = 2

[[mutation]]
id = "smp-ring3-work-immutability-bypassed"
kind = "transition-effect"
severity = "critical"
model = "smp-ring3-qualification/SmpRing3Qualification"
flow = "smp-ring3-qualification"
find = '''RecordReady(worker) ==
    /\ ActiveRecordAdmission(worker)
    /\ worker = NextReadyWorker
    /\ workerPhase[worker] = WorkerInit
    /\ evidence' = [evidence EXCEPT ![Ready][worker] = CorrectRecord(worker)]'''
replace = '''RecordReady(worker) ==
    /\ ActiveRecordAdmission(worker)
    /\ worker = NextReadyWorker
    /\ workerPhase[worker] = WorkerInit
    /\ evidence' = [evidence EXCEPT ![Ready][worker] =
        RecordFor(worker, worker, QualificationPid, worker + 1, 0,
                  now, absoluteDeadline, bindingId)]'''
occurrence = 1
invariant = "PhaseMetadataIsImmutableAndBounded"
min_counterexample_states = 5

[[mutation]]
id = "synchronous-concurrency-begin-publication-slot-redirected"
kind = "transition-effect"
severity = "critical"
model = "synchronous-ipc-handoff-concurrency/SynchronousIpcHandoffConcurrency"
flow = "scheduler-dispatch"
find = '''       /\ publicationBegins' = [publicationBegins EXCEPT ![task] = 1]'''
replace = '''       /\ publicationBegins' = [publicationBegins EXCEPT ![CHOOSE other \in Tasks : other # task] = 1]'''
occurrence = 1
invariant = "QueuedRecordsOnlyComeFromBegin"
min_counterexample_states = 3

[[mutation]]
id = "synchronous-concurrency-begin-target-fifo-redirected"
kind = "transition-effect"
severity = "critical"
model = "synchronous-ipc-handoff-concurrency/SynchronousIpcHandoffConcurrency"
flow = "scheduler-dispatch"
find = '''       /\ queue' = [queue EXCEPT ![targetCpu] = nextQueue]'''
replace = '''       /\ queue' = [queue EXCEPT ![CHOOSE other \in Cpus : other # targetCpu] = nextQueue]'''
occurrence = 1
invariant = "QueuedRecordsStayOnCapturedCpu"
min_counterexample_states = 3

[[mutation]]
id = "synchronous-concurrency-nonlocal-consume-guard-bypassed"
kind = "transition-guard-removal"
severity = "critical"
model = "synchronous-ipc-handoff-concurrency/SynchronousIpcHandoffConcurrency"
flow = "scheduler-dispatch"
find = '''ConsumeNonDispatchableHead(cpu) ==
    /\ cpu \in Cpus
    /\ Len(queue[cpu]) > 0
    /\ LET record == Head(queue[cpu]) IN
       /\ ~SelectionAdmission(record, cpu)'''
replace = '''ConsumeNonDispatchableHead(cpu) ==
    /\ cpu \in Cpus
    /\ Len(queue[cpu]) > 0
    /\ LET record == Head(queue[cpu]) IN
       /\ TRUE'''
occurrence = 1
invariant = "NonLocalHeadNeverDispatches"
min_counterexample_states = 4

[[mutation]]
id = "synchronous-concurrency-post-enqueue-recheck-bypassed"
kind = "transition-guard-removal"
severity = "critical"
model = "synchronous-ipc-handoff-concurrency/SynchronousIpcHandoffConcurrency"
flow = "scheduler-dispatch"
find = '''    /\ LET accepted == PublicationAdmission(task)
       IN'''
replace = '''    /\ LET accepted == TRUE
       IN'''
occurrence = 1
invariant = "PostEnqueueObserverNeverBypassed"
min_counterexample_states = 4

[[mutation]]
id = "synchronous-concurrency-publication-generation-bypassed"
kind = "transition-guard-removal"
severity = "critical"
model = "synchronous-ipc-handoff-concurrency/SynchronousIpcHandoffConcurrency"
flow = "scheduler-dispatch"
find = '''PublicationAdmission(task) ==
    /\ tokenPhase[task] \in {TokenMinted, TokenBegun}
    /\ tokenRecord[task].slot = task
    /\ tokenRecord[task].taskId = TaskId(task)
    /\ tokenRecord[task].generation = ownerGeneration[task]'''
replace = '''PublicationAdmission(task) ==
    /\ tokenPhase[task] \in {TokenMinted, TokenBegun}
    /\ tokenRecord[task].slot = task
    /\ tokenRecord[task].taskId = TaskId(task)
    /\ TRUE'''
occurrence = 1
invariant = "PublicationObserverNeverBypassed"
min_counterexample_states = 4

[[mutation]]
id = "synchronous-concurrency-publication-state-widened"
kind = "transition-guard-removal"
severity = "critical"
model = "synchronous-ipc-handoff-concurrency/SynchronousIpcHandoffConcurrency"
flow = "scheduler-dispatch"
find = '''PublicationAdmission(task) ==
    /\ tokenPhase[task] \in {TokenMinted, TokenBegun}
    /\ tokenRecord[task].slot = task
    /\ tokenRecord[task].taskId = TaskId(task)
    /\ tokenRecord[task].generation = ownerGeneration[task]
    /\ tokenRecord[task].cpu = ownerCpu[task]
    /\ taskState[task] = TaskRunnable
    /\ ownerRunnable[task]
    /\ ownerState[task] \in {Local, RemoteQueued}'''
replace = '''PublicationAdmission(task) ==
    /\ tokenPhase[task] \in {TokenMinted, TokenBegun}
    /\ tokenRecord[task].slot = task
    /\ tokenRecord[task].taskId = TaskId(task)
    /\ tokenRecord[task].generation = ownerGeneration[task]
    /\ tokenRecord[task].cpu = ownerCpu[task]
    /\ taskState[task] = TaskRunnable
    /\ ownerRunnable[task]
    /\ ownerState[task] \in {Local, RemoteQueued, OwnerMigrating}'''
occurrence = 1
invariant = "PublicationObserverNeverBypassed"
min_counterexample_states = 3

[[mutation]]
id = "synchronous-concurrency-publication-target-cpu-bypassed"
kind = "transition-guard-removal"
severity = "critical"
model = "synchronous-ipc-handoff-concurrency/SynchronousIpcHandoffConcurrency"
flow = "scheduler-dispatch"
find = '''PublicationAdmission(task) ==
    /\ tokenPhase[task] \in {TokenMinted, TokenBegun}
    /\ tokenRecord[task].slot = task
    /\ tokenRecord[task].taskId = TaskId(task)
    /\ tokenRecord[task].generation = ownerGeneration[task]
    /\ tokenRecord[task].cpu = ownerCpu[task]'''
replace = '''PublicationAdmission(task) ==
    /\ tokenPhase[task] \in {TokenMinted, TokenBegun}
    /\ tokenRecord[task].slot = task
    /\ tokenRecord[task].taskId = TaskId(task)
    /\ tokenRecord[task].generation = ownerGeneration[task]
    /\ TRUE'''
occurrence = 1
invariant = "PublicationObserverNeverBypassed"
min_counterexample_states = 4

[[mutation]]
id = "synchronous-concurrency-retirement-purge-bypassed"
kind = "transition-effect"
severity = "critical"
model = "synchronous-ipc-handoff-concurrency/SynchronousIpcHandoffConcurrency"
flow = "scheduler-dispatch"
find = '''    /\ queue' = PurgeSlotEverywhere(task)'''
replace = '''    /\ queue' = queue'''
occurrence = 1
invariant = "RetirementPurgesEveryCpuFifo"
min_counterexample_states = 4

[[mutation]]
id = "synchronous-concurrency-scheduler-serialization-bypassed"
kind = "transition-effect"
severity = "critical"
model = "synchronous-ipc-handoff-concurrency/SynchronousIpcHandoffConcurrency"
flow = "scheduler-dispatch"
find = '''       /\ replyCommitSerial' = [replyCommitSerial EXCEPT ![task] = schedulerSerial + 1]'''
replace = '''       /\ replyCommitSerial' = [replyCommitSerial EXCEPT ![task] = schedulerSerial]'''
occurrence = 1
invariant = "SchedulerReplyWakesAreSerialized"
min_counterexample_states = 2

[[mutation]]
id = "synchronous-concurrency-selection-local-custody-bypassed"
kind = "transition-guard-removal"
severity = "critical"
model = "synchronous-ipc-handoff-concurrency/SynchronousIpcHandoffConcurrency"
flow = "scheduler-dispatch"
find = '''SelectionAdmission(record, cpu) ==
    /\ record \in RecordType
    /\ record.cpu = cpu
    /\ record.slot \in Tasks
    /\ record.taskId = TaskId(record.slot)
    /\ record.generation = ownerGeneration[record.slot]
    /\ ownerCpu[record.slot] = cpu
    /\ taskState[record.slot] = TaskRunnable
    /\ ownerRunnable[record.slot]
    /\ ownerState[record.slot] = Local'''
replace = '''SelectionAdmission(record, cpu) ==
    /\ record \in RecordType
    /\ record.cpu = cpu
    /\ record.slot \in Tasks
    /\ record.taskId = TaskId(record.slot)
    /\ record.generation = ownerGeneration[record.slot]
    /\ ownerCpu[record.slot] = cpu
    /\ taskState[record.slot] = TaskRunnable
    /\ ownerRunnable[record.slot]
    /\ TRUE'''
occurrence = 1
invariant = "DispatchObserverNeverBypassed"
min_counterexample_states = 4

[[mutation]]
id = "synchronous-concurrency-stale-drop-fallback-published"
kind = "transition-effect"
severity = "critical"
model = "synchronous-ipc-handoff-concurrency/SynchronousIpcHandoffConcurrency"
flow = "scheduler-dispatch"
find = '''    /\ tokenPhase' = [tokenPhase EXCEPT ![task] = TokenDropped]
    /\ tokenUses' = [tokenUses EXCEPT ![task] = 1]
    /\ UNCHANGED <<taskState, ownerGeneration, ownerCpu, ownerState,
                    ownerRunnable, replyDonation, tokenRecord, queue,
                    publicationBegins, postAccepted, schedulerSerial,'''
replace = '''    /\ tokenPhase' = [tokenPhase EXCEPT ![task] = TokenDropped]
    /\ tokenUses' = [tokenUses EXCEPT ![task] = 1]
    /\ publicationBegins' = [publicationBegins EXCEPT ![task] = 1]
    /\ UNCHANGED <<taskState, ownerGeneration, ownerCpu, ownerState,
                    ownerRunnable, replyDonation, tokenRecord, queue,
                    postAccepted, schedulerSerial,'''
occurrence = 1
invariant = "StaleDropHasNoFallbackPublication"
min_counterexample_states = 3

[[mutation]]
id = "synchronous-generic-handoff-target-fifo-redirected"
kind = "transition-effect"
severity = "critical"
model = "synchronous-ipc-handoff/SynchronousIpcHandoff"
flow = "scheduler-dispatch"
find = '''       /\ queue' = [queue EXCEPT ![cpu] = nextQueue]'''
replace = '''       /\ queue' = [queue EXCEPT ![CHOOSE other \in Cpus : other # cpu] = nextQueue]'''
occurrence = 1
invariant = "QueueRecordsBelongToTheirCpu"
min_counterexample_states = 2

[[mutation]]
id = "synchronous-generic-handoff-weakens-reply-custody"
kind = "transition-effect"
severity = "critical"
model = "synchronous-ipc-handoff/SynchronousIpcHandoff"
flow = "scheduler-dispatch"
find = '''           nextQueue ==
               IF NoQueuedTaskId(cpu, taskId[slot])
               THEN Append(queue[cpu], record)
               ELSE queue[cpu]'''
replace = '''           nextQueue ==
               IF NoQueuedTaskId(cpu, taskId[slot])
               THEN Append(queue[cpu], record)
               ELSE ReplaceSameTaskRecord(queue[cpu], taskId[slot], record)'''
occurrence = 1
invariant = "GenericNeverDowngradesReplyCustody"
min_counterexample_states = 4

[[mutation]]
id = "synchronous-nondispatchable-head-fairness-bypassed"
kind = "transition-guard-removal"
severity = "high"
model = "synchronous-ipc-handoff/SynchronousIpcHandoff"
flow = "scheduler-dispatch"
find = '''DropNonDispatchableQueuedHandoff(cpu) ==
    /\ cpu \in Cpus
    /\ Len(queue[cpu]) > 0
    /\ handoffBurst[cpu] < MaxHandoffBurst'''
replace = '''DropNonDispatchableQueuedHandoff(cpu) ==
    /\ cpu \in Cpus
    /\ Len(queue[cpu]) > 0
    /\ TRUE'''
occurrence = 1
invariant = "NonDispatchableHeadRespectsFairnessPrecedence"
min_counterexample_states = 5

[[mutation]]
id = "synchronous-reply-handoff-fairness-bypassed"
kind = "transition-guard-removal"
severity = "high"
model = "synchronous-ipc-handoff/SynchronousIpcHandoff"
flow = "scheduler-dispatch"
find = '''DispatchHandoff(cpu) ==
    /\ cpu \in Cpus
    /\ Len(queue[cpu]) > 0
    /\ handoffBurst[cpu] < MaxHandoffBurst'''
replace = '''DispatchHandoff(cpu) ==
    /\ cpu \in Cpus
    /\ Len(queue[cpu]) > 0
    /\ TRUE'''
occurrence = 1
invariant = "HandoffBurstIsBounded"
min_counterexample_states = 4

[[mutation]]
id = "synchronous-reply-handoff-fairness-charged-to-wrong-cpu"
kind = "transition-effect"
severity = "high"
model = "synchronous-ipc-handoff/SynchronousIpcHandoff"
flow = "scheduler-dispatch"
find = '''    /\ handoffBurst' = [handoffBurst EXCEPT ![cpu] = @ + 1]'''
replace = '''    /\ handoffBurst' = [handoffBurst EXCEPT ![CHOOSE other \in Cpus : other # cpu] = @ + 1]'''
occurrence = 1
invariant = "HandoffBurstIsBounded"
min_counterexample_states = 4

[[mutation]]
id = "synchronous-reply-record-selection-owner-check-bypassed"
kind = "transition-guard-removal"
severity = "critical"
model = "synchronous-ipc-handoff/SynchronousIpcHandoff"
flow = "scheduler-dispatch"
find = '''ReplyRecordIsLocallyDispatchable(record, cpu) ==
    /\ record \in RecordType
    /\ record.kind = Reply
    /\ record.cpu = cpu
    /\ record.taskId = taskId[record.slot]
    /\ record.ownerGeneration = ownerGeneration[record.slot]'''
replace = '''ReplyRecordIsLocallyDispatchable(record, cpu) ==
    /\ record \in RecordType
    /\ record.kind = Reply
    /\ record.cpu = cpu
    /\ record.taskId = taskId[record.slot]
    /\ TRUE'''
occurrence = 1
invariant = "StaleRecordNeverDispatches"
min_counterexample_states = 4

[[mutation]]
id = "synchronous-reply-selection-local-custody-bypassed"
kind = "transition-guard-removal"
severity = "critical"
model = "synchronous-ipc-handoff/SynchronousIpcHandoff"
flow = "scheduler-dispatch"
find = '''ReplyRecordIsLocallyDispatchable(record, cpu) ==
    /\ record \in RecordType
    /\ record.kind = Reply
    /\ record.cpu = cpu
    /\ record.taskId = taskId[record.slot]
    /\ record.ownerGeneration = ownerGeneration[record.slot]
    /\ ownerCpu[record.slot] = cpu
    /\ taskState[record.slot] = Runnable
    /\ ownerRunnable[record.slot]
    /\ ownerState[record.slot] = Local'''
replace = '''ReplyRecordIsLocallyDispatchable(record, cpu) ==
    /\ record \in RecordType
    /\ record.kind = Reply
    /\ record.cpu = cpu
    /\ record.taskId = taskId[record.slot]
    /\ record.ownerGeneration = ownerGeneration[record.slot]
    /\ ownerCpu[record.slot] = cpu
    /\ taskState[record.slot] = Runnable
    /\ ownerRunnable[record.slot]
    /\ TRUE'''
occurrence = 1
invariant = "StaleRecordNeverDispatches"
min_counterexample_states = 4

[[mutation]]
id = "synchronous-reply-token-call-refresh-disabled"
kind = "transition-guard-removal"
severity = "critical"
model = "synchronous-ipc-handoff/SynchronousIpcHandoff"
flow = "scheduler-dispatch"
find = '''ReplyTokenRefreshesExisting(slot) ==
    \E index \in 1..Len(queue[tokenRecord[slot].cpu]) :
        /\ queue[tokenRecord[slot].cpu][index].taskId = tokenRecord[slot].taskId
        /\ (queue[tokenRecord[slot].cpu][index].kind = Call \/'''
replace = '''ReplyTokenRefreshesExisting(slot) ==
    \E index \in 1..Len(queue[tokenRecord[slot].cpu]) :
        /\ queue[tokenRecord[slot].cpu][index].taskId = tokenRecord[slot].taskId
        /\ (FALSE \/'''
occurrence = 1
invariant = "ReplyRefreshMatchesMonotonicQueueSemantics"
min_counterexample_states = 4

[[mutation]]
id = "synchronous-reply-token-cross-cpu-global-dedup"
kind = "transition-guard-removal"
severity = "critical"
model = "synchronous-ipc-handoff/SynchronousIpcHandoff"
flow = "scheduler-dispatch"
find = '''           nextQueue ==
               IF NoQueuedTaskId(targetCpu, tokenRecord[slot].taskId)
               THEN Append(queue[targetCpu], tokenRecord[slot])'''
replace = '''           nextQueue ==
               IF tokenRecord[slot].taskId \notin
                   UNION {QueueTaskIds(other) : other \in Cpus}
               THEN Append(queue[targetCpu], tokenRecord[slot])'''
occurrence = 1
invariant = "ReplyRefreshMatchesMonotonicQueueSemantics"
min_counterexample_states = 5

[[mutation]]
id = "synchronous-reply-token-double-publish"
kind = "transition-effect"
severity = "critical"
model = "synchronous-ipc-handoff/SynchronousIpcHandoff"
flow = "scheduler-dispatch"
find = '''    /\ publicationPending' = [publicationPending EXCEPT ![slot] = FALSE]
    /\ tokenLive' = [tokenLive EXCEPT ![slot] = FALSE]'''
replace = '''    /\ publicationPending' = [publicationPending EXCEPT ![slot] = FALSE]
    /\ UNCHANGED tokenLive'''
occurrence = 1
invariant = "ReplyTokenPublishesAtMostOnce"
min_counterexample_states = 5

[[mutation]]
id = "synchronous-reply-token-owner-generation-bypassed"
kind = "transition-guard-removal"
severity = "critical"
model = "synchronous-ipc-handoff/SynchronousIpcHandoff"
flow = "scheduler-dispatch"
find = "    /\\ tokenRecord[slot].ownerGeneration = ownerGeneration[slot]"
replace = "    /\\ TRUE"
occurrence = 1
invariant = "NoStaleTokenFallbackOrPublication"
min_counterexample_states = 3

[[mutation]]
id = "synchronous-reply-token-owner-state-bypassed"
kind = "transition-guard-removal"
severity = "critical"
model = "synchronous-ipc-handoff/SynchronousIpcHandoff"
flow = "scheduler-dispatch"
find = '''TokenMatchesOwner(slot) ==
    /\ tokenLive[slot]
    /\ tokenRecord[slot].kind = Reply
    /\ tokenRecord[slot].ownerGeneration = ownerGeneration[slot]
    /\ tokenRecord[slot].cpu = ownerCpu[slot]
    /\ ownerRunnable[slot]
    /\ ownerState[slot] \in {Local, RemoteQueued}'''
replace = '''TokenMatchesOwner(slot) ==
    /\ tokenLive[slot]
    /\ tokenRecord[slot].kind = Reply
    /\ tokenRecord[slot].ownerGeneration = ownerGeneration[slot]
    /\ tokenRecord[slot].cpu = ownerCpu[slot]
    /\ ownerRunnable[slot]
    /\ TRUE'''
occurrence = 1
invariant = "NoStaleTokenFallbackOrPublication"
min_counterexample_states = 4

[[mutation]]
id = "synchronous-reply-token-post-enqueue-recheck-bypassed"
kind = "transition-guard-removal"
severity = "critical"
model = "synchronous-ipc-handoff/SynchronousIpcHandoff"
flow = "scheduler-dispatch"
find = '''    /\ invalidPostCheck' =
        (invalidPostCheck \/
            (TokenMatchesOwner(slot) /\ ~ObservedTokenMatchesOwner(slot)))'''
replace = '''    /\ invalidPostCheck' =
        (invalidPostCheck \/
            (TRUE /\ ~ObservedTokenMatchesOwner(slot)))'''
occurrence = 1
invariant = "PostEnqueueSuccessRequiresExactOwner"
min_counterexample_states = 6

[[mutation]]
id = "synchronous-reply-token-publication-pending-slot-redirected"
kind = "transition-effect"
severity = "critical"
model = "synchronous-ipc-handoff/SynchronousIpcHandoff"
flow = "scheduler-dispatch"
find = '''       /\ publicationPending' = [publicationPending EXCEPT ![slot] = TRUE]'''
replace = '''       /\ publicationPending' = [publicationPending EXCEPT ![CHOOSE other \in Tasks : other # slot] = TRUE]'''
occurrence = 1
invariant = "ReplyTokenPublishesAtMostOnce"
min_counterexample_states = 5

[[mutation]]
id = "synchronous-reply-token-reply-refresh-disabled"
kind = "transition-guard-removal"
severity = "critical"
model = "synchronous-ipc-handoff/SynchronousIpcHandoff"
flow = "scheduler-dispatch"
find = '''            (queue[tokenRecord[slot].cpu][index].kind = Reply /\
             tokenRecord[slot].ownerGeneration >=
                 queue[tokenRecord[slot].cpu][index].ownerGeneration))'''
replace = '''            (queue[tokenRecord[slot].cpu][index].kind = Reply /\ FALSE))'''
occurrence = 1
invariant = "ReplyRefreshMatchesMonotonicQueueSemantics"
min_counterexample_states = 7

[[mutation]]
id = "synchronous-reply-token-runnable-bypassed"
kind = "transition-guard-removal"
severity = "critical"
model = "synchronous-ipc-handoff/SynchronousIpcHandoff"
flow = "scheduler-dispatch"
find = '''TokenMatchesOwner(slot) ==
    /\ tokenLive[slot]
    /\ tokenRecord[slot].kind = Reply
    /\ tokenRecord[slot].ownerGeneration = ownerGeneration[slot]
    /\ tokenRecord[slot].cpu = ownerCpu[slot]
    /\ ownerRunnable[slot]'''
replace = '''TokenMatchesOwner(slot) ==
    /\ tokenLive[slot]
    /\ tokenRecord[slot].kind = Reply
    /\ tokenRecord[slot].ownerGeneration = ownerGeneration[slot]
    /\ tokenRecord[slot].cpu = ownerCpu[slot]
    /\ TRUE'''
occurrence = 1
invariant = "NoStaleTokenFallbackOrPublication"
min_counterexample_states = 4

[[mutation]]
id = "synchronous-reply-token-stale-fallback-published"
kind = "transition-guard-removal"
severity = "critical"
model = "synchronous-ipc-handoff/SynchronousIpcHandoff"
flow = "scheduler-dispatch"
find = '''BeginReplyTokenPublication(slot) ==
    /\ slot \in Tasks
    /\ ~publicationPending[slot]
    /\ TokenMatchesOwner(slot)'''
replace = '''BeginReplyTokenPublication(slot) ==
    /\ slot \in Tasks
    /\ ~publicationPending[slot]
    /\ tokenLive[slot]'''
occurrence = 1
invariant = "NoStaleTokenFallbackOrPublication"
min_counterexample_states = 4

[[mutation]]
id = "synchronous-reply-token-target-cpu-bypassed"
kind = "transition-guard-removal"
severity = "critical"
model = "synchronous-ipc-handoff/SynchronousIpcHandoff"
flow = "scheduler-dispatch"
find = "    /\\ tokenRecord[slot].cpu = ownerCpu[slot]"
replace = "    /\\ TRUE"
occurrence = 1
invariant = "NoStaleTokenFallbackOrPublication"
min_counterexample_states = 3

[[mutation]]
id = "synchronous-reply-token-target-fifo-redirected"
kind = "transition-effect"
severity = "critical"
model = "synchronous-ipc-handoff/SynchronousIpcHandoff"
flow = "scheduler-dispatch"
find = '''       /\ queue' = [queue EXCEPT ![targetCpu] = nextQueue]'''
replace = '''       /\ queue' = [queue EXCEPT ![CHOOSE other \in Cpus : other # targetCpu] = nextQueue]'''
occurrence = 1
invariant = "QueueRecordsBelongToTheirCpu"
min_counterexample_states = 4

[[mutation]]
id = "synchronous-reply-token-task-identity-bypassed"
kind = "transition-guard-removal"
severity = "critical"
model = "synchronous-ipc-handoff/SynchronousIpcHandoff"
flow = "scheduler-dispatch"
find = '''ReplyRecordIsLocallyDispatchable(record, cpu) ==
    /\ record \in RecordType
    /\ record.kind = Reply
    /\ record.cpu = cpu
    /\ record.taskId = taskId[record.slot]'''
replace = '''ReplyRecordIsLocallyDispatchable(record, cpu) ==
    /\ record \in RecordType
    /\ record.kind = Reply
    /\ record.cpu = cpu
    /\ TRUE'''
occurrence = 1
invariant = "StaleRecordNeverDispatches"
min_counterexample_states = 5

[[mutation]]
id = "tlb-active-root-target-filter-removed"
kind = "transition-effect"
severity = "critical"
model = "tlb-shootdown-lifecycle/TlbShootdownLifecycle"
flow = "tlb-shootdown-lifecycle"
find = '''AddressSpaceTargets(root) ==
    {cpu \in Cpus: eligible[cpu] /\ activeRoot[cpu] = root}'''
replace = '''AddressSpaceTargets(root) ==
    {cpu \in Cpus: eligible[cpu]}'''
occurrence = 1
invariant = "TargetsMatchMutationScope"
min_counterexample_states = 3

[[mutation]]
id = "tlb-reclaim-before-all-acks"
kind = "transition-guard-removal"
severity = "critical"
model = "tlb-shootdown-lifecycle/TlbShootdownLifecycle"
flow = "tlb-shootdown-lifecycle"
find = '''Finish ==
    /\ phase = Published
    /\ \A cpu \in targets: ackGeneration[cpu] = generation
'''
replace = '''Finish ==
    /\ phase = Published
    /\ TRUE
'''
occurrence = 1
invariant = "FinishRequiresEveryAcknowledgement"
min_counterexample_states = 3

[[mutation]]
id = "tlb-reclaimed-root-reactivation"
kind = "transition-guard-removal"
severity = "critical"
model = "tlb-shootdown-lifecycle/TlbShootdownLifecycle"
flow = "tlb-shootdown-lifecycle"
find = '''Activate(cpu, root) ==
    /\ rootState[root] = RootLive
    /\ references[root] > 0
'''
replace = '''Activate(cpu, root) ==
    /\ TRUE
'''
occurrence = 1
invariant = "ActiveRootsAreLiveAndReferenced"
min_counterexample_states = 3

[[mutation]]
id = "tlb-retirement-with-live-reference"
kind = "transition-guard-removal"
severity = "critical"
model = "tlb-shootdown-lifecycle/TlbShootdownLifecycle"
flow = "tlb-shootdown-lifecycle"
find = '''BeginRetirement(root) ==
    /\ phase = Idle
    /\ rootState[root] = RootLive
    /\ references[root] = 0
'''
replace = '''BeginRetirement(root) ==
    /\ phase = Idle
    /\ rootState[root] = RootLive
    /\ TRUE
'''
occurrence = 1
invariant = "RetiredRootsOwnNoReference"
min_counterexample_states = 2

[[mutation]]
id = "tlb-same-root-activation-reloads"
kind = "transition-effect"
severity = "high"
model = "tlb-shootdown-lifecycle/TlbShootdownLifecycle"
flow = "tlb-shootdown-lifecycle"
find = "    /\\ lastActivationReloaded' = (activeRoot[cpu] # root)"
replace = "    /\\ lastActivationReloaded' = TRUE"
occurrence = 1
invariant = "SameRootActivationDoesNotReload"
min_counterexample_states = 2

[[mutation]]
id = "user-stack-eager-map-incomplete"
kind = "transition-effect"
severity = "critical"
model = "user-stack-growth/UserStackGrowth"
flow = "exception-retirement"
find = "    /\\ allUsableMapped' = TRUE"
replace = "    /\\ allUsableMapped' = FALSE"
occurrence = 1
invariant = "MappedStateHasCompleteEagerStack"
min_counterexample_states = 3

[[mutation]]
id = "user-stack-guard-page-mapped"
kind = "transition-effect"
severity = "critical"
model = "user-stack-growth/UserStackGrowth"
flow = "exception-retirement"
find = "    /\\ guardMapped' = FALSE"
replace = "    /\\ guardMapped' = TRUE"
occurrence = 1
invariant = "MappedStateHasCompleteEagerStack"
min_counterexample_states = 3

[[mutation]]
id = "waitset-check-arm-recheck-removed"
kind = "transition-guard-removal"
severity = "critical"
model = "userspace-wait-set/UserspaceWaitSet"
flow = "waitset"
find = "ready \\/ generation # observedGeneration"
replace = "FALSE"
occurrence = 1
invariant = "SleepingRequiresStableRecheck"
min_counterexample_states = 3

[[mutation]]
id = "waitset-object-target-filter-removed"
kind = "transition-effect"
severity = "critical"
model = "userspace-wait-set/UserspaceWaitSet"
flow = "waitset"
find = '''    /\ UNCHANGED <<generation, epoch, providerLive, ready, waitState,
                   observedGeneration, observedEpoch, deadline, now, objectRefs,
                   epollRefs, ingressBacklog, wakeObject>>
'''
replace = '''    /\ waitState' = IF waitState = "sleeping" THEN "woken" ELSE waitState
    /\ wakeObject' = IF waitState = "sleeping" THEN 2 ELSE wakeObject
    /\ UNCHANGED <<generation, epoch, providerLive, ready,
                   observedGeneration, observedEpoch, deadline, now, objectRefs,
                   epollRefs, ingressBacklog>>
'''
occurrence = 1
invariant = "NoWrongObjectWake"
min_counterexample_states = 3

[[mutation]]
id = "waitset-registry-object-cap-removed"
kind = "transition-guard-removal"
severity = "critical"
model = "userspace-wait-set/WaitSetRegistry"
flow = "waitset"
find = "    /\\ Cardinality(epollLive) < MaxEpollObjects"
replace = "    /\\ TRUE"
occurrence = 1
invariant = "LiveEpollObjectsRemainBounded"
min_counterexample_states = 4

[[mutation]]
id = "waitset-registry-per-epoll-cap-removed"
kind = "transition-guard-removal"
severity = "critical"
model = "userspace-wait-set/WaitSetRegistry"
flow = "waitset"
find = "    /\\ Cardinality(interests[epoll]) < MaxInterestsPerEpoll"
replace = "    /\\ TRUE"
occurrence = 1
invariant = "PerEpollInterestCountsStayBounded"
min_counterexample_states = 3

[[mutation]]
id = "waitset-registry-rejected-restore-prefix-commit"
kind = "transition-effect"
severity = "critical"
model = "userspace-wait-set/WaitSetRegistry"
flow = "waitset"
find = '''             ELSE /\ UNCHANGED <<epollLive, interests>>'''
replace = '''             ELSE /\ \E prefix \in candidate.live:
                    /\ epollLive' = epollLive \cup {prefix}
                    /\ interests' = [interests EXCEPT
                        ![prefix] = candidate.interests[prefix]]'''
occurrence = 1
invariant = "RejectedRestorePreservesExactPriorState"
min_counterexample_states = 2

[[mutation]]
id = "wayland-accept-readiness-guard-removed"
kind = "transition-guard-removal"
severity = "high"
model = "wayland-accept-isolation/WaylandAcceptIsolation"
flow = "wayland-client-ingress"
find = '''BeginAcceptCall ==
    /\ acceptCall = Idle
    /\ listenerReady
'''
replace = '''BeginAcceptCall ==
    /\ acceptCall = Idle
    /\ TRUE
'''
occurrence = 1
invariant = "AcceptRequiresPublishedReadiness"
min_counterexample_states = 2

# --- Cost invariants ------------------------------------------------------
#
# Everything else in this corpus mutates a guard whose absence produces a wrong
# answer. These three mutate a guard whose absence produces the *right* answer
# more slowly, which is the defect class this kernel kept shipping: an
# eight-bind receive, a per-dispatch scan for a value read only at spawn, a
# CPUID triple exit per IPI. All three were correct and none tripped an
# assertion. A cost invariant that no mutation kills is decoration, so each of
# the three has one here.
