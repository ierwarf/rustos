# TLA+ source-conformance audit

This is a source-level audit of the contracts modeled in this directory. It
checks that the Rust transition, identity, and cleanup boundary exists at the
named source anchor. It is not a claim that finite TLC exploration proves Rust
equivalence, CPU memory ordering, ELF/PE parser safety, or full scheduler CPU-time fairness.

## Audited model contracts

| Model | Rust transition/linearization anchors checked | Result |
| --- | --- | --- |
| dual-abi-image-admission | `libs/rustos-image-admission/src/lib.rs` `admit_image`; `services/loaderd/src/main.rs` ELF `validate_elf_fd` and PE `load_pe_image_fd` | Fixed during this audit: both formats now cross one source-tested admission gate before broker mapping. Overflow, out-of-window and overlapping regions, W+X mappings, missing main entry points, and entries outside executable memory fail closed. PE DLL entry zero remains the only explicit entryless case. Byte-level source conformance is checked separately by the byte-parser model, Kani, fuzzing, and focused tests. |
| dual-abi-byte-parser | `libs/rustos-image-admission/src/lib.rs` `admit_elf64_image`, `admit_pe64_image_headers`, `apply_pe64_base_relocations`, `validate_pe64_import_table`; `services/loaderd/src/main.rs`; `kernel/compat/src/user/syscall/linux/proc_broker_ops.rs` exact file copy | Matched at source level: exact-size header tables, W^X sections, bounded relocation/import tables, missing terminators, truncated file mappings, and post-parse snapshot stability fail closed before mapping. Pinned Kani 0.67.0 proves exact integer decoding, arbitrary single ELF segment and PE section admission, common entry/W^X rules, relocation-table absence, an arbitrary single relocation entry, and an arbitrary single import thunk. Arbitrary-length multi-block/multi-descriptor corpora and native launch captures remain release evidence. |
| page-table-lifecycle | `kernel/mm/src/memory/address_space.rs` range and flag normalization | Matched at source level: page-aligned user windows and arithmetic are checked, writable user pages require NX, huge-page flags are rejected, and teardown owns frame release. Target TLB shootdown and multicore hardware evidence remains open. |
| dma-iommu-isolation | `tools/hostd/src/{main.rs,runtime.rs}`; `libs/driver-domain-host/src/lib.rs`; `kernel/io-manager/src/driver/iommu.rs` | Partially matched, still release-blocking: the physical display-DVM supervisor now requires `/dev/iommu`, creates one QEMU IOMMUFD object, attaches the exact complete VFIO group to it, uses allocator-returned DMA handles in AHCI/NVMe, and fails closed if reset/restore cannot complete. No target IOMMU fault/revoke/read-only-DMA capture exists yet, and the separate RustOS native boot-device backend remains identity-only. |
| filesystem-content-integrity | `tools/xtask/src/stage/mod.rs`; `kernel/io-manager/src/storage/boot_volume.rs` | Matched for immutable boot content: the GRUB-authenticated extent manifest binds exact per-64-KiB SHA-256 digests, duplicate/overlapping/inexact extents fail parsing, and full/range reads verify before returning bytes. Corrupted-media boot and recovery captures remain open. |
| network-payload-session | `libs/driver-domain-protocol/src/lib.rs`; `kernel/io-manager/src/io/dvm_network.rs`; `tools/xtask/src/kvm.rs` | Matched at the fixed DVM Ethernet boundary: only bounded ARP or checksum-valid, nonfragmented IPv4 passes; malformed frames are consumed and dropped so they cannot poison the ring head. The current composite KVM run reaches DVM network readiness but cannot start netprobe because the required direct-scanout UI provider fails closed; a fresh bidirectional capture remains required. Saturation, cancellation, backpressure, and physical-NIC behavior remain outside this transport proof. |
| scheduler-cpu-distribution | `kernel/ps/src/multitask/scheduler.rs` bounded System burst, per-User ready age, bounded latency FIFO, activation handoff, and vruntime accounting; `services/runtimed/src/spawn.rs` | Matched for the checked one-System/two-User abstraction: two System dispatches reserve the next User turn; ready-age is per User; the deduplicated FIFO is bounded, removes blocked/stale owners, and permits at most eight consecutive latency picks. Strong fairness is explicit for each User dispatch because a continuously replenished event queue makes weak process fairness insufficient. TLC explored 635,490 generated and 126,100 distinct states to depth 30 with no error. Every catalog process is created suspended, recorded by runtimed, then activated with a one-shot spawn hint. Coalescing input/Wayland presentation and moving callbacks onto a previous-presentation permit raised steady WayClick throughput to roughly 33--44 FPS, but the current 30-second proof still fails 55 FPS; the remaining synchronous AF_UNIX data path, multicore distribution, and hostile interrupt/DVM saturation remain release-blocking. |
| boot-volume-admission | `kernel/io-manager/src/storage/block.rs`; `storage/block/boot.rs`; `storage/boot_volume.rs`; `libs/storage-core/src/lib.rs` `BootVolumeLocator` | Fixed during this audit: a supplied identity permits only an exact partition/FAT serial match. Identity-free Multiboot2 boot requires the immutable extent manifest and exactly one FAT candidate; ambiguous discovery and an identity mismatch both fail closed. |
| runtime-control-rpc | `libs/runtime-control/src/lib.rs` `response_payload_len`; Kani `verification::*` harnesses; Verus `formal/verus-proof-kernel/runtime_response.rs` | Fixed during this audit: successful replies now require the exact request opcode and payload shape; only a bounded snapshot may carry payload. Positive or `i32::MIN` status values fail closed, so a malformed peer cannot turn integer negation into a panic or a false success. |
| rootd-bootstrap | `services/rootd/src/main.rs` lease/restart path; `services/loaderd/src/main.rs` spawn/activate path | Matched: bootstrap admission, endpoint wait, and lease-led restart remain service-owned. |
| endpoint-registry | `kernel/compat/src/user/syscall/linux/ipc_ops.rs` registration/revoke/cleanup; `kernel/ps/src/multitask/process_table.rs` exit marker | Matched: the shared mutation lock plus exit-marker recheck makes publication fail closed. |
| endpoint-publication | `ipc_ops.rs` `SERVICE_ENDPOINT_REGISTRY_MUTATION`; process-table exit state | Matched: endpoint store remains the observable commit point and exiting owners lose lookup/capability authority. |
| deferred-start | `services/initd/src/main.rs`; `services/runtimed/src/spawn.rs`; rootd readiness report and loader activation | Matched: service and catalog child creation carries `LOADER_SPAWN_FLAG_DEFER_START`; runtimed records ordinary app ownership before activation, while endpoint services complete lease admission first. Activation is the single-use scheduler handoff commit point. |
| post-init-leases | `services/rootd/src/main.rs` sender-stamped readiness/capability/lookup dispatch | Matched: report authority is bound to supervisor sender and exact PID lease; a live lease rejects a different `reporter_pid` without changing PID, reporter, or capability authority. |
| rootd-restart-backoff | `services/rootd/src/main.rs` `restart_failed_leases`; `kernel/compat/src/user/syscall/linux/lifecycle_broker_ops.rs` rootd wait broker | Fixed during this audit: observed core-service exit now enters `RESTART_PENDING` before retry, failed spawn/activation reuses that bounded delay, and only rootd may invoke the 1s-capped timer substrate. |
| post-init-supervisor-recovery | `services/rootd/src/main.rs` post-init lease query/reclaim and lifecycle drain; `services/initd/src/main.rs` reconciliation | Fixed during this audit: initd adopts a surviving service only after the exact-PID endpoint is ready; an endpoint-pending admitted lease has one 30-second bounded window before authenticated rootd reclaim performs full teardown. A sessiond exit immediately revokes and terminates its reported uiserver child. |
| dvm-control-relay | `libs/driver-domain-host/src/lib.rs` challenge/proof/WELCOME and `new_input_epoch`; `driver-domains/linux/package/rustos-dvm-agent/src/rustos-dvm-agent.c` fw_cfg HMAC proof; `kernel/io-manager/src/input/dvm_frames.rs` RDI3 ingress | Matched: CID and static HELLO do not authorize the relay. L0 requires a fresh per-launch HMAC-SHA256 proof over the exact HELLO before WELCOME, probe, or input authority; the agent reads the secret only from fw_cfg's root-only raw attribute. Relay epochs are monotonic for a hostd lifetime and fail closed before reuse on wrap. |
| dvm-control-endpoint | `ControlSecret::control_port`, hostd/xtask listener binding, DVM `control_port_from_secret` | Fixed during this audit: the control port is no longer static. L0 and the root-only agent derive the same private endpoint from the per-launch secret, so an ordinary same-CID process cannot reserve the listener setup slot; successful connection still requires the fresh HMAC proof. |
| dvm-agent-readiness | `rustos-dvm-agent.c` `open_ready_directory`/`publish_ready`/`local_health`/`serve` | Fixed during this audit: a static readable file is no longer local health. The initialized serving process holds a singleton lock and a lock on a complete candidate inode, atomically renames that inode to `ready`, and retains both descriptors for its lifetime. Health validates the owner-private directory, exact regular-file metadata and payload, then succeeds only while the ready inode remains locked. Exit, SIGKILL, and the RT hard limit release the lock, so stale contents fail closed; the diagnostic `announce` command cannot publish readiness. One fixed candidate name bounds pre-rename crash residue. TLC checked 1,461 distinct states including stale/malformed/symlink state, partial publication, restart, crash, and announcement-only paths without error. |
| dvm-network-ring | `kernel/io-manager/src/io/dvm_network.rs`; `driver-domains/linux/package/rustos-dvm-net/src/rustos-dvm-net.c`; `libs/driver-domain-protocol/src/lib.rs` | Matched: the host-created header fixes region/slot/MTU bounds at install. RustOS neither dereferences guest descriptors nor trusts an unbounded counter; invalid shared state returns `Invalid` without advancing its producer/consumer cursor. |
| dvm-network-control | `libs/driver-domain-host/src/lib.rs` RDI1 session lifecycle; `kernel/io-manager/src/input/dvm_frames.rs`; `kernel/io-manager/src/io/dvm_network.rs` | Matched: mapping a valid ivshmem aperture does not report a live network by itself. Only the active L0-authenticated one-shot control epoch permits transmit/receive; its exact end returns NoDevice, while stale cleanup and DVM-writable data-plane state cannot revoke or create a newer lease. |
| dvm-input-revocation | `libs/driver-domain-host/src/lib.rs` epoch allocation; `libs/driver-domain-protocol/src/lib.rs` shared `RustosInputFrame` plus Kani harnesses; `kernel/io-manager/src/input/dvm_frames.rs`, `input/event_queue.rs`; `services/inputd/src/main.rs`; `drivers/libs/keyboard-core/src/lib.rs` | Matched: every session start/end injects a priority reset barrier, inputd accepts that exact flag combination, and keyboard reset emits provider-owned releases before state is discarded. L0 never reuses a relay epoch within a hostd lifetime, so a retired sequence cannot restart after a later barrier. |
| dvm-input-ring | `libs/driver-domain-protocol/src/lib.rs` `DvmInputRingHeader`; `libs/driver-domain-host/src/{lib.rs,ivshmem.rs}` `InputRingSink`/`IvshmemInputProducer`; `tools/xtask/src/kvm.rs`; `kernel/io-manager/src/input/{dvm_ring.rs,dvm_frames.rs}`; `kernel/compat/src/user/syscall/linux/input_broker_ops.rs` | The 128 KiB backing holds exactly 2,048 32-byte slots with cache-line-separated cursors. L0 writes a complete checked RDI3 record, release-fences it, advances only producer, then signals one MSI-X eventfd. RustOS publishes transport readiness after exact mapping/vector validation and policy-consumer readiness only after inputd's capability-gated ingestion worker has successfully armed the sole kernel waiter; application poll cannot publish it. L0 requires both before production. Revoke clears both claims, resets decoder/network/input authority, unmaps the aperture, and recovery may reuse only the same pinned PCI aperture and permanent vector. The leaf only wakes; malformed/stale slots are consumed as rejected so no hostile record can pin the bounded aperture. |
| trusted-ui-boundary | `kernel/io-manager/src/io/dvm_display.rs`, `io/gui.rs`; `libs/rustos-user-abi/src/{device,syscall}.rs`; `services/uiserver/src/sys.rs` | Fixed during this audit: DVM scanout provenance now crosses the driver registration and display-info ABI, while uiserver exposes a fail-closed trusted-UI status. No existing scanout or input provider can clear the required independent-attestation blockers. |
| input-readiness | `kernel/io-manager/src/input/{event_queue.rs,dvm_ring.rs}`; `kernel/compat/src/user/syscall/linux/{ipc_ops.rs,service_ops/poll_epoll.rs,service_ops/ipc_helpers.rs}`; `services/inputd/src/main.rs` `serve`/`dispatch`/`dispatch_read`; `services/uiserver/src/input_loop.rs` | Updated during this audit: a generic caller cannot drain the DVM ring; the MSI-X-woken inputd worker alone owns event-driven transport progress and may move records into its policy queue. Finite poll rechecks use bounded `STATS`. Until a service-owned readiness object exists, uiserver does not infer service readiness from the ring0 wake: its dedicated thread issues authorized nonblocking reads on a non-accumulating 4 ms cadence. TLC now includes worker transfer, poll transfer, and direct-read transfer while preserving exact ownership and DVM provenance. Generic indefinite input poll/epoll remains a failed next-ABI gate, not claimed as complete. |
| input-ingestion-worker | `formal/input-ingestion-worker/InputIngestionWorker.tla`; `kernel/compat/src/user/syscall/linux/input_broker_ops.rs`; `services/inputd/src/main.rs` `inputd-dvm-ingress` | Fixed during this audit: client poll has no transport-readiness authority. Inputd owns one capability-gated, MSI-X-woken ingestion worker; only its successfully armed waiter publishes policy readiness, it cursor-rechecks before sleep, drains at most 256 records per broker turn, yields before recovery backlog work, and is the sole consumer authority. TLC proves bounded cursor ownership and eventual draining independent of arbitrary client polls. |
| ui-frame-budget | `services/uiserver/src/input_loop.rs` `process_pending_input`; `services/uiserver/src/app/input.rs` `handle_input_event`; `services/uiserver/src/app/runtime.rs` `ConsoleCommandDispatcher`; `services/uiserver/src/main.rs` profile loop | Fixed during this audit: uiserver no longer waits for console routing or focus ioctls on its input/present thread. Keyboard and focus commands receive one bounded `try_send` admission decision to a FIFO worker; full/disconnected admission is counted as a visible rejection, while the worker alone may wait on `devmgrd`/`runtimed`. The UI continues to create and present its local redraw update, and the heartbeat exposes pending, rejected, slow, and failed delivery counts. |
| scheduler-thread-demotion | `kernel/ps/src/multitask/scheduler.rs` `demote_current_user_task_to_user_class`; `kernel/compat/src/user/syscall/linux/scheduler_ops.rs`; `services/uiserver/src/{sys.rs,main.rs,input_loop.rs,runtime_sync.rs}` | Fixed during this audit: POSIX clone inheritance no longer gives uiserver's catalog, refresh, observability, desktop-generation, or untrusted Wayland-accept helpers permanent System-class competition with input/present. `SYS_RUSTOS_SCHED_DEMOTE_SELF` is self-demotion only and preserves a live reply-scoped IPC donation until its normal terminal release; KVM acceptance requires observed helper demotion. |
| wayland-accept-isolation | `services/uiserver/src/wayland.rs` `start_wayland_acceptor`, `WaylandCompositor::tick`; `services/netd/src/main.rs` `handle_accept` | A netd-backed nonblocking `accept4` may still have cross-service RPC latency. Only the bounded accept worker owns that call; the UI loop drains at most eight completed streams per tick through `try_recv`. The intermediate queue is capped at 16, overload rejects and logs the new client, and accept stalls cannot own or suppress UI frame progress. |
| wayland-frame-pacing | `services/uiserver/src/main.rs` coalesced `main-present` loop; `services/uiserver/src/wayland.rs` `consume_frame_callback_permit` | Fixed during this audit: input, runtime, and Wayland damage accumulate into one main presentation rather than a cursor-only early present. The boot frame and each successful real presentation grant one non-accumulating callback permit; a damage-free callback-only commit may use the separate monotonic 16 ms cadence, and either route consumes its permit. TLC proves coalescing, permit ownership, and fair eventual progress. It does not claim the 55 FPS KVM gate, which remains failed while ordinary AF_UNIX Wayland traffic uses synchronous netd request/reply transport. |
| ui-input-motion | `driver-domains/linux/package/rustos-dvm-agent/src/rustos-dvm-agent.c` `input_selftest_emit_cycle`; `services/uiserver/src/{input_loop.rs,main.rs}`; `tools/xtask/src/kvm.rs` | Matched: the KVM-only DVM selftest traces a 192-pixel axis-aligned square instead of the retired center-tremble pattern. Its source is polled at most every 5 ms, safely below the L0 256-frame/s admission ceiling; the end-to-end 60 Hz contract requires three consecutive one-second windows with at least 55 accepted events, 50 presented cursor moves, no drop/slow/error/backlog, at most 50 ms input gap/age, equal logical/presented coordinates, and at least 96 pixels of travel on both axes. The model proves both axes receive visible work and every accepted visible-motion epoch is eventually presented. |
| dvm-input-selftest | `driver-domains/linux/package/rustos-dvm-agent/src/rustos-dvm-agent.c` `input_scheduler_enter`/`input_scheduler_leave`/`input_selftest_start`/`input_selftest_emit_cycle`/`input_selftest_timeout_ms`/`stream_input_devices`; `tools/xtask/src/{build/mod.rs,kvm.rs}` source contract and `--exercise-input` | Matched: the KVM-only composite device advertises `BTN_LEFT` with bounded `ABS_X/ABS_Y`, so the ordinary pointer selector opens its own evdev description and exercises the production absolute path. After authenticated stream admission, the agent requires an initial normal policy, installs and reads back the exact 50/100 ms `RLIMIT_RTTIME`, then enters and reads back SCHED_RR priority 10. Exit restores and reads back both the saved policy/priority and RT limit; any admission rollback or restore mismatch terminates the agent instead of reconnecting. It sends one F12 proof before 4,000 pointer-only position cycles; every pointer cycle consumes one cumulative monotonic 10 ms deadline and missed slots are skipped. TLC checks 45 distinct states spanning partial admission, successful rollback, restore, fatal restore, and hard-limit termination with no emission outside verified bounded scheduling. |
| dvm-absolute-pointer | `rustos-dvm-agent.c` report staging; `driver-domain-protocol`/`driver-domain-host` RDI3 position frame; `kernel/io-manager/src/input/{dvm_frames,event_queue}.rs`; `services/inputd/src/main.rs`; `services/uiserver/src/app/input.rs` | Fixed during this audit: absolute tablet X/Y remain staging state until one `SYN_REPORT`; the agent publishes only complete changed positions. L0 and ring0 enforce the fixed 1600x900 bound and distinct position kind, inputd rejects out-of-surface coordinates and duplicate coordinates are idempotent. TLC checks 74,772 distinct states with no partial publication, duplicate publication, mixed ownership, out-of-bounds cursor, or phantom UI motion. |
| devmgrd-sessiond-isolation | `services/devmgrd/src/main.rs` `serve`, `start_sessiond_ioctl_workers`, `reply_device_ioctl` | Fixed during this audit: sessiond-executed console ioctls are no longer called inline by devmgrd's sole receiver. A four-worker, 32-item bounded FIFO accepts them; saturation replies `EAGAIN` rather than holding unrelated device messages behind a recovering sessiond call. |
| vfio-release-authorization | `tools/hostd/src/main.rs` `verify_release_authorization`; `libs/driver-domain-host/src/lib.rs` `ReleaseAuthorization`, `VfioReleaseBinding`, and VFIO lease transitions | Fixed during this audit: irreversible device binding now requires a pinned-key detached signature, exact artifact and policy digest binding, durable evidence, and a fresh validity-window check before prepare, bind, and active-state persistence. Durable serialization accepts only schema 3 with fleet-policy evidence; schemas 1/2 are rejected, and an unsigned preflight snapshot cannot serialize as a lease. |
| dvm-commercial-lifecycle | `tools/hostd/src/{main.rs,runtime.rs}`; `libs/driver-domain-host/src/lib.rs` group reset/restore and live display safety | Matched at source level: release inputs, the signature verifier, QEMU, artifacts, policies, and their canonical directory chains must have trusted ownership and reject group/world mutation; SHA-256 is computed in-process. Before any prepared lease or VFIO bind, activation validates the exact display-only policy, production QEMU digest, complete schema-8 bundle, writable `/dev/iommu`, an empty IOMMUFD IOAS allocate/destroy round trip, and live sysfs evidence that the assigned group is neither `boot_vga` nor driving a connected DRM connector; an incomplete operator protected set cannot detach the L0 display. The signed schema-3 device policy additionally binds `amdgpu` `1002:1900` and rejects an excluded storage/network function in the group. After durable prepare, hostd repeats both display-safety and exact-identity checks immediately before mutation. QEMU is held behind a pre-exec gate until an owner-private fsync'd runtime record binds PID/start time and release inputs. Launch requires pre-reset plus IOMMUFD; readiness requires authenticated consecutive physical-display evidence. Supervised teardown rejects signaled/nonzero exit, uses exact pidfd recovery, resets the complete group, and restores original drivers. Reset failure retains VFIO quarantine. Physical-hardware map/fault/reset/revoke captures remain required evidence. |
| dvm-release-bundle | `driver-domains/linux/scripts/{write-manifest,verify-release-artifacts,stage-release}.sh`; `tools/xtask/src/kvm.rs` `verify_dvm_artifacts`; `libs/driver-domain-host/src/lib.rs` `ControlContract::parse`; `tools/hostd/src/runtime.rs` `verify_artifacts` | Matched: schema 8 admits exactly 25 unique manifest keys and a six-key control contract, then packages them with kernel, rootfs, Buildroot config, kernel config, module-signing certificate, and source lock. All three verifiers hash the co-located companions; hostd derives every path from the admitted manifest directory, independently compares every control field, and launches with that verified parsed snapshot instead of reopening it. Staging refuses an existing destination, symlink traversal, and group/world-writable ancestors, verifies before and after copying, fsyncs the 0644 files and 0755 temporary directory, and publishes it by atomic no-clobber rename. TLC exhaustively checks 6,828 states spanning all component subsets and corrupt-copy/replacement/mutation outcomes. |
| dvm-display-driver-supply | `driver-domains/linux/sources.lock`; `package/rustos-dvm-nvidia-open`; `configs/rustos_linux_dvm_x86_64_defconfig`; `board/linux.fragment`; `scripts/verify-module-signatures.sh`; `board/overlay/etc/init.d/S48rustos-dvm-net`; `rustos-dvm-agent.c` display driver admission | Matched at source/build level for the current Blackwell target: one SHA-256-pinned official 580.173.02 release supplies the open `nvidia`, `nvidia-modeset`, and `nvidia-drm` modules and exact-version GSP images. UVM/CUDA and the proprietary kernel directory are not built. The kernel requires SHA-256 module signatures and signs every installed in-tree and external module. Schema-8 artifacts bind both the resulting certificate and the exact kernel configuration that proves enforcement; build verification extracts every installed module's detached PKCS#7 payload and verifies it cryptographically against that certificate. Boot selects only a kernel-produced display-class PCI modalias, completes NVIDIA modeset/DRM loading before relay start, and the control agent admits `nvidia` only as a DRM-bound display owner. The firmware package is explicitly non-redistributable; commercial redistribution authorization plus physical connector, IOMMU fault/reset/revoke, and sustained 60 FPS page-flip captures remain failed release evidence. |
| dvm-amdgpu-supply | `sources.lock`; `configs/rustos_linux_dvm_x86_64_defconfig`; `board/linux.fragment`; `scripts/verify-module-signatures.sh`; `S48rustos-dvm-net`; `rustos-dvm-agent.c` | Matched at source/build level for AMD PCI `1002:1900`: the DVM contains the signed upstream `amdgpu.ko`, selects it only from the kernel PCI modalias, and fails image verification unless all twelve target GC 11.0.1, PSP 13.0.4, SDMA 6.0.1, and VCN 4.0.2 firmware payloads are regular files matching their source-locked SHA-256 digests. The existing build tree passed cryptographic verification for 39 signed modules and the exact firmware gate; an injected wrong VCN digest failed closed. TLC exhaustively checked 32,770 states and proved that incomplete, rejected, or revoked AMD supply never reaches KMS/relay readiness. Physical assignment is still blocked while this AMD function remains the L0 boot display. |
| dvm-amdgpu-evidence | `libs/driver-domain-host/src/lib.rs`; `tools/hostd/src/{main.rs,runtime.rs}`; `rustos-dvm-agent.c`; `rustos-dvm-display.c` | Matched at source level: schema 3 fixes exact AMDGPU identity and signed performance bounds. The relay atomically publishes one sequence-numbered one-second sample from completed DRM page-flip events, including direct-scanout/zero-copy provenance, frame rate, actual commit-to-event latency, and nonblocking commit time. The authenticated agent rejects malformed/private-state violations; hostd requires five distinct consecutive passing samples and then rejects stale, restarted, wrong-identity, CPU-copy, low-throughput, or high-latency evidence. TLC checks the finite two-sample abstraction. This source/model conformance is not a physical capture; the active boot GPU keeps that release gate failed. |
| driver-domain-fleet | `tools/hostd/src/main.rs` `verify_release_authorization`; `libs/driver-domain-host/src/lib.rs` `DriverDomainFleetPolicy` | Fixed during this audit: a signed release now binds a strict fleet artifact. The parser rejects CID, IOMMU-group, and PCI-function aliases across members, and activation requires the exact validated member. |
| gui-dvm-surface | `formal/gui-dvm-surface/GuiDvmSurface.tla`; `formal/dvm-atomic-scanout/DvmAtomicScanout.tla`; `tools/xtask/src/kvm.rs`; `kernel/io-manager/src/io/dvm_display.rs`; `kernel/io-manager/src/io/gui/{backend.rs,mod.rs}`; `kernel/compat/src/user/{sysops/device.rs,syscall/linux/device_broker_ops.rs}`; `services/uiserver/src/main.rs`; `driver-domains/linux/package/rustos-dvm-display/src/{rustos_dvm_ivshmem_uio.c,rustos-dvm-display.c}` | Partially matched and explicitly failed for physical scanout. V3 still gives exactly three page-aligned host-owned slots, read-only DVM mapping, bounded generations, module-mediated release, and a read-only DMA-BUF exporter. The former userspace direct-import relay was unreachable: `open_shared_display` requires a nonzero valid V3 atlas, so `serve_display` always selected the GPU-command path and could never enter the old DMA-BUF branch. That dead branch and its pre-command CPU-frame renderer were removed. `xtask check` rejects their symbols if they return. The model remains the acceptance specification for a future explicit physical-AMD mode; there is currently no runnable direct-DMA-BUF consumer, so physical import/scanout and 60 FPS evidence remain failed rather than source-matched. |
| dvm-gpu-compositor | `formal/dvm-gpu-compositor/DvmGpuCompositor.tla`; `libs/driver-domain-protocol/src/lib.rs`; `services/uiserver/src/{gpu_scene.rs,gpu_runtime.rs}`; `kernel/io-manager/src/io/dvm_display.rs`; `driver-domains/linux/package/rustos-dvm-display/src`; `tools/xtask/src/{build/mod.rs,kvm.rs}` | Source/model and rebuilt packaged-QEMU evidence match for the pre-public-ABI slice. Applications retain the Wayland buffer contract; only uiserver can emit the bounded clear/solid-quad/textured-quad vocabulary. RustOS owns one three-entry monotonic context timeline and an asynchronous three-slot queue. A restarted DVM receives a new host-selected context epoch, old live slots are revoked, and stale uiserver work is rejected. The DVM measures fixed EGL/GLES program creation plus the initial fenced atomic KMS frame; its kernel module validates and publishes that exact prime record before readiness, and uiserver no longer fabricates a local duration. Runtime render and KMS completion use distinct explicit fences. The 16.667 ms frame target remains a measured acceptance limit, but the previous front and current epoch remain live until the distinct 50 ms hard timeout or an explicit context/device error. Rust and both C consumers are cross-checked by `xtask check` for version, magic, and prime offsets. TLC exhaustively checked 2,847,356 distinct states, including the target-miss window, measured current-epoch prime, queue bounds, hard timeout, revoke, and reset, without invariant or temporal-property error. The final 30-second QEMU capture admitted GPU composition and explicit fences, completed the private 120-frame proof at 128.705 FPS with 7.769/11.075 ms average/maximum GPU time, and retained the provider without context loss or compositor-offline markers. The relay now has no pre-command CPU-frame renderer or impossible zero-atlas direct-import branch; display readiness can follow only a validated command batch with GPU and present completion, and `xtask check` rejects those retired symbols if they return. This does not claim a public application 3D ABI or physical AMD zero copy. |
| dvm-gpu-proof-scheduler | `formal/dvm-gpu-proof-scheduler/DvmGpuProofScheduler.tla`; `driver-domains/linux/package/rustos-dvm-display/src/rustos-dvm-gpu-probe.c`; `tools/xtask/src/{build/mod.rs,kvm.rs}` | The pipeline prime remains under normal scheduling. The proof requires an initial normal policy, installs and reads back the exact 50/100 ms `RLIMIT_RTTIME`, then admits only the finite 120-frame measurement to SCHED_RR priority 8 below display/input. Success and ordinary failure restore and read back both the saved policy/priority and RT limit before evidence. Hard-limit or uncertain-restore termination leaves no evidence, and the long-lived health proof remains normal. xtask rejects loss of those source guards. TLC checked 18 distinct states and both restore-settlement/publication properties without error. Earlier QEMU evidence recorded the scheduler, limit, hard-action, and restored-normal fields; a new runtime capture is not claimed by this source-only hardening. |
| dvm-display-scheduler | `formal/dvm-display-scheduler/DvmDisplayScheduler.tla`; `driver-domains/linux/package/rustos-dvm-display/src/rustos-dvm-display.c`; `tools/xtask/src/{build/mod.rs,kvm.rs}` | The display relay remains normal-priority through setup and, only after host confirmation, requires an initial normal policy, installs/reads back the exact 50/100 ms RT bound, then admits SCHED_RR priority 9 below input. Partial admission cannot run the relay. Ordinary stop/failure may retry only after exact saved policy/priority and RT-limit readback; rollback/restore uncertainty is a fatal process exit, while the Linux hard limit also removes all process authority. xtask guards those source conditions. TLC checked 13 distinct states and stop settlement through either verified restoration or terminal failure. Earlier QEMU evidence proved normal successful admission; no runtime fault injection is claimed for the new fatal branches. |
| dvm-display-readiness | `formal/dvm-display-readiness/DvmDisplayReadiness.tla`; `rustos-dvm-display.c` process owner/publication/teardown; `rustos-dvm-agent.c` `display_relay_is_ready`; `tools/xtask/src/{build/mod.rs,kvm.rs}` | Fixed during this audit: the display process first owns a singleton lock. After a confirmed peer and completed GPU frame, it writes, fsyncs, and locks one fixed candidate inode before atomic rename to the ready path. Agent health already requires the exact payload plus a held inode lock. Ordinary relay failure now closes that lock before scheduler restoration; SIGKILL and the RT hard limit close all process locks. A duplicate process, partial/stale candidate, unlocked old path, and post-exit state therefore fail closed, while the fixed candidate bounds crash residue. TLC checked 306 distinct states spanning duplicate start, partial publication, atomic promotion, ordinary fault/restore, fatal restore, retry, and hard-limit/crash paths without error. |
| dvm-gpu-admission | `formal/dvm-gpu-admission/DvmGpuAdmission.tla`; `services/uiserver/src/{gpu_runtime.rs,gpu_scene.rs,render.rs,sys.rs}`; `libs/rustos-user-abi/src/device.rs`; `kernel/{io-manager,ps}` display-surface paths; `driver-domains/linux/package/rustos-dvm-display/src` | Source/model matched for bounded promotion and pacing. DVM scanout without a current GPU prime remains waiting and cannot be counted as software success. Atlas creation/mapping runs on a uniquely owned worker mapping while the UI thread keeps the existing CPU-presented surface live; initialization and the first GPU frame have separate terminal deadlines. The DVM primes the full provider-stride atlas upload, fixed textured draw, EGL native fence, and atomic KMS present within the 500 ms setup budget; a clear-only prime cannot activate the model. Only that representative current-epoch prime, exact provider pitch, bounded mapping, ready retained scene, and completed GPU frame activate the path. Each subsequent frame consumes one non-accumulating cadence permit, matching uiserver's cumulative 16 ms deadline and early-call `EAGAIN` backpressure. Provider revoke clears the epoch and requires a fresh prime. TLC checked 1,464 distinct states and both temporal settlement properties without error. The first paced QEMU capture exposed repeated provider loss and visible `display not available` because the 16.667 ms target was also the hard fence timeout. Post-separation packaged captures no longer show that marker or provider revoke; the 50 ms hard timeout preserves the live epoch while the separate 16.667 ms performance gate still rejects latency tails. |
| dvm-gpu-atlas-transport | `formal/dvm-gpu-atlas-transport/DvmGpuAtlasTransport.tla`; `libs/driver-domain-protocol/src/lib.rs`; `services/uiserver/src/{gpu_scene.rs,gpu_runtime.rs}`; `kernel/io-manager/src/io/dvm_display.rs`; `driver-domains/linux/package/rustos-dvm-display/src` | Source/model matched for transport contract 3. One immutable atlas generation and one bounded command batch occupy one of exactly three host-created slots. The first texture update must define the full atlas; later non-overlapping bounded damage records may update only changed pixels, and command-only cursor movement may carry no pixel damage. Host publication is serialized, the DVM consumes the lowest outstanding transport sequence, and its GLES runtime independently requires ordered submit/content epochs. uiserver confirms the first GPU+KMS completion synchronously, then maintains at most three asynchronous frames and retires completions in order without CPU final-frame fallback. The rebuilt packaged-QEMU run passed the three-slot staged-copy GPU-composition and explicit-fence path without revoke; that mode and the future physical read-only DMA-BUF mode cannot exchange zero-copy evidence. TLC exhaustively checked 238,264 distinct states without invariant error. Physical zero-copy import/scanout remains a failed hardware gate. |
| gui-dvm-pixel-authority | `formal/gui-dvm-pixel-authority/GuiDvmPixelAuthority.tla`; `tools/xtask/src/kvm.rs`; `kernel/io-manager/src/io/dvm_display.rs`; `driver-domains/linux/package/rustos-dvm-display/src/rustos_dvm_ivshmem_uio.c` | Matched: the UC ivshmem BAR carries only versioned control/MSI-X state; a separate 32 MiB cacheable pixel region is writable only in RustOS QEMU and exposed read-only/ROM to the DVM. The Linux module requires byte-identical control and pixel headers, maps pixel pages WB read-only, and rejects `VM_WRITE`/`VM_MAYWRITE`. TLC proves a READY slot is complete and host-written, only one host snapshot writer exists, incremental construction requires the exact predecessor content generation and unchanged captured source, retained content is distinct from release authority, DVM write attempts do not change pixel epochs, newest selection is fresh, and loss of either plane revokes selection. Source publishes the generation only after a complete copy and valid record exist; an unexpected competing writer clears the touched slot and revokes the transport. |
| gui-dvm-install | `formal/gui-dvm-install/GuiDvmInstall.tla`; `kernel/io-manager/src/io/dvm_display.rs` | Matched: the GUI-DVM installer is a serialized, one-way capability admission. It maps the validated UC control BAR and BAR0 doorbell, requires the fixed cacheable pixel region to contain the identical V3 header and geometry, allocates exactly two permanent MSI-X vectors, and registers the provider once. Every validation, arm, or registration failure releases acquired device mappings before terminal rejection; a rejected or runtime-revoked transport has no retry or fallback provider path. |
| ivshmem-pairing | `libs/driver-domain-host/src/ivshmem.rs` `wait_for_peer_count`; `tools/xtask/src/kvm.rs` `spawn_guests` | Fixed during this audit: peer ID assignment is no longer inferred from process spawn order. The launcher waits for the RustOS QEMU ivshmem connection before it starts the GUI DVM, and a broker-observed disconnect terminates the pair instead of allowing ID reuse. |
| ipc-reply-deadline | `kernel/ipc-runtime/src/ipc/mod.rs`; `kernel/compat/src/user/syscall/linux/ipc_ops.rs` `wait_for_reply_with_deadline` | Matched: reply completion is one-shot; arm/recheck/commit, cancellation, and peer-close have explicit paths. |
| scheduler-wakeup | `kernel/ps/src/multitask/scheduler.rs`, `current.rs`, and `irq.rs` | Matched: wake clears `wake_armed`, commit reports the race, and monotonic deadline service runs before scheduling. |
| clocksource-deadline | `kernel/hal/src/arch/{acpi.rs,clock.rs,rtc.rs}`; `kernel/hal/src/hooks.rs`; `kernel/ps/src/multitask/{current.rs,scheduler.rs,irq.rs}` | Fixed during this audit: ACPI publishes the HPET GAS correctly; invariant TSC is admitted only after CPUID validation and HPET/CPUID frequency calibration, with validated 64-bit HPET as the bounded fallback. RTC remains calendar-only. PIT services absolute clocksource deadlines, so coalesced edges cannot slow elapsed time. Both deadline construction and the outer sleep completion loop now read the same monotonic `ticks()` domain; the retired `RTC_TICKS` outer condition could never advance after periodic RTC shutdown and caused every finite UI poll to loop forever. Sleep uses the scheduler task id and arm/register/recheck/commit to reject a raced wake. |
| scheduler-admission | `formal/scheduler-admission/SchedulerAdmission.tla`; `services/runtimed/src/{main.rs,spawn.rs}` | Fixed during this audit: launch-catalog `weight` is untrusted policy input. Runtimed clamps every non-UI launch to the ordinary fair-share ceiling; only the exact `services/uiserver/uiserver.elf` path receives the source-pinned UI System weight. A path lookalike and an arbitrarily large registry value cannot enter the strict band. |
| ipc-priority-inheritance | `kernel/ps/src/multitask/{scheduler.rs,current.rs}`; `kernel/compat/src/user/syscall/linux/ipc_ops.rs` | Fixed during this audit: every live synchronous reply can carry one bounded caller-to-receiver donation. Its effective System class crosses nested calls, while completion, timeout cancellation, and either task's retirement revoke the exact donation before a stale boost can survive. A saturated System lane is capped at two consecutive dispatches before one ready User turn. Separately, a live exact-version netd local-event response may enqueue only a User target in the bounded latency FIFO; unknown flags, foreign owners, System targets, stale entries, and more than eight consecutive handoffs fail closed. |
| ipc-handle-transfer | `kernel/ps/src/user/handles.rs`, `kernel/ipc-runtime/src/ipc/mod.rs`, `kernel/compat/src/user/syscall/linux/ipc_ops.rs`, `kernel/ps/src/multitask/current.rs` | Fixed during this audit: descriptor registry ownership moved to the process substrate; cancel, peer-close observation, receive-output rejection, and caller exit now converge on exactly-once drop/install. |
| ipc-endpoint-ownership | `kernel/ipc-runtime/src/ipc/mod.rs`, `kernel/compat/src/user/syscall/linux/ipc_ops.rs`, `kernel/ps/src/multitask/current.rs`, `kernel/ps/src/user/handles/table.rs`, `service_ops/vfs_socket.rs` | Fixed during this audit: user receive/reply paths require the endpoint owner process, so service worker threads remain valid while foreign processes fail closed; process exit revokes process-owned endpoint authority; `dup2`/`dup3` and `F_DUPFD*` reject targets beyond the bounded descriptor ceiling. |
| proc-broker-session | `kernel/compat/src/user/syscall/linux/proc_broker_ops.rs`, `services/loaderd/src/main.rs`, process-exit paths | Fixed during this audit: process exit and signal termination now purge owner-bound uncommitted `PROC_PREPARES`. Commit remains terminal by design because it removes the handle before later validation. A zero session argument inherits the exact caller/target-thread session only; missing state returns `EINVAL`, never system-session authority. |
| commercial-service-envelope | `services/rootd/src/main.rs`; `services/storaged/src/main.rs`; `libs/rustos-user-abi/src/syscall.rs` | Fixed during this audit: rootd and storaged accept only their versioned commercial protocol envelopes. The unreferenced private request/response ABIs and dispatchers were removed. A short or oversized synchronous request receives an explicit commercial-protocol `EINVAL` response instead of being reinterpreted or abandoned without reply. Storaged additionally rejects nonzero arguments, payloads, and operation-incompatible paths instead of silently ignoring ambiguous input. |
| exec-ticket | `services/procd/src/main.rs`, `services/loaderd/src/main.rs`, `kernel/compat/src/user/syscall/linux/proc_broker_ops.rs`, normal/signal process-exit paths | Fixed during this audit: authorize now requires an exact live PID/TID and rechecks post-publication exit; cancel/exec validate before remove; transition publication precedes target image replacement; non-final TID exit, exec sibling retirement, and process exit purge target tickets/transitions; loaderd aborts early rejected commit state. |

## Discrepancies found and closed

1. `TRANSFER_OBJECTS` was owned by compat while endpoint removal and task exit
   occur in IPC runtime/process substrate. A handle-aware receive could dequeue
   descriptors, fail later user-output validation, and leave registry entries
   unreachable; caller exit also left queued request/reply descriptors alive.
   The registry now resides in `kernel-ps`; runtime returns detached descriptor
   batches, and compat/PS dispose of them on every terminal path.
2. `PROC_PREPARES` was owner-bound for authorization but had no owner-exit
   cleanup. A loader crash between `PREPARE` and `COMMIT` could retain bounded
   prepare slots and pinned mapping metadata. Both ordinary exit and
   signal-driven process termination now remove the owner's uncommitted state.
3. Exec authorization accepted any live TID without proving its requested PID,
   while cancel and exec-target removed tickets before comparing that pair. A
   malformed request could therefore authorize a mismatched target or destroy
   a valid ticket. Target-thread exit and exec sibling retirement also retained
   pending tickets/register handoffs, and the handoff was inserted after image
   replacement. Authorization now checks the exact live pair, removal follows
   comparison, transition insertion precedes replacement, and thread/process
   teardown purges target-bound ticket state.
4. `runtime-control` accepted any successful response opcode and negated every
   nonzero signed status. A stale/cross-RPC reply could therefore become a
   false launch/terminate success, while `i32::MIN` could overflow. Response
   identity and shape are now validated before success; server error envelopes
   retain their documented opcode-zero compatibility.

## Remaining boundaries outside this audit

- The signed post-cleanup QEMU capture source-matches the retired-path guards:
  GPU/display readiness followed only staged-copy GPU composition with explicit
  fences and three scanout buffers, and no legacy/direct-import or
  display-unavailable marker occurred. It remains performance-blocking rather
  than release evidence: the private proof reached 110.389 FPS, but settled
  relay samples were 50.730--59.105 FPS and WayClick was 35.894--41.776 FPS.
  uiserver cannot yet include aggregate Wayland/AF_UNIX readiness in its wait;
  the general cross-provider wait-set ABI is a separate required boundary.
- Public IPC calls intentionally retain blocking ABI semantics; only internal
  policy-service waits are deadline-bounded.
- The scheduler distribution model proves the one-System/one-User bounded
  reservation only. It does not prove multicore CPU-time distribution or
  hardware interrupt latency; target measurements remain release-blocking.
- ELF/PE byte parsing and page-table lifecycle now have finite abstractions and
  mapped source tests. Kani also proves the compositional decoder,
  segment/section, entry, single-relocation, and single-import contracts. Full
  multi-block corpus fuzz/native launches and target TLB behavior are
  independent release evidence and remain open.
- **Release-blocking hardware gap:** the supervised physical display-DVM source
  requires a non-identity QEMU IOMMUFD/VFIO address space, read-only display DMA,
  complete-group reset, and fail-closed revoke. Target fault/revoke/reset and
  guest-kernel captures are still absent. The separate RustOS native boot-device
  path also retains an identity-only kernel backend. Both remain failed gates;
  the source state machines and TLC runs do not substitute for hardware proof.
- No enabled topology advertises a trusted physical display or trusted
  human-input device. The trusted-UI model verifies the fail-closed admission
  rule and source provenance only; an in-band DVM display overlay cannot
  authenticate to a human, so privileged-prompt admission remains blocked.
