# TLA+ source-conformance audit

This is a source-level audit of the contracts modeled in this directory. It
checks that the Rust transition, identity, and cleanup boundary exists at the
named source anchor. It is not a claim that finite TLC exploration proves Rust
equivalence, CPU memory ordering, ELF/PE parser safety, or full scheduler CPU-time fairness.

## Executable cross-tool mappings

| Registered model | Additional executable evidence | Boundary |
| --- | --- | --- |
| ipc-reply-deadline | `services/rootd/src/main.rs`; `services/runtimed/src/{main.rs,session.rs,spawn.rs}`; `services/netd/src/main.rs`; `formal/run-source-conformance.sh` | The bounded receive primitive combines a service endpoint with an RTC deadline and withdraws a receiver waiter after timer wake. Rootd, runtimed, and netd use message-or-timeout idle waits; runtimed parks only bounded empty console reads and netd folds detached poll deadlines into its receive budget. |
| runtime-control-rpc | `libs/runtime-control` test emits `rustos-formal-trace-v1`; `check-runtime-trace.py` independently replays `ReceiveResponse` and its `Classify` matrix | Six concrete source outcomes, not production telemetry or arbitrary socket interleavings |
| endpoint-publication | TLAPS proves `EffectiveEndpointRequiresRunningOwner`; Loom enumerates the lock/recheck versus exit/revoke proof kernel | The theorem is an unbounded definitional lemma and Loom is a mapped kernel, not a full-source equivalence proof |
| pre-QEMU concurrency triangle | `concurrency-triangle.toml` binds critical endpoint, IPC, wait-set, IPI, TLB, and scheduler stack-owner publication flows to source anchors, Loom, Shuttle PCT, and, for atomic publication, x86_64 herd7 baseline/mutant pairs | Not a full Rust/C11/compiler proof, generic SMP coverage, device-ordering proof, or target-runtime execution |
| bounded and unbounded Rust proof kernels | `proof-index.toml` binds every selected Kani harness and Verus lemma to one source symbol, registered TLA+ model, executable counterpart, dependency edge, and hashed evidence; the checker rejects missing `kani::cover!`, unindexed Verus files, cycles, and trusted Verus shortcuts | Kani remains bounded and Verus proves only the stated ghost theorem; neither establishes Rust-source equivalence, compiler mapping, IPI delivery, or unindexed ring0 code |
| exec-ticket | typed `apalache-pilots/ExecTicketPilot.tla`, bounded to eight transitions | Exact-target and one-shot core only; full sibling/prepare lifecycle remains in TLC |
| ipc-handle-transfer | typed `apalache-pilots/IpcHandleTransferPilot.tla`, bounded to eight transitions | One descriptor only; TLC retains batch and teardown interleavings |
| dual-abi-byte-parser and network-payload-session | Rust libFuzzer drives the shared ELF and Ethernet admission functions; Clang libFuzzer/ASan/UBSan includes and drives the exact DVM GPU C batch parser | Bounded smoke runs are bug finding, not exhaustive proof or a sustained corpus campaign |
| dual ABI compiled contract | `run-abi-differential.sh` executes native Linux and MinGW/Wine probes beside `abi_contract_probe`; `abi-divergences.tsv` is exact and expiring | Constants and layouts only; syscall behavior remains covered by source/runtime witnesses |
| checkpoint, service, and storage recovery | `run-recovery-scenarios.sh` executes the complete disruption-class registry with exact filtered tests and per-scenario deadlines | Source-level bounded outcomes, not physical power-cut evidence |
| critical/high mutation sensitivity | run-spec-mutations.sh applies one exact temporary TLA+ property/transition mutation and requires its named invariant plus normalized counterexample trace; run-implementation-mutations.sh rejects duplicate semantics, seals an exact unique or `N/M` anchor in an isolated live-tree copy, resolves one fully qualified libtest, proves its unchanged baseline executes, and requires that same witness to fail after injection | Registry-selected lifecycle decisions, not whole-source mutation completeness or execution of a temporary model mutant against production RustOS |
| host-testable critical/high instrumentation | `run-sanitizers.sh` rebuilds the matching standard library and registered Rust targets under pinned address/thread instrumentation | Host paths only; target assembly, DMA, and hardware remain outside this lane |

## Audited formal and source contracts

| Model | Rust transition/linearization anchors checked | Result |
| --- | --- | --- |
| boot-storage-handoff | `tools/hostd/src/{storage.rs,main.rs,runtime.rs}`; `libs/driver-domain-host/src/lib.rs` | Source contract matched: schema-4 admission opens the exact whole device exclusively, rejects holders/mounts/swaps across the whole partition family, completes bounded `fsync` plus `BLKFLSBUF`, creates and Ed25519-signs a private monotonic aperture epoch, and durably binds it to the exact VFIO lease before launch. Durable lease schema 5 persists the SHA-256 of the exact immutable signing bytes plus signature before VFIO assignment; hostd rechecks it before launch and readiness admission. The caller-owned raw signing seed must be a non-symlink regular file with no group/world permissions and never enters the DVM. QEMU remains behind a parent gate until the exact PID/start-time record is fsync'd. Authenticated readiness must match the policy, controller, block geometry, access mode, live aperture, and generation; read-only evidence is accepted only when the signed aperture is also read-only. Direct release rejects an active storage lease; recovery first terminates and observes the exact recorded QEMU process, then revokes the aperture before reset/original-driver restoration. Revocation clears readiness/cursors but preserves the signed immutable read-only flag, making explicit retry idempotent for both writable and read-only epochs. Revocation or reset/restore failure retains the durable record and quarantine. RustOS ring0 has no physical-controller authority. Physical NVMe/AHCI fault/reset/revoke capture remains hardware evidence. |
| storage-DVM independent KVM acceptance | `tools/xtask/src/kvm.rs`; `docs/ai/commands.md` | `--storage-dvm-only` enables the private block topology and requires RustOS boot/provenance, authenticated DVM control, both block peers, exact live geometry, first completion, and the exact storaged marker `storaged: dvm-block e2e media barrier completed generation=` with `path=vfs-policy->block-broker->shared-ring->linux-dvm->media-barrier`, while deliberately excluding unrelated GPU/UI readiness. This is an exact-generation read-only media-barrier/liveness proof, not backing-image or device-write durability. Its negative `block.flush` fault gate admits only one exact rule, replaces normal media-barrier success with the live-generation injection marker, retains first-completion and geometry checks, and rejects an impossible simultaneous media-barrier-success marker. Incompatible UI/input/network/physical options fail at argument admission. Rebuilt-DVM evidence passed `cargo xtask build-dvm`, `cargo xtask verify-dvm`, and `cargo xtask kvm-smoke --timeout 30 --storage-dvm-only`; the runtime trace reached `terminal_ms=2259`. |
| product-boot | `formal/product-boot/ProductBoot.tla`; `formal/product-scenarios.tsv`; `tools/xtask/src/formal_contracts/{registry.rs,runtime_trace.rs}`; rootd/input/display/storage/vfs/WayClick milestone sources | Independent component models refine a dependency DAG instead of being converted post hoc from host booleans or forced into a false total order. Fixed milestone IDs enter a kernel-timestamped closed vocabulary; replay requires the exact scenario, every declared predecessor to precede its dependent event, all branches to join the terminal, per-stage absolute deadlines, and the topology's ten-second interactive terminal. Display and storage may finish in either order. Interactive success additionally requires a terminally sealed executable snapshot and a real first-frame callback. Uiserver retries only transient input wait-set control failures under the five-second boot-control deadline while each mutation keeps its 100 ms reconciliation bound; permanent provider or shape failures remain terminal. The independent storage-only branch retains its five-second deadline and excludes UI. This is target runtime evidence for the named KVM topology, not proof of physical hardware or every service trace. |
| dvm-block-startup | `formal/dvm-block-startup/DvmBlockStartup.tla`; `kernel/io-manager/src/io/dvm_block.rs`; `kernel/compat/src/user/syscall/linux/block_broker_ops.rs`; `services/storaged/src/{block.rs,main.rs}` | The fixed aperture is admitted only when ABI-v2 immutable geometry and generation verify under the Ed25519 key in the GRUB-authenticated early-system header. RustOS readiness is published by compare-exchange from the exact verified flags snapshot, so a peer race fails without leaving a false ready bit. The aperture may be installed before Linux publishes `DVM_READY`; that initial state is sleepable and cannot become a fault or bootstrap-storage fallback. Once fixed PCI enumeration finds ivshmem functions but no exact block-aperture shape, that topology absence is cached; an exact-shaped aperture with an unready header is never negative-cached. Storaged applies one shared four-second deadline to readiness and the first generation-bound read-only media-barrier liveness proof and sleeps in at most one-second broker slices. The kernel wait predicate reports a newly observed ready edge, uses check-arm-recheck, and treats readiness withdrawal after observation as revoke. A revoked mapping accepts only a strictly newer, zero-cursor, L0-signed epoch; unsigned, stale, or DVM-forged successors remain revoked. Volume use follows an exact-generation media-barrier round trip; the read-only qualification path makes no backing-device write-durability claim. |
| early-system-admission | `boot/boot-protocol/src/lib.rs`; `kernel/nucleus-core/src/multiboot2.rs`; `kernel/io-manager/src/storage/boot_volume.rs`; `kernel/compat/src/user/syscall/linux/early_system_broker_ops.rs`; `services/vfsd/src/early_system.rs`; `tools/xtask/src/{stage/mod.rs,build/mod.rs}` | Source contract matched for admission and no physical fallback: BootInfo v18 carries one bounded module range and requires the retired physical-extent words to remain zero; Multiboot2 accepts only one exact command line; schema-2 fixed header binds a nonzero storage-epoch verifying key and entries reject reserved fields, aliases, bad bounds, duplicates, disorder, and digest mismatch; the deterministic staging allowlist is signed and loaded by GRUB. A vfsd-capability-only broker exposes only exact immutable file length and bounded reads, while vfsd retains path/open/cursor/checkpoint policy and overlays those entries before the DVM volume. No native NVMe/AHCI, FAT opener, physical descriptor, or boot-extent path remains. |
| dual-abi-image-admission | `libs/rustos-image-admission/src/lib.rs` `admit_image`; `services/loaderd/src/main.rs` ELF `validate_elf_fd` and PE `load_pe_image_fd` | Fixed during this audit: both formats now cross one source-tested admission gate before broker mapping. Overflow, out-of-window and overlapping regions, W+X mappings, missing main entry points, and entries outside executable memory fail closed. PE DLL entry zero remains the only explicit entryless case. Byte-level source conformance is checked separately by the byte-parser model, Kani, fuzzing, and focused tests. |
| dual-abi-byte-parser | `libs/rustos-image-admission/src/lib.rs` `admit_elf64_image`, `admit_pe64_image_headers`, `apply_pe64_base_relocations`, `validate_pe64_import_table`; `services/loaderd/src/main.rs`; `kernel/compat/src/user/syscall/linux/proc_broker_ops.rs` exact file copy | Matched at source level: exact-size header tables, W^X sections, bounded relocation/import tables, missing terminators, truncated file mappings, and post-parse snapshot stability fail closed before mapping. Pinned Kani 0.67.0 proves exact integer decoding, arbitrary single ELF segment and PE section admission, common entry/W^X rules, relocation-table absence, an arbitrary single relocation entry, and an arbitrary single import thunk. Arbitrary-length multi-block/multi-descriptor corpora and native launch captures remain release evidence. |
| page-table-lifecycle | `kernel/compat/src/user/syscall/linux/mm_broker_ops.rs`; `kernel/mm/src/memory/address_space.rs` range and flag normalization | Matched at source level: broker start/end arithmetic rejects noncanonical or wrapping ranges before constructing `VirtAddr`, mapping cursors advance to the page-rounded region end, writable user pages require NX, huge-page flags are rejected, and teardown owns frame release. Target TLB shootdown and multicore hardware evidence remains open. |
| page-cache-lifecycle | `services/pagerd/src/page_cache.rs`; `libs/rustos-user-abi/src/pager.rs`; `formal/page-cache-lifecycle/PageCacheLifecycle.tla` | Phase-8 policy foundation matched: ABI v2 binds file objects to a nonzero backing-service identity and pagerd keys entries by service, object generation, backing generation, and page offset. One bounded miss owner publishes a clean frame; concurrent misses coalesce. Private COW returns a distinct frame while the shared source remains clean. Dirty pages require an exact writeback token. Provider restart permanently seals only the exact old backing generation; mapped frames remain quarantined until mapping release plus exact TLB acknowledgement, while dirty frames require exact-token reauthorization to a newer generation with unchanged object/page identity. Production vfsd/storaged I/O, ring0 fault/PTE integration, pressure/restart QEMU, and dual-ABI runtime evidence remain open, so Phase 8 is not closed. |
| pager-frame-grant-lifecycle | `kernel/mm/src/memory/frame_capability.rs`; `kernel/compat/src/pager.rs`; `formal/pager-frame-grant-lifecycle/PagerFrameGrantLifecycle.tla` | Matched through the live first-touch transaction: kernel-mm publishes only a bounded opaque generation handle bound to the exact fault/process/MM/VMA/pager epoch and non-W+X rights. Pagerd may return only that handle with attenuated rights; kernel-compat consumes it once, revalidates the live address space, commits the prepared PTE, or returns/frees custody on every rejection. Stale generation, mismatched binding, rights escalation, publication pressure, and generation exhaustion fail closed without exposing a physical address. |
| pager-fault-slot-lifecycle | `kernel/ps/src/multitask/pager_fault.rs`; `kernel/ps/src/multitask/scheduler.rs`; `kernel/compat/src/user/syscall/linux/pager_fault_ops.rs`; `kernel/compat/src/pager.rs`; `formal/pager-fault-slot-lifecycle/PagerFaultSlotLifecycle.tla` | Matched through the live direct rendezvous: exception ingress reserves one fixed all-atomic slot, commits the exact `PagerFault(token)` wait, and directly hands scheduler custody to an endpoint-matching pagerd worker without the generic IPC runtime or housekeeping. The waiter syscall binds the donor's bounded scheduling context to that same token before returning the dispatch. Only the exact worker can claim the reply; completion or cancellation releases donation before waking the donor. Reservation and exception handoff do not allocate, take `ProcessStateLock`, perform service lookup, or acquire the donation ledger. Runtime latency still requires fresh KVM measurement and is not inferred from this source/model proof. |
| pager-region-agreement | `libs/rustos-user-abi/src/pager/region_edit.rs`; `kernel/ps/src/multitask/pager_vma.rs`; `services/pagerd/src/lib.rs`; `kernel/compat/src/user/syscall/linux/mm_broker_ops.rs`; `formal/pager-region-agreement/PagerRegionAgreement.tla` | Matched at the replica boundary: ring0's per-process VMA table and pagerd's region table now apply one shared `apply_region_edit` rule, so a partial `munmap` trims or splits on both sides instead of surviving on one and being deleted on the other. Backing offsets shift with every surviving fragment; `mprotect` narrowing is published to pagerd, which decides granted frame rights from the protection it holds; `PROT_NONE` is the one declared asymmetry - ring0 keeps a deny-all VMA, pagerd keeps nothing, because a span with no rights raises no fault and is not a canonical wire region. Both replicas prove split headroom before withdrawing a region, and a pager that cannot fit the second fragment keeps the whole region and refuses with `PAGER_PRESSURE_REGION_SPLIT_NO_SLOT` for the broker's parked retry, because keeping more than ring0 is inert while keeping less kills a live mapping. Capacity relations between the fault-slot table, the wired frame reserve, the grant table, and both region tables are static-asserted rather than coincidental. Multi-vCPU partial-unmap runtime evidence remains open. |
| pager-vma-publication | `kernel/ps/src/multitask/pager_vma.rs`; `kernel/ps/src/multitask/current.rs`; `formal/pager-vma-publication/PagerVmaPublication.tla` | Phase-7 prerequisite matched at the fault lookup boundary: kernel-ps stamps an unbound template with the current or retained target process/MM identity and a nonzero non-reused VMA generation, rejects overlap, W+X, protection escalation, and capacity pressure, and publishes every field atomically behind an even sequence commit. Target PID publication first retains the table slot, so a recycled PID or exec/exit boundary cannot authorize another address space. Exception lookup is bounded to two atomic snapshot attempts per slot, requires exact live process/MM authority and access permission, and observes exact-generation revoke without taking the scheduler or `ProcessStateLock`. Actual broker call-site cutover, fault blocking, kernel-generated pager IPC, and reply/PTE commit remain open. |
| physical-frame-lifecycle | `kernel/mm/src/memory/phys.rs`; `formal/physical-frame-lifecycle/PhysicalFrameLifecycle.tla` | Fixed and matched: firmware-usable spans are clipped to the direct-map ceiling, aligned, and filtered before publication; kernel/module ranges are reserved before allocation; allocation and release update one bounded bitmap authority; invalid or double release cannot mint a free frame; exhaustion returns no frame. The model includes the valid all-reserved terminal state explicitly instead of disabling deadlock checking. |
| service-heap-lifecycle | `libs/rustos-svc-runtime/src/allocator.rs`; `services/syscalld/src/linux_policy.rs`; `tools/xtask/src/kvm/evidence.rs`; `formal/service-heap-lifecycle/ServiceHeapLifecycle.tla` | Fixed and matched: the shared policy-service allocator no longer leaks every dropped value through a bump-only cursor. Exact allocation spans return to an address-ordered free list, adjacent spans coalesce, alignment is preserved, and another mapped region is admitted only on no-fit. Syscalld's next-fit hint wraps once to reusable post-heap gaps. Allocator failure and a fatal core-service readiness cascade now fail KVM acceptance and enter bounded causal evidence. |
| process-address-space-lifetime | `kernel/ps/src/multitask/{process_table.rs,current.rs,scheduler.rs}` | Fixed during this audit: `ProcessRef` no longer exposes references through a raw `UnsafeCell` pointer. A retained object reaches `UserProcessState` only through its per-process mutex. Exec now holds that mutex while atomically checking/updating the process-table exit marker, so a concurrent exit cannot be cleared after address-space replacement. Thread attachment now rejects an exiting process instead of clearing its monotonic exit marker; rejected attachment also releases its unpublished kernel-stack storage. The finite model separates stack preparation, publication, and rejection, permits exit between preparation and commit, freezes the exit-time thread-count ceiling, requires every prepared attachment to settle, and forbids reclamation while unpublished stack authority remains. Reclaim still requires zero task/reference ownership. Multicore target stress remains open. |
| process-lifecycle-transaction | `kernel/compat/src/user/{process/mod.rs,syscall/linux/proc_broker_ops.rs}`; `kernel/ps/src/multitask/{process_table.rs,spawn.rs,scheduler.rs}`; `kernel/mm/src/memory/{address_space.rs,address_space/{owned_frames.rs,rollback.rs},phys.rs}`; `formal/process-lifecycle-transaction/ProcessLifecycleTransaction.tla`; `docs/benchmarks/README.md` Phase-6 checkpoint | Phase 6 matched and closed: fork and loader-created processes reserve one invisible process-table generation and non-reusable lifecycle token before any child address-space mapping, bind the prepared image to that exact reservation, and consume it only when the suspended child is fully published. RAII failure settlement cancels the exact reservation and a cancellation mismatch is fatal in every build. Production process-state spawn has no unreserved alias. Exec reservations bind the exact process generation, MM-generation pair, and a non-reused lifecycle transaction; exit racing exec retains a distinct pending-exit transaction and attachment remains sealed; reap requires the exact process generation with zero task/reference ownership. Address-space failure returns every unpublished private frame only after rollback and shootdown, using bounded allocator batches, while invalid or duplicate return is fatal. Executable fault/race witnesses cover exec/exit, attach/exit, reap/slot reuse, spawn failure, and partial-frame rollback. Fresh one-vCPU tracing carried all nine required spawn/exec/exit/reap stages for 34 exact process/MM/transaction identities; fresh one- and eight-vCPU lifecycle/1,024-page probes passed isolation, and same-session anchored A-B-A measured a 64x free-side allocator-lock reduction without repeatable latency regression. |
| futex-waiter-lifecycle | `kernel/ps/src/multitask/{scheduler.rs,current.rs,retirement.rs}`; `kernel/executive/src/boot.rs`; `kernel/compat/src/user/syscall/linux/{service_ops/futex_thread.rs,waitset_broker_ops.rs,ipc_ops.rs}`; `kernel/io-manager/src/{input/wait_queue.rs,io/dvm_block.rs,sync.rs}` | Fixed and matched for BSP execution: completion and ABI-aware exit remove by exact task identity rather than the pre-requeue key, while every forced, fault, invalid-context, ordinary, and exec-sibling retirement publishes one exact deferred cleanup identity plus the retiring thread's `clear_child_tid` and robust-list snapshot. Cleanup accepts only the exact 24-byte Linux robust-list head, checks signed futex offsets and user ranges, traverses at most 2048 entries, processes `list_op_pending`, skips unsupported PI entries without claiming them, changes only a word still owned by the retiring TID, preserves `FUTEX_WAITERS`, sets `FUTEX_OWNER_DIED`, and wakes one matching waiter. Exec siblings remain quarantined until all runtime authority is removed and the same `(task_id, process_id)` is acknowledged. The finite model covers robust publication, pending operations, owner death, waiter wake, and deferred cleanup. Before AP execution is enabled, the user-word read/modify/write must be replaced by a translated atomic compare-exchange primitive; SMP robust cleanup is therefore an explicit failed enablement gate, not silently claimed. |
| kernel-resource-accounting | `kernel/ipc-runtime/src/{ipc/mod.rs,api.rs}`; `kernel/ps/src/multitask/process_table.rs`; display shared-region callers in `kernel/{compat,io-manager}` | Fixed and matched: process/task endpoint ownership is capped at 32/8 with 64 global endpoint slots reserved for kernel/anonymous substrate. The owner quota is reserved before endpoint queue allocation and is rolled back on allocation/publication failure. Shared regions retain the 256-MiB object ceiling and add a 512-MiB global admitted-byte ceiling plus per-process ceilings of 64 objects and 128 MiB. Region owner and byte charges survive handle drop and return only after deferred physical reclaim completes. Process task attachment is capped at 32 before count mutation, preventing one process from consuming the 256-slot scheduler table. The finite model covers process exit, immediate endpoint/task revocation, deferred region reclaim, and exact owner/global bounds. |
| process-signal-delivery | `services/procd/src/main.rs`; `libs/rustos-user-abi/src/{linux.rs,syscall.rs}`; `kernel/executive/src/lib.rs`; `kernel/compat/src/user/syscall/{mod.rs,linux/{support.rs,syscalld_ops.rs,offload_ops.rs}}`; `kernel/ps/src/multitask/{scheduler.rs,process_table.rs}` | Matched for delivery, scheduler state, wait status, and parent notification: ring0 revalidates a procd selection against the pending/mask snapshot, strips SIGKILL/SIGSTOP from masks, and accepts handler/restorer/frame addresses only inside the user window. Procd ABI v3 classifies default stop signals separately from termination and carries coalesced SIGCHLD exit/stop/continue causes. A stop marks every live Linux task in the process unschedulable without destroying its pre-stop ready/block state. SIGCONT resumes every task before its own mask/disposition is processed; SIGKILL also clears the stop gate so a stopped process can terminate. `wait4` accepts `WUNTRACED`/`WCONTINUED` and consumes exact Linux `0x7f` stopped or `0xffff` continued child status only when requested. Parent SIGCHLD targets the live process leader or a live fallback thread; `SA_NOCLDSTOP` suppresses only stop/continue causes, never a coalesced exit, and ring0 removes only the exact revalidated cause snapshot so a later cause remains pending. Termination/fatal fault still use one complete cleanup-and-publication commit. The process-signal and SIGCHLD finite models cover stale policy replies, stop, resume-before-delivery, child-status consumption, cause coalescing, policy suppression, recoverable fault, and terminal authority revoke. |
| sigchld-notification | `libs/rustos-user-abi/src/{linux.rs,syscall.rs}`; `services/procd/src/main.rs`; `kernel/compat/src/user/syscall/linux/support.rs`; `kernel/ps/src/multitask/scheduler.rs` | Matched: exit, stop, and continue causes coalesce beside one pending SIGCHLD bit. Selection carries an exact bounded cause snapshot. A receiving thread transfers those causes to a live sibling before retirement. `SA_NOCLDSTOP` suppresses a nonempty stop/continue-only snapshot but cannot suppress a snapshot containing exit. Ring0 accepts only a selected subset of still-pending causes and removes exactly that subset, retaining any cause queued during the procd round trip. The finite model includes concurrent cause consumption and rejects a stale snapshot. |
| netd-deferred-reply | `libs/rustos-user-abi/src/syscall.rs`; `kernel/compat/src/user/syscall/linux/{ipc_ops.rs,service_ops/vfs_meta.rs,service_ops/vfs_socket.rs}`; `services/inputd/src/{main.rs,dvm_session_sync.rs}`; `services/netd/src/main.rs` | A global atomic reservation remains owned while a worker processes a detached AF_UNIX poll batch, so concurrent queue admission cannot exceed 32. Netd ABI v7 carries one nonzero caller-owned absolute monotonic end; the kernel may shorten it to the admitted reply class and netd may shorten it once at ingress, but neither queueing nor retry can refresh it. Expiry precedes queue reservation and provider entry; AF_INET stream creation also repeats that exact end under the state lock before provider mutation. A rejected reply-bound AF_INET bind discards its unpublished token exactly once. Every accepted queued/detached request eventually makes one terminal reply attempt and releases one reservation, and a response observed after the caller end cannot revive its one-shot reply authority. Queue poison drains into bounded `EIO` replies. A reply attempt is not misreported as confirmed delivery. |
| memfd-seal-lifecycle | `kernel/ps/src/user/memfd.rs` | Matched and hardened: the object mutex serializes atomic seal installation, mapping holds and resize; write sealing rejects writable mappings, shrink rejects any mapping, EOF-extending writes now honor `F_SEAL_GROW`, and mapping admission rejects counter overflow instead of saturating ownership accounting. |
| msi-vector-lifecycle | `kernel/hal/src/arch/msi.rs`; `kernel/io-manager/src/{input/dvm_ring.rs,io/dvm_block.rs,io/dvm_display.rs}` | MSI setup now holds an unpublished atomic vector lease. Every DVM MSI-X installer binds its exact leaf handler while the device is masked, and any later validation or mapping failure drops the lease, clears that handler, and returns the slot. Only a completely programmed route commits permanently, so hostile setup failures cannot exhaust or steal the bounded vector pool. |
| acpi-table-admission | `formal/acpi-table-admission/AcpiTableAdmission.tla`; `kernel/hal/src/arch/acpi.rs` | Firmware tables are admitted as external input: RSDP/SDT lengths are capped, RSDT/XSDT signatures and entry widths are exact, zero child pointers are rejected, MCFG publication stages and atomically commits only aligned bounded non-overlapping ECAM regions, and HPET requires an aligned system-memory GAS with exact offset/access semantics. Rejection leaves no partial ECAM or HPET authority and uses only the bounded legacy PCI fallback. |
| cpu-topology-admission | `formal/cpu-topology-admission/CpuTopologyAdmission.tla`; `kernel/hal/src/arch/acpi.rs` | MADT local-APIC and x2APIC processor entries are staged into a dense logical index independent of raw APIC ID, then the executing BSP is required and normalized to logical CPU zero before publication. Truncation, reserved flags, duplicate UID/APIC identity, an absent BSP, hot-add-only CPUs, an invalid APIC override, zero CPUs, and more than eight CPUs reject the complete topology. |
| cpu-online-lifecycle | `formal/cpu-online-lifecycle/CpuOnlineLifecycle.tla`; `kernel/nucleus-core/src/{ap_trampoline.rs,multiboot2_entry.S}`; `kernel/mm/src/memory/cache_attributes.rs`; `kernel/hal/src/arch/{smp,gdt,msi}.rs`; `kernel/ps/src/user/syscall.rs`; `kernel/nucleus-core/src/util/lockdep.rs`; `kernel/executive/src/boot.rs` | The admitted topology is release-published once with generation one. State compare-exchange admits only Discovered → Starting → OnlineParked → SchedulerReady → Online and explicit failure/quarantine paths. This is a boot-static envelope: Failed remains terminal until reboot, and explicit stale-generation or in-boot restart attempts raise the fail-stop state instead of reusing generation one. Before the first SIPI, the BSP seals one MTRR/PAT/cache baseline with release publication. The reset AP clears NW while retaining CD, executes WBINVD before mailbox/paging, acquires and restores that exact baseline in no-fill mode, flushes cache/TLB state, then enables cache only after readback. The BSP atomically claims two boot-usable low pages, verifies one RX trampoline, serially publishes one generation mailbox, sends bounded INIT-SIPI-SIPI to the exact xAPIC destination, waits for OnlineParked, and retires both startup pages R/NX. Each logical CPU owns distinct bootstrap/GDT/TSS/RSP0/IST and GS/syscall storage. Lockdep owns distinct raw-stack, IRQ-depth, preemption-depth, and current-task slots selected by a dense APIC map. Dispatch authority still cannot exist before scheduler readiness. |
| smp-reschedule-ipi-lifecycle | `formal/smp-reschedule-ipi/SmpRescheduleIpi.tla`; `kernel/ps/src/multitask/{cpu_local,irq,reschedule_observation}.rs`; `kernel/ps/src/multitask/scheduler/{runqueue_policy,smp}.rs`; `kernel/hal/src/arch/msi.rs`; `kernel/lowlevel/src/interrupts.rs` | Each active CPU publishes one unique current-task slot. One packed per-CPU word linearizes the monotonic request sequence with pending; an AcqRel claim clears pending and returns that exact sequence, so a racing later publisher must create a new edge. Remote wake, affinity, stop/continue, exec, and retirement name the exact runqueue owner; a lock-held 0→1 target edge retains one target bit until raw scheduler unlock, then emits only that target's fixed vector. Same-CPU work uses the return safe point and never a fixed self IPI. A raw-lock interruption acknowledges without clearing its request, while an admitted safe point claims and eventually consumes it. Duplicate running ownership, lost generation, self notification, or unrelated-CPU broadcast is fatal. |
| scheduler-cpu-ownership | `formal/scheduler-cpu-ownership/SchedulerCpuOwnership.tla`; `kernel/nucleus-core/src/util/lockdep.rs`; `kernel/lowlevel/src/interrupts.rs`; `kernel/hal/src/interrupt_stubs.rs`; `kernel/ps/src/multitask/{cpu_local,irq,scheduler,scheduler/{smp,runqueue,runqueue_policy}}.rs` | A raw acquisition reserves one exact pending preemption unit, then converts pending-to-held or cancels it in a local-IRQ atomic transition. Stable accounting requires total depth to equal pending plus held units, so an interrupt-side acquisition cannot consume the process guard's pin. A published guard captures one dense CPU index and architectural APIC identity. Lifecycle custody is exact across current, blocked, and queued states, while the CPU-local stack-transition lease remains a separate non-dispatchable dimension. Dispatch rejects every task with an active transition lease. A blocked outgoing task woken during that lease moves `Blocked -> RemoteQueued`, publishes one target mailbox record before its notification hint, and may retain both the stack lease and queued custody until assembly commit; legacy `ready` is not authority. Only the aligned assembly edge after `rsp` names the incoming frame releases the outgoing stack, after which the queued owner may be drained and claimed exactly once. A guarded CPU cannot expose a stack transition. Its exact source/destination token requires CR3/TSS/syscall-stack/segment/FS/GS restoration for a real slot switch and retains the already-active state only for an equal-slot turn; SIMD restore remains unconditional after the save boundary. Scheduler-backed observations remain unavailable during this edge. IRQ work is retained while guarded; missing or duplicate authority, premature stack release, accounting disagreement, cross-CPU/APIC release, underflow, and guarded dispatch fail closed. |
| bootstrap-activation-handoff | `formal/bootstrap-activation-handoff/BootstrapActivationHandoff.tla`; `kernel/ps/src/multitask/{scheduler,spawn}.rs` | Supervisor commit appends the exact child slot to a fixed `MAX_TASK` FIFO. A repeated activation is deduplicated, later activation and IPC hints cannot overwrite older work, retirement removes only the exact stale slot, and an intervening strict-System recovery or fair-share turn leaves the FIFO intact. The scheduler dispatches the oldest remaining child first; capacity contradiction panics instead of dropping activation authority. |
| atomic-process-activation-batch | `formal/atomic-process-activation-batch/AtomicProcessActivationBatch.tla`; `libs/rustos-user-abi/src/syscall/activation_batch.rs`; `services/initd/src/{activation.rs,main.rs}`; `services/loaderd/src/main.rs`; `kernel/compat/src/user/syscall/linux/proc_broker_ops.rs`; `kernel/compat/src/user/syscall/linux/proc_broker_ops/activation_batch.rs`; `kernel/ps/src/multitask/scheduler.rs`; `kernel/ps/src/multitask/scheduler/{activation_batch_tests.rs,handoffs.rs}` | Initd constructs a bounded unique zero-tailed sibling cohort only after exact rootd lease reporting. Loaderd binds the requester to the stamped sender. Ring0 snapshots every target PID/process-generation/MM-generation before acquiring ProcBrokerRegistry, rechecks those full identities under the final lock, then acquires Scheduler, consumes every one-shot capability while every target remains suspended, checks that the callback preserved that state, and publishes every runnable member before releasing the critical section. PID-equal target replacement is rejected without consuming or publishing any cohort member. The model exposes the consumption-to-publication interior only as a lock-held verification state, so requester exit, dispatch, and reply cannot interleave it. A bounded atomic-cohort counter drains exactly that FIFO prefix before the synchronous loader reply chain; later single spawns cannot extend it. Validation failure preserves all members; a post-preflight mismatch panics rather than exposing partial startup. |
| cpu-affinity-observation | `formal/cpu-affinity-observation/CpuAffinityObservation.tla`; `kernel/hal/src/arch/smp.rs`; `kernel/compat/src/user/syscall/{linux/syscalld_ops.rs,windows/dispatch.rs}`; `services/syscalld/src/{affinity_policy,linux_policy,win32_policy}.rs`; `libs/rustos-user-abi/src/{syscall/affinity.rs,windows.rs}`; `compat/windows/services/winsys/{common/win_types.h,ntdll/syscall.c,kernelbase/exports.c}` | HAL snapshots the exact dense Online logical set and panics on partial post-admission topology. Kernel-compat stamps version, bitmap, popcount, exact target-process owner, and effective task mask into the trusted service request. Syscalld accepts only an exact nonempty bounded observation. Linux receives the effective task bitmap rather than fabricated CPU zero or the whole topology. Windows `SystemBasicInformation` publishes only the count and zeroes every Microsoft-reserved byte; kernelbase derives `SYSTEM_INFO.dwActiveProcessorMask` solely from the admitted dense single-group count. |
| task-affinity-lifecycle | `formal/task-affinity-lifecycle/TaskAffinityLifecycle.tla`; `kernel/ps/src/multitask/scheduler/affinity.rs`; `kernel/compat/src/user/syscall/{linux/syscalld_ops.rs,windows/dispatch.rs}`; `services/syscalld/src/{affinity_policy,linux_policy,win32_policy}.rs`; `compat/windows/services/winsys/{ntdll/syscall.c,kernelbase/exports.c}` | Linux resolves only an exact same-process TID, intersects the requested mask with Online, admits policy before a second exact scheduler resolution, and returns the effective task mask. Windows query/set operations accept only documented current-process/current-thread pseudo handles in the fixed single-group envelope. Process mutation updates every live thread under one scheduler lock; thread mutation remains inside the process mask and returns the previous mask. Excluding a running CPU publishes a mandatory reschedule edge, dispatch outside the committed mask panics, fork/clone inherits, exec preserves, and current-processor observation is Online-bounded. TLC passed the finite lifecycle after exposing and fixing both a parse defect and an invalid child-mask invariant in the initial model. |
| post-init-bootstrap-barrier | `formal/post-init-bootstrap-barrier/PostInitBootstrapBarrier.tla`; `services/initd/src/main.rs`; `services/rootd/src/main.rs` | initd may overlap exact rootd-leased foundation-service initialization, but activation alone never satisfies a dependency. Each netd/devmgrd/inputd child must publish the endpoint owned by its exact recorded PID, and initd must admit the complete live set before runtimed or storaged. Foreign, stale, exited, partial, or timed-out endpoint state fails closed. |
| tlb-shootdown-lifecycle | `formal/tlb-shootdown-lifecycle/TlbShootdownLifecycle.tla`; `kernel/hal/src/arch/tlb_shootdown.rs`; `kernel/mm/src/memory/{address_space,kernel_vm}.rs`; `kernel/mm/src/memory/address_space/rollback.rs`; `kernel/ps/src/multitask/{scheduler,process_table}.rs` | Address-space mutations capture targets after the edit from each CPU's release-published active root; a changed activation writes CR3 before publishing, so an observed different root has already discarded translations and any later activation back performs another CR3 write. Global mappings and retirement still target every eligible CPU. Retirement requires zero task/slot/process references and no active CPU, changes the root to Retiring, and forbids reference acquisition or activation through Reclaimed. A mutation publishes one exact generation/root mailbox per captured target, flushes locally and through the private fixed vector, and retries notification while reclaim remains quarantined. The boot-static product chooses fail-stop only at the two-second liveness watchdog; no timeout permits reclaim. |
| cross-cpu-task-retirement | `formal/cross-cpu-task-retirement/CrossCpuTaskRetirement.tla`; `kernel/ps/src/multitask/{current,process_table,scheduler.rs}`; `kernel/ps/src/multitask/scheduler/smp.rs`; `kernel/hal/src/arch/tlb_shootdown.rs`; `kernel/mm/src/memory/address_space.rs` | Process exec first seals thread attachment. Remote running siblings are quarantined and rescheduled before detach; an externally targeted thread uses a separate no-dispatch quiesce bit so its process binding survives until commit. Replacement requires one remaining thread and no remote target owner. Address-space drop scans every shootdown-eligible CPU and panics if the old root remains active. |
| thread-exit-futex-cleanup | `formal/robust-futex-owner-death/RobustFutexOwnerDeath.tla`; `kernel/mm/src/memory/address_space/atomic_user.rs`; `kernel/compat/src/user/syscall/linux/service_ops/futex_thread.rs` | The retained process state prevents mapping mutation while kernel-mm resolves an aligned writable atomic u32. Robust owner death uses acquire load and at most 64 AcqRel compare-exchange attempts, revalidates every observed race, preserves FUTEX_WAITERS, and wakes only after success. clear_child_tid uses a release atomic zero store before wake. |
| per-cpu-clockevent-lifecycle | `formal/per-cpu-clockevent-lifecycle/PerCpuClockeventLifecycle.tla`; `kernel/hal/src/arch/{clock,msi,timer}.rs`; `kernel/hal/src/arch/idt/mod.rs`; `kernel/ps/src/multitask/{spawn,irq,cpu_local}.rs` | AP admission requires local xAPIC, invariant-TSC calibration, and architectural TSC-deadline support. The private vector is masked until its first future deadline is stored, then armed before Online. Interrupt entry catches missed intervals up to a strictly future deadline before scheduling, never programs the BSP PIT, and completes with local APIC EOI. After deadline service, a single-user or exact Idle continuation bypasses the global catalog only when CPU-local queue/mailbox, foreign runnable load, deferred work, and user-return requests are all empty; any publication disables the fast path. The same gate covers BSP RTC and PIT/LAPIC periodic edges. |
| persistent-mutation-admission | `services/vfsd/src/{lib.rs,main.rs}` | Matched for the current read-only persistent topology: the service dispatch calls one testable policy library that returns `EROFS` for write/truncate and for persistent create/unlink, while narrow `/run` policy never mutates the boot volume. The model's `WritableFeatureEnabled` is explicitly false. Journal, replay, atomic rename, fsync ordering, and power-loss evidence remain failed gates before that constant and source policy may change. |
| dma-iommu-isolation | `tools/hostd/src/{main.rs,runtime.rs}`; `libs/driver-domain-host/src/lib.rs` | Source contract matched for L0 ownership: every physical DVM launch requires `/dev/iommu`, one non-identity QEMU IOMMUFD object, an exact complete VFIO group, bounded assigned apertures, and fail-closed reset/revoke/restore. RustOS ring0 has no physical-storage DMA allocator or controller backend. Target IOMMU fault, post-revoke DMA, and reset captures remain required hardware evidence. |
| filesystem-content-integrity | `tools/xtask/src/stage/mod.rs`; `kernel/io-manager/src/storage/boot_volume.rs` | Matched for immutable bootstrap content: the GRUB-authenticated early-system table binds exact paths, ranges, and SHA-256 digests; duplicate, disordered, overlapping, out-of-range, and digest-mismatched entries fail before returning bytes. |
| network-payload-session | `libs/driver-domain-protocol/src/lib.rs`; `kernel/io-manager/src/io/dvm_network.rs`; `tools/xtask/src/kvm.rs` | Matched at the fixed DVM Ethernet boundary: only bounded ARP or checksum-valid, nonfragmented IPv4 passes; malformed frames are consumed and dropped so they cannot poison the ring head. The current composite KVM run reaches DVM network readiness but cannot start netprobe because the required direct-scanout UI provider fails closed; a fresh bidirectional capture remains required. Saturation, cancellation, backpressure, and physical-NIC behavior remain outside this transport proof. |
| scheduler-active-balance | `formal/scheduler-active-balance/SchedulerActiveBalance.tla`; `kernel/ps/src/multitask/scheduler/runqueue_policy.rs` | The source CPU increments a private relaxed counter only after observing at least two published runnable continuations. Residue zero is due before the first loaded opportunity and again after each eight loaded opportunities; RTC/time residue has no role. A due scan transfers at most one runnable queued `Local(source)` continuation only if it has neither current/execution nor transition custody and the exact selected target is inside affinity. The source-owner CAS removes the source queue owner before publishing one matching target mailbox/request; only target drain produces target-local custody and only that drained custody dispatches. The model uses explicit continuously-loaded/eligible action fairness for eventual target execution; it does not claim a wall-clock balancing deadline or verify least-loaded target selection. |
| scheduler-cpu-distribution | `kernel/ps/src/multitask/scheduler.rs` CPU-owned System/handoff bursts, virtual timeline, bounded latency/synchronous/activation FIFOs, strict-System ready age, and weighted-vruntime accounting; `kernel/ps/src/multitask/scheduler/{locality.rs,runtime_profile.rs,runqueue.rs,runqueue_policy.rs,smp.rs}` bounded last-CPU selection, fixed attribution, exact queue transfer, and distinct local-dispatch/source-migration predicates; `kernel/ps/src/multitask/cpu_local.rs`; `services/runtimed/src/spawn.rs` | A local claim is admitted from one owner snapshot only when it is `Local(exact_cpu) && runnable`, and rejection precedes queue or CAS mutation. Hints and legacy context flags cannot create dispatch authority. Two System dispatches reserve the next ordinary User turn on that logical CPU only, and that User is selected by least charged local virtual runtime or an exact last-CPU candidate no more than one minimum-granularity unit behind the same-class minimum. Handoff and first-turn authority is placed in the target runqueue owner's CPU policy so one CPU cannot consume another CPU's class budget or FIFO. An idle CPU with no eligible local continuation pulls one affinity-valid peer only after a distinct source predicate proves `Local(source) && runnable`, no execution owner, target affinity, handoff admission, and a valid frame; the existing owner CAS then publishes the target mailbox. Source active balance is separately modeled as per-source first/each-eight loaded opportunity cadence rather than a timer tick; neither path holds two runqueue locks. Process-owned IPC server boost selects the associated runnable worker across all CPU owners, publishes its target-CPU handoff, and issues one directed reschedule instead of scanning only the caller CPU. Repeated rehome replaces an older record for the same slot, so stale generations cannot consume unbounded mailbox capacity. User ready age does not override weight or vruntime; the 2 ms absolute recovery rail applies only to admitted System work. One destructive one-second profile separates same-task decisions, real task switches, address-space switches, exact-task-id-keyed CPU migrations, and aggregate/max catalog-lock wait/hold time without making profiler state scheduler authority. Current functional evidence passes the same 1/2/4/8-vCPU control build; UI 55 FPS remains a separate acceptance gate. |
| dvm-volume-io | `libs/rustos-user-abi/src/syscall.rs`; `libs/storage-fat/src/lib.rs`; `services/vfsd/src/{lib.rs,block.rs}`; `services/storaged/src/block.rs`; `kernel/compat/src/user/syscall/linux/block_broker_ops.rs`; `kernel/io-manager/src/io/dvm_block.rs`; `libs/driver-domain-protocol/src/lib.rs` | Before mounting, vfsd binds provider geometry to the exact response generation and capacity, accepts only supported 512--4096-byte FAT logical sectors, and requires `info.flags == expected_flags`. The expected private-volume authority is exactly `READ_ONLY`: writable flags=0 and composite flags=3 fail alongside foreign geometry, reserved fields, zero capacity, and overflowing byte length. An allowed-bit mask is not authority equality. `storage-fat` snapshots that admitted geometry before allocating scratch space; malformed boot sectors fail without mounting. Exact non-empty logical-block ranges are then converted with checked sector arithmetic. Control/write IPC stays within the generic 4-KiB payload; reads use a dedicated exact 64-KiB response with an 80-byte header and 65,456-byte capacity, and vfsd admits only a block-aligned chunk whose complete request header, generation, LBA, block count, reserved field, and payload length match exactly. The bulk op reuses read authority. Each kernel transfer slot remains 64 KiB and every request binds the live generation. The complete ring/slot footprint fits inside one explicit 8-MiB power-of-two PCI BAR; its unused tail is reserved. Timeout cancels without early slot reuse; stale/revoked generations stay retryable errors rather than cached namespace negatives. Configured read, mutation, and flush faults fire only after live signed-generation admission and before any request ID, slot, pending record, producer cursor, or doorbell publication; the executable source test and model require that failure to leave request authority untouched. |
| dvm-read-cache | `formal/dvm-read-cache/DvmReadCache.tla`; `services/storaged/src/block.rs` | Storaged retains at most eight validated 64-KiB read-ahead windows (512 KiB total). Hits require the exact generation and a wholly covered block range; overlapping replacements cannot coexist. A first or random miss submits one window; only an exact preceding-window-boundary miss may publish up to eight contiguous tickets before waiting. Cache publication is atomic after the complete batch; partial submission is cancelled and completion failure cancels the remainder and clears the epoch. Inserting another generation atomically drops the old epoch, and every write attempt clears the cache before submission. Restart/generation mismatch and transport-info failure also clear it, so stale bytes cannot be served after mutation or revoke. |
| remote-file-mapping | `formal/remote-file-mapping/RemoteFileMapping.tla`; `libs/rustos-user-abi/src/syscall.rs`; `services/vfsd/src/{early_system.rs,main.rs}`; `kernel/compat/src/user/syscall/linux/{offload_ops.rs,proc_broker_ops.rs}` | VFS IPC v4 uses one exact 64-KiB inline response with a 40-byte header and 65,496-byte payload. Remote mapping reads are capped to that versioned payload; early-system ownership is resolved before its 4-KiB broker chunking, and an owned entry that disappears cannot fall through to the mutable DVM volume. Kernel commit rejects every short copy rather than preserving a zero-filled executable tail. |
| syscall-simd-lifecycle | `formal/syscall-simd-lifecycle/SyscallSimdLifecycle.tla`; `kernel/ps/src/multitask/{scheduler.rs,spawn.rs}`; `kernel/compat/src/user/syscall/mod.rs` | Syscall entry saves all sixteen XMM registers in assembly before the first ordinary Rust instruction, then captures the complete user SIMD/FPU image into a per-task slot distinct from the scheduler continuation image. This pre-Rust prefix is mandatory: a compiler prologue may otherwise consume XMM scratch before the Rust capture call. The exact owner syscall frame and entry XMM prefix remain live while blocking publishes one scheduler frame; dispatch consumes only that scheduler frame. Restore requires the exact entering task and one active snapshot, clears that activity, and cannot be reused by a nested or foreign return; the assembly suffix finally restores the entering XMM image after all Rust code. The SYSRET canonical-address and RFLAGS contract is revalidated after the last interruptible tail reschedule, so a pre-schedule decision cannot authorize a later return. |
| syscall-scheduler-continuation | `formal/syscall-scheduler-continuation/SyscallSchedulerContinuation.tla`; `kernel/ps/src/multitask/{scheduler.rs,irq.rs}`; `kernel/compat/src/user/syscall/mod.rs` | Composite source/model match: syscall entry owns one live frame while the running scheduler continuation is consumed. Arm does not republish old stack bytes. A raced wake invalidates only the arm token; successful commit and CPU transfer publish one frame atomically. Blocked wake retains that frame, exact-owner dispatch consumes it, and only a post-resume SYSRET check can return. Retirement removes both authorities. This composition closes the gap that allowed wake validation to inspect an already-consumed running frame and quarantine the live UI task. |
| pci-bar-discovery | `formal/pci-bar-discovery/PciBarDiscovery.tla`; `kernel/hal/src/arch/pci.rs` | Standard BAR sizing disables command decode, restores the low dword before probing a 64-bit high partner, restores both halves before publication, and computes size from the least significant implemented mask bit. This accepts QEMU ivshmem's valid 8-MiB 64-bit BAR when upper mask bits are unimplemented without weakening exact resource-size admission. |
| runtime-control-rpc | `libs/runtime-control/src/lib.rs` `response_payload_len`; Kani `verification::*` harnesses; Verus `formal/verus-proof-kernel/runtime_response.rs` | Fixed during this audit: successful replies now require the exact request opcode and payload shape; only a payload-carrying op (`OP_SNAPSHOT_RUNNING_PROGRAMS`, `OP_WATCH_RUNNING_PROGRAMS`) may carry a bounded payload. A watch is a snapshot the server may hold before answering; parking decides when the reply arrives and never widens what it may contain, so both admit a reply under one rule. Positive or `i32::MIN` status values fail closed, so a malformed peer cannot turn integer negation into a panic or a false success. |
| rootd-bootstrap | `services/rootd/src/main.rs` lease/readiness/restart path; `services/loaderd/src/main.rs` spawn/activate path | Fixed during this audit: bootstrap admission and lease-led restart remain service-owned. Each live core lease has twenty supervised 250 ms readiness intervals; absent concurrent restart backoff this is five seconds, while the finite restart budget keeps the combined bootstrap path bounded. A process that remains alive without publishing its exact endpoint is retired and re-enters that restart budget instead of stalling bootstrap forever. The model timeout clears process, lease, endpoint, and capability authority before another issued PID may start. |
| service-bootstrap-lifecycle | `services/rootd/src/main.rs` raw entry, worker handoff, and manifest-derived dependency policy; `kernel/compat/src/user/syscall/linux.rs` process-exit classification; `kernel/ps/src/multitask/scheduler.rs` final-process authority cleanup; `services/initd/src/main.rs` readiness classification | Fixed and mapped: rootd enters ordinary Rust only through a raw stack-aligning trampoline, a non-final helper exit cannot be invented from missing process state or revoke process-owned authority, and initd receives lookup authority for exactly the core dependencies declared in the bootstrap manifest. An unpublished endpoint is the only retryable lookup result; permission, protocol, and invalid-service failures terminate initd instead of becoming an infinite not-ready loop. A bounded 30-second parallel KVM run reached the authenticated DVM/input/display readiness terminal after these corrections. |
| endpoint-registry | `kernel/compat/src/user/syscall/linux/ipc_ops.rs` registration/revoke/cleanup/wait; `kernel/ps/src/multitask/process_table.rs` exit marker | Fixed during this audit: the shared mutation lock plus exit-marker recheck makes publication fail closed. Exact-PID wait now queries the process table using the ABI's process PID instead of treating that value as the initial task TID, so a surviving multithreaded service is not reported as exited merely because its leader thread retired. |
| endpoint-receiver-wakeup | `kernel/ipc-runtime/src/ipc/mod.rs` endpoint receive/waiter queue; `kernel/compat/src/user/syscall/linux/ipc_ops.rs` blocking receive loops; `kernel/ps/src/multitask/scheduler.rs` arm/commit/wake | Fixed and mapped: pending-message observation and waiter publication share the endpoint slot. A message already pending at registration returns the re-poll result without retaining a waiter, so a later producer cannot consume stale wake or synchronous-handoff authority for a receiver that never blocked. |
| endpoint-publication | `ipc_ops.rs` `SERVICE_ENDPOINT_REGISTRY_MUTATION`; process-table exit state | Fixed during this audit: registration, revoke, exit cleanup, capability reads, endpoint lookup, and expected-owner probes share one registry critical section. A reader therefore observes one endpoint/owner/capability generation instead of combining independently loaded fields across replacement; endpoint publication remains the commit point and exiting owners lose lookup/capability authority. |
| exception-retirement-lifecycle | `kernel/hal/src/arch/{gdt.rs,idt/handlers.rs,idt/mod.rs}`; `kernel/executive/src/lib.rs`; `kernel/compat/src/user/syscall/mod.rs`; `kernel/ps/src/multitask/scheduler.rs` | Fixed and mapped: generated error-code interrupt wrappers cross an explicit 16-byte SysV call-site alignment bridge before ordinary Rust cleanup. User #GP/#PF diagnostics retain scalar vector/error/RIP/RSP only and never dereference the attacker-controlled saved RSP. NMI owns a distinct per-CPU IST and is a lock-free emergency-marker leaf even when it interrupts an arbitrary tracked lock. Recoverable faults preserve authority, non-final thread retirement clears task-local wait/reply state only, and final retirement revokes process-owned endpoint authority. Whole-process termination publishes process-exiting before retiring its first thread. Scheduler validation examines only immutable published runnable frames, never a live current kernel frame. An invalid frame found from hardware IRQ is made undispatchable in its fixed slot and finalized by housekeeping, so IPC/process teardown never allocates or takes cross-subsystem locks in IRQ context. |
| deferred-start | `services/initd/src/main.rs`; `services/runtimed/src/spawn.rs`; rootd readiness report and loader activation | Fixed during this audit: service and catalog child creation carries `LOADER_SPAWN_FLAG_DEFER_START`; runtimed records ordinary app ownership before activation, while endpoint services complete lease admission first. Activation is the single-use scheduler handoff commit point. In both initd and runtimed, lease-report, activation, duplicate-PID, and endpoint-wait failure require exact child retirement (`ESRCH` means already retired); another cleanup error stops the supervisor instead of silently removing ownership and continuing retries. The model explicitly separates successful cleanup from this terminal uncertain-cleanup state. |
| post-init-leases | `services/rootd/src/main.rs` sender-stamped readiness/capability/lookup dispatch; `kernel/compat/src/user/syscall/linux/proc_broker_ops.rs` deferred-spawn proof | Fixed and strengthened during this audit: initd owns the ordinary post-init reports while the live sessiond/runtimed service owns uiserver admission. Every report must name the exact declared executable and prove that ring0 still holds the unconsumed `(target_pid, reporter_pid)` deferred-spawn authority; a caller-selected live PID is not a lease candidate. Report authority is then bound to the supervisor sender and exact PID lease. A live lease rejects a different `reporter_pid` without changing PID, reporter, or capability authority. Reporter exit revokes the complete bounded descendant closure in the same rootd turn, and both capability and lookup admission walk the reporter chain to a live core lease instead of trusting only the immediate child state. |
| rootd-restart-backoff | `services/rootd/src/main.rs` `spawn_initd_via_loaderd`, lifecycle drain, and `restart_failed_leases`; `services/{procd,syscalld}/src`; `kernel/compat/src/user/syscall/linux/{lifecycle_broker_ops.rs,offload_ops.rs}` | Fixed during this audit: observed core-service exit enters `RESTART_PENDING` before retry, failed spawn/activation reuses that bounded delay, and only rootd may invoke the 1s-capped timer substrate. The steady-state supervisor now uses the endpoint bounded message-or-timeout receive with the same 10 ms public budget; the old timer-only idle helper is removed and the nonblocking drain remains separate. The model now makes external monotonic progress strongly fair across unrelated lifecycle churn and requires every pending restart to settle or fail the supervisor closed instead of relying on a deadline safety invariant alone. Initial initd activation and deferred restart activation both terminate the exact suspended PID before rootd may stop or retry; uncertain cleanup fails the supervisor closed instead of accumulating hidden children. Rootd, procd, and syscalld receive independent bounded fan-out queues. Procd clears signal policy locally; syscalld clears credential/MM policy before its next request; neither performs cross-service discovery during drain, so queued recovery evidence cannot recreate the bootstrap authorization cycle. Rootd overflow stays sticky and terminal, while each policy overflow clears its owning cache and rebases only its private queue. The retired procd-to-syscalld exit opcode no longer exposes PID-shaped cleanup through ordinary service IPC. Every drain validates its exact version and reserved envelope. A single-consumer snapshot releases the queue lock before user-memory access, consumes only after both outputs commit, preserves concurrent appends, and consumes nothing on copyout fault. |
| post-init-supervisor-recovery | `services/rootd/src/main.rs` post-init lease query/reclaim and lifecycle drain; `services/initd/src/main.rs` reconciliation | Fixed during this audit: normal initd exit now revokes the complete post-init reporter closure in the same rootd turn. The defensive reconciliation path adopts an imported surviving service only after its exact-PID endpoint is ready; an endpoint-pending admitted lease has one 30-second bounded window before authenticated rootd reclaim performs full teardown. Sessiond exit immediately revokes and terminates its reported uiserver child, and capability/lookup admission revalidates the complete live reporter chain. The model previously described deadline settlement without a temporal property; it now treats monotonic clock progress as strongly fair across repeated sibling failures and requires every recovery instance to eventually adopt or reclaim. |
| dvm-control-relay | `libs/driver-domain-host/src/lib.rs` challenge/proof/WELCOME and `new_input_epoch`; `driver-domains/linux/package/rustos-dvm-agent/src/rustos-dvm-agent.c` fw_cfg HMAC proof; `services/inputd/src/dvm_protocol.rs` RDI ingress | Matched: CID and static HELLO do not authorize the relay. L0 requires a fresh per-launch HMAC-SHA256 proof over the exact HELLO before WELCOME, probe, or input authority; the agent reads the secret only from fw_cfg's root-only raw attribute. Relay epochs are monotonic for a hostd lifetime and fail closed before reuse on wrap. |
| dvm-control-endpoint | `ControlSecret::control_port`, hostd/xtask listener binding, DVM `control_port_from_secret` | Fixed during this audit: the control port is no longer static. L0 and the root-only agent derive the same private endpoint from the per-launch secret, so an ordinary same-CID process cannot reserve the listener setup slot; successful connection still requires the fresh HMAC proof. |
| dvm-agent-readiness | `rustos-dvm-agent.c` `open_ready_directory`/`publish_ready`/`local_health`/`serve` | Fixed during this audit: a static readable file is no longer local health. The initialized serving process holds a singleton lock and a lock on a complete candidate inode, atomically renames that inode to `ready`, and retains both descriptors for its lifetime. Health validates the owner-private directory, exact regular-file metadata and payload, then succeeds only while the ready inode remains locked. Exit, SIGKILL, and the RT hard limit release the lock, so stale contents fail closed; the diagnostic `announce` command cannot publish readiness. One fixed candidate name bounds pre-rename crash residue. TLC checked 1,461 distinct states including stale/malformed/symlink state, partial publication, restart, crash, and announcement-only paths without error. |
| dvm-network-ring | `kernel/io-manager/src/io/dvm_network.rs`; `driver-domains/linux/package/rustos-dvm-net/src/rustos-dvm-net.c`; `libs/driver-domain-protocol/src/lib.rs` | Matched: the host-created header fixes region/slot/MTU bounds at install. RustOS neither dereferences guest descriptors nor trusts an unbounded counter; invalid shared state returns `Invalid` without advancing its producer/consumer cursor. |
| dvm-network-control | `libs/driver-domain-host/src/lib.rs` RDI session lifecycle; `services/inputd/src/dvm_protocol.rs`; `services/netd/src/main.rs`; `kernel/io-manager/src/io/dvm_network.rs` | Matched: mapping a valid ivshmem aperture does not report a live network by itself. Inputd validates the session and notifies netd through an exact 16 ms bounded call; netd validates the live inputd owner and owns exact generation grant/revoke. A transient pre-publication failure retains the decoded input batch and exact unacknowledged revoke/grant suffix outside the policy lock, retries for at most five seconds, and publishes no event before ACK. Each successful transition is cleared individually, preventing stale replay after a later suffix failure; timeout exits fail-closed. Ring0 enforces only the generic lease and bounded ring. Stale cleanup and DVM-writable data-plane state cannot revoke or create a newer lease. |
| dvm-input-revocation | `libs/driver-domain-host/src/lib.rs` epoch allocation; `libs/driver-domain-protocol/src/lib.rs` shared `RustosInputFrame` plus Kani harnesses; `kernel/io-manager/src/input/{dvm_ring.rs,wait_queue.rs}`; `services/inputd/src/{dvm_protocol.rs,main.rs}`; `drivers/libs/keyboard-core/src/lib.rs` | Matched: every session start/end or transport revoke reaches inputd as a priority reset barrier, and keyboard reset emits provider-owned releases before state is discarded. L0 never reuses a relay epoch within a hostd lifetime, so a retired sequence cannot restart after a later barrier. |
| dvm-input-ring | `libs/driver-domain-protocol/src/lib.rs` `DvmInputRingHeader`; `libs/driver-domain-host/src/{lib.rs,ivshmem.rs}` `InputRingSink`/`IvshmemInputProducer`; `tools/xtask/src/kvm.rs`; `kernel/io-manager/src/input/{dvm_ring.rs,wait_queue.rs}`; `services/inputd/src/dvm_protocol.rs`; `kernel/compat/src/user/syscall/linux/input_broker_ops.rs` | The 128 KiB backing holds exactly 2,048 32-byte slots with cache-line-separated cursors. L0 writes a complete checked record, release-fences it, advances only producer, then signals one MSI-X eventfd. RustOS publishes transport readiness after exact mapping/vector validation and policy-consumer readiness only after inputd arms the sole kernel waiter. Because capability authority is process-wide, an AcqRel single-flight claim admits exactly one broker caller to cursor/reset mutation; losers consume nothing. Ring0 copies fixed generation-stamped bytes only; inputd consumes malformed/stale records as rejected. Revoke clears claims, publishes a reset barrier, unmaps the aperture, and recovery may reuse only the same pinned PCI aperture and permanent vector. |
| dvm-block-transport | `libs/driver-domain-protocol/src/lib.rs`; `kernel/io-manager/src/io/dvm_block.rs`; `kernel/compat/src/user/syscall/linux/block_broker_ops.rs`; `services/storaged/src/{block.rs,main.rs}`; `services/vfsd/src/block.rs`; `driver-domains/linux/{board/linux.fragment,board/overlay/etc/init.d/S12rustos-dvm-block,scripts/verify-kernel-config.sh,package/rustos-dvm-block/src}`; `tools/xtask/src/kvm/{guest.rs,layout.rs,layout/block_transport.rs}` | Integrated ABI v2 fixes the aperture, queue depth, transfer slots, feature bits, operation numbers, address-free records, L0-signed launch generation, request identity, range/alignment bounds, exact `READ_ONLY` authority, naturally aligned atomic cursors, and Release/Acquire publication. Before aperture creation and signing, xtask copies the private runtime snapshot, sets mode 0600, calls `sync_all` on the file, then calls `sync_all` on its containing directory. The QEMU backend is `readonly=on`; it uses `ide-cd` on existing q35 AHCI `ide.0` because `ide-hd` rejects immutable media, and it supplies no invalid logical/physical block-size property. The kernel fragment and verifier require exact built-in `CONFIG_BLK_DEV_SR=y`; S12 relies on built-in `sr` for immutable ATAPI and loads signed modular `sd_mod` for writable AHCI. Disk creation/rotation requires 2048-byte media alignment; the signed header and host readiness check require logical/physical 2048/2048 while capacity remains counted in 512-byte protocol sectors, matching guest ATAPI read-capacity. This read-only media advertises exactly `DVM_BLOCK_FEATURE_FLUSH`, never FUA. Ring0 admits one exact aperture and wake-only MSI-X vector; its finite broker wait uses check-arm-recheck and a deadline. Storaged owns generation binding, timeout/cancel, and a startup media-barrier probe whose exact marker names `vfs-policy->block-broker->shared-ring->linux-dvm->media-barrier`; this proves liveness through the data plane, not backing-image write durability. On DVM restart xtask captures the exact successor generation returned by `rotate_dvm_block_epoch`, passes it into recovery wait, and requires both rebound log evidence and a header with RustOS/DVM/READ_ONLY flags plus that exact generation; predecessor generation or missing READ_ONLY fails. Rebuilt-DVM build/verification and the bounded storage-only KVM profile passed, with storage boot trace, first read completion, and the media-barrier terminal at 2259 ms independently of GPU/UI markers. Physical NVMe/AHCI power-loss, reset, and revoke capture remains required target evidence. |
| trusted-ui-boundary | `kernel/io-manager/src/io/dvm_display.rs`, `io/gui.rs`; `libs/rustos-user-abi/src/{device,syscall}.rs`; `services/uiserver/src/sys.rs` | Fixed during this audit: DVM scanout provenance now crosses the driver registration and display-info ABI, while uiserver exposes a fail-closed trusted-UI status. No existing scanout or input provider can clear the required independent-attestation blockers. |
| input-readiness | `kernel/io-manager/src/input/{wait_queue.rs,dvm_ring.rs}`; `kernel/compat/src/user/syscall/linux/{ipc_ops.rs,service_ops/poll_epoll.rs,service_ops/ipc_helpers.rs}`; `services/inputd/src/{dvm_protocol.rs,main.rs}`; `services/uiserver/src/{input_loop.rs,sys.rs}` | Updated during this audit: a generic caller cannot drain the DVM ring; the MSI-X-woken inputd worker alone owns event-driven transport progress and moves decoded records into its policy queue. Finite poll rechecks use bounded `STATS`, while the generic wait-set observes inputd's monotonic readiness generation. The dedicated uiserver reader retains its bounded, non-consuming recheck before an authorized read. |
| userspace-wait-set | `formal/userspace-wait-set/{UserspaceWaitSet,WaitSetRegistry}.tla`; `libs/rustos-user-abi/src/{syscall.rs,performance.rs}`; `kernel/compat/src/user/syscall/linux/{waitset_broker_ops.rs,ipc_ops.rs,proc_broker_ops.rs,lifecycle_broker_ops.rs,service_ops/{ipc_helpers.rs,poll_epoll.rs,vfs_meta.rs,vfs_socket.rs}}`; `kernel/ps/src/user/{epoll.rs,handles/{table.rs,handles.rs}}`; `services/{vfsd,netd,inputd,runtimed}/src`; `services/uiserver/src/{main.rs,input_loop.rs,wayland.rs}` | Source/model matched for the generic wait boundary: provider-owned monotonic generations and exact endpoint epochs feed a bounded ring0 waiter registry; persistent epoll and transient multi-fd poll both use check-arm-recheck and every returned event is re-queried from its service owner. The registry refinement separately caps live epoll objects and per-object interests, derives the global product bound, preserves retired-object emptiness, and publishes a disjoint checkpoint batch only after complete validation; rejected duplicate or over-capacity restore preserves exact prior membership. ppoll/epoll_pwait apply a temporary checked signal mask and unmasked delivery cancels with `EINTR`. vfsd keys registration by the target fd plus stable provider object identity and stores the observed epoch as mutable state, so explicit MOD after restart rebinds one interest while ADD remains `EEXIST` and DEL remains possible. Netd's fixed 1 ms provider worker processes at most 32 DVM/smoltcp ingress packets per turn and publishes only socket-state transitions so external arrival wakes sleepers without an unbounded service turn. Standard descriptors are real table entries with unique console open-description tokens, so close/dup replacement cannot be bypassed by fd-number special cases. A sessiond TTY route is accepted only after compat revalidates the live fd as a console. Sessiond-backed stdin epoll binds its token and exact endpoint epoch; stdout/stderr report writable only for a live session and HUP after revoke; an empty nonblocking read returns `EAGAIN` without retry. Session close also makes stale read/write/TTY/input operations fail with `ENODEV`, so no operation can recreate the retired session behind a HUP token. Console and epoll reference accounting counts only fd-table ownership, not transient syscall snapshots, so a concurrent poll/read cannot suppress final retirement. Dup/fork share each open-description identity; zero cannot be resurrected. Epoll duplication is kernel-local and service-IPC-free, while vfsd receives only create and the durable final-retire tombstone. Non-consuming provider snapshots use the 16 ms readiness rail; persistent create/control/retire/purge mutations use one 100 ms interactive-control rail and retain exact reconciliation ownership after timeout. Close retires the local fd before cleanup. Uiserver merges Wayland-server's aggregate epoll wake with its input wake and rearms only after dispatch. Runtime SMP/UI evidence remains a separate acceptance gate. |

The wait registry now stores the complete bounded `(provider, object_id, generation)` observation set for each armed task. Vfsd separately caps the persistent registry at 256 epoll objects and 512 interests per object, publishes restore only after the full candidate validates, and uses a provider-object reverse index so purge touches only exact bounded memberships. Netd publishes only the mutating socket and its peer, vfsd publishes only affected epoll objects, inputd publishes its two shared access-ABI objects, and sessiond publishes the exact session. Provider restart remains the only provider-wide wake. `NoWrongObjectWake`, the exact reverse-index implementation mutant, and cap/restore mutants make cross-object removal or partial over-capacity publication a failing transition rather than an unmeasured fanout caveat.
| service-mutation-recovery | `formal/service-mutation-recovery/ServiceMutationRecovery.tla`; `libs/rustos-user-abi/src/{syscall.rs,performance.rs}`; `services/rootd/src/{main.rs,service_checkpoint.rs}`; `services/{vfsd,netd}/src/main.rs`; `kernel/compat/src/user/syscall/linux/service_ops/{ipc_helpers.rs,poll_epoll.rs,vfs_socket.rs}` | Source/model matched for exact mutation identity and restart publication order. Rootd authenticates a service checkpoint namespace, enforces monotonic revisions, idempotent exact retries, stale/alias rejection, and atomic parent-child revoke. Vfsd checkpoints epoll ownership/interests before local publication and replays before endpoint publication. Epoll and remote VFS dup/fork/transfer references are kernel-local, so only create/open and final retire/close cross the service boundary. Non-consuming readiness queries retain the complete 16 ms rail, while persistent epoll create/ADD/MOD/DEL/retire/purge and netd socket DUP/CLOSE/ACK mutations own one complete bounded 100 ms interactive-control attempt. A timeout is not synchronously retried: it retains the exact operation for background reconciliation, avoiding several prematurely revoked requests that vfsd or netd would otherwise finish only as rejected late replies. Only an early terminal transport break may retry inside the still-remaining absolute budget. Netd reserves replay space before socket ref mutation, retains results through kernel ACK, and never silently evicts unresolved work. The dedicated nucleus housekeeping task performs at most one VFS or netd recovery transaction per yielded turn with the same bounded 100 ms control deadline; a required ACK is enqueued rather than synchronously chained. No recovery wait is charged to a foreground syscall. Runtime crash-injection remains separately required. |
| vfs-open-description-recovery | `formal/vfs-open-description-recovery/VfsOpenDescriptionRecovery.tla`; `libs/rustos-user-abi/src/syscall.rs`; `libs/runtime-control/src/lib.rs`; `services/rootd/src/{main.rs,service_checkpoint.rs}`; `services/vfsd/src/{lib.rs,main.rs}`; `kernel/compat/src/user/syscall/linux/service_ops/{ipc_helpers.rs,vfs_meta.rs,vfs_socket.rs}` | Source/model matched for ordinary VFS open-description restart continuity. Vfsd stages a parent, exact path chunks, then the final live record before publishing the handle; read/getdents durably prepare a cursor result and compat commits only after copyout or cancels on failure. Immutable runtime configuration is size-bounded and read through explicit offsets, preserving the open-description cursor and avoiding unnecessary durable cursor transitions on the serialized vfsd executor. Final close retains an exact tombstone across lost replies and service restart; only a kernel visibility ACK permits exact-proof compaction. Kernel fd lookup and the temporary service reference increment are atomic against concurrent close. Runtime crash-injection remains separately required. |
| input-ingestion-worker | `formal/input-ingestion-worker/InputIngestionWorker.tla`; `kernel/compat/src/user/syscall/linux/input_broker_ops.rs`; `services/inputd/src/main.rs` `inputd-dvm-ingress` | Fixed during this audit: client poll has no transport-readiness authority. Inputd owns one capability-gated, MSI-X-woken ingestion worker; only its successfully armed waiter publishes policy readiness, it cursor-rechecks before sleep, drains at most 256 records per broker turn, and is the sole consumer authority. ABI v5 retires caller-driven DRAIN/INPUT_INGEST routes, and the worker performs the transport copy outside the policy-queue mutex before a bounded decode/coalesce critical section. A worker-first handoff blocks request threads on a condition variable instead of leaving a higher-class reader runnable in a yield loop; the designated worker therefore acquires the queue and returns fixed-ring credit without priority inversion or reader barging. A 100 ms timer independently wakes the already-armed task for cursor recheck if MSI-X is lost or coalesced, while L0 retries the same generation after each two additional outstanding commits; neither owns data or policy. Cross-service session synchronization releases the queue lock, uses the 100 ms interactive-control value only as a per-call cap, and derives every service call and retry sleep from one five-second monotonic absolute deadline. Child calls receive `min(remaining, cap)` and no phase starts a fresh stopwatch. Owner exit withdraws producer admission, retires the old cursor, and restart must rearm with a checked generation. The model now includes aggregate elapsed time and rejects a final call or sleep that exceeds the remaining budget. |
| ui-frame-budget | `services/uiserver/src/input_loop.rs` `process_pending_input`; `services/uiserver/src/app/input.rs` `handle_input_event`; `services/uiserver/src/app/runtime.rs` `ConsoleCommandDispatcher`; `services/uiserver/src/main.rs` profile loop | Fixed during this audit: uiserver no longer waits for console routing or focus ioctls on its input/present thread. Keyboard and focus commands receive one bounded `try_send` admission decision to a FIFO worker; full/disconnected admission is counted as a visible rejection, while the worker alone may wait on `devmgrd`/`runtimed`. The UI continues to create and present its local redraw update, and the heartbeat exposes pending, rejected, slow, and failed delivery counts. |
| ui-main-loop-wakeup | `formal/ui-main-loop-wakeup/UiMainLoopWakeup.tla`; `services/uiserver/src/{input_loop.rs,main.rs,wayland.rs}`; `kernel/hal/src/{hooks.rs,arch/rtc.rs}`; `kernel/ps/src/multitask/irq.rs` | The UI owns a monotonic readiness generation and uses the thread parker only as a coalescing notification. It checks, arms by observation, rechecks, and always retains an independent bounded deadline. The kernel exports only the atomic block-commit-and-reschedule leaf; HAL snapshots and releases its hook-registry guard before invoking that callback. RTC expiry is a repeatable notification, while only the resumed task's exact disarm acknowledges and releases the timer record. The public commit-only leaf and every split commit/yield wait site were retired. |
| scheduler-thread-demotion | `kernel/ps/src/multitask/scheduler.rs` `demote_current_user_task_to_user_class`; `kernel/compat/src/user/syscall/linux/scheduler_ops.rs`; `services/uiserver/src/{sys.rs,main.rs,input_loop.rs,runtime_sync.rs}`; `services/{loaderd,vfsd}/src/main.rs` | Fixed during this audit: POSIX clone inheritance no longer gives uiserver's background helpers either permanent System-class competition or the compositor's elevated fair share. `SYS_RUSTOS_SCHED_DEMOTE_SELF` clears the base class, caps permanent weight at the nominal user share without raising a lower weight, and preserves a live reply-scoped IPC donation until its normal terminal release. Loaderd and vfsd retain boot-critical class custody until their exact terminal uiserver spawn/snapshot replies succeed, so a still-blocked System caller cannot be stranded by premature demotion; KVM acceptance requires observed completion and demotion. |
| ipc-fast-handoff | `formal/ipc-fast-handoff/IpcFastHandoff.tla`; `kernel/ipc-runtime/src/{api.rs,ipc/mod.rs}`; `kernel/ps/src/multitask/{current.rs,scheduler.rs,scheduler/{runqueue,smp}.rs}`; `kernel/compat/src/user/syscall/linux/{ipc_ops.rs,ipc_fast_metrics.rs}`; `tools/xtask/src/bench.rs`; `services/syscalld/src/fast_offload.rs` | A byte-only request and reply of at most 256 bytes reserve one fixed frame for the exact blocked receiver. The fast commit binds the reply's scheduling-context custody before checking dispatch eligibility, so the check charges the effective donated caller context rather than stale server-native budget state. Same-CPU custody bypasses the ordinary queue; cross-CPU custody publishes one generation-bound run transfer and directed IPI. Every rejected commit restores the waiter, frame, block state, and scheduling-context custody before the slow transaction restarts. Rejection-reason counters are observational only and compile only with the IPC diagnostic profile, where they drain through the existing benchmark wire. The real syscalld identity wrapper admits only exact-sender getuid/gid/euid/egid requests. |
| synchronous-ipc-handoff | `formal/synchronous-ipc-handoff/SynchronousIpcHandoff.tla`; `formal/synchronous-ipc-handoff-concurrency/SynchronousIpcHandoffConcurrency.tla`; `kernel/compat/src/user/syscall/linux/{ipc_ops.rs,ipc_reply_recv.rs}`; `kernel/ps/src/multitask/{current.rs,scheduler.rs,scheduler/sync_handoff.rs}` | Fixed during this audit: terminal reply completion performs donation release, exact caller wake, and `{slot, task_id, owner_generation, target_cpu}` capture under one Scheduler acquisition. Only after that guard returns may `current` submit the opaque token to its captured per-CPU FIFO; publication accepts exact Local or RemoteQueued custody, while selection additionally requires Local custody on that exact CPU. A still-RemoteQueued head is consumed without dispatch, and a stale, migrated, retired, or withdrawn token loses urgency without catalog/global fallback or a second publication. The deep lifecycle model covers generation refresh, no Generic downgrade, and per-CPU fairness precedence; the companion two-task model exhaustively covers coexisting Local/RemoteQueued replies, independent Begin/Finish, same/cross-CPU publication, owner churn, and all-CPU retirement purge without a task-role symmetry assumption. |
| ipc-reply-recv-transaction | `libs/rustos-user-abi/src/syscall/ipc_reply_recv.rs`; `libs/rustos-svc-runtime/src/ipc.rs`; `kernel/compat/src/user/syscall/linux/{ipc_ops.rs,ipc_reply_recv.rs}`; `services/inputd/src/service_loop.rs`; `services/loaderd/src/{lib.rs,main.rs}` | Added from measured software-scheduler-entry dominance: the versioned byte-only ABI validates endpoint ownership and every user range before consuming the old one-shot reply; successful completion wakes and retains the exact caller before reusing the endpoint check-arm-recheck receive. Exact disjoint ranges distinguish pre-commit, tagged post-commit, and fatal invalid results. The native-syscall registry rejects number collisions before dispatch. Inputd and loaderd retry only a proven-live pre-commit cap through the standalone reply path and never replay a completed cap. Both terminate malformed dequeued requests, including a zero-byte loader call. Loaderd selects fusion only when no descriptor cleanup or immediate class demotion is pending; handle-bearing and post-reply-action paths remain separate. |
| wayland-accept-isolation | `services/uiserver/src/wayland_accept.rs` `start_wayland_acceptor`, `WaylandAcceptor::try_recv`; `services/uiserver/src/wayland.rs` `WaylandCompositor::has_pending_protocol_input`, `WaylandCompositor::tick`; `services/uiserver/src/main.rs` `wayland_service_required`; `services/netd/src/main.rs` `handle_accept` | A netd-backed nonblocking `accept4` may still have cross-service RPC latency. Only the bounded accept worker owns that call and it starts only after blocking epoll publishes listener readiness; the UI loop drains at most eight ownership-counted streams per tick through `try_recv`. Empty UI turns do not dispatch client reads without protocol input, server events, or a due callback. The intermediate queue is capped at 16, overload rejects and logs the new client, and accept stalls cannot own or suppress UI frame progress. |
| wayland-frame-pacing | `services/uiserver/src/main.rs` coalesced `main-present` loop; `services/uiserver/src/wayland.rs` `consume_frame_callback_permit`; `tools/xtask/src/kvm/evidence.rs` | Input, runtime, and Wayland damage accumulate into one main presentation rather than a cursor-only early present. The boot frame and each successful real presentation grant one non-accumulating callback permit. A damage-free callback-only commit uses the same monotonic 15 ms cadence as DVM GPU presentation; pending callback work participates in the main sleep deadline, and either route consumes its permit. TLC proves coalescing, permit ownership, and fair eventual progress. Initial topology repacking has a distinct 100 ms pre-profile bound, while every later loop above 50 ms fails. The final bounded QEMU proof matched 57, 65, and 68 commit/callback/release cycles over 3.060 seconds (62.092 FPS aggregate), with a 45 ms maximum gap, and passed the matching uiserver/input/DVM-relay gates. |
| ui-input-motion | `driver-domains/linux/package/rustos-dvm-agent/src/rustos-dvm-agent.c` `input_selftest_emit_cycle`; `services/uiserver/src/{input_loop.rs,main.rs}`; `tools/xtask/src/kvm.rs` | Matched: the KVM-only DVM selftest traces a 192-pixel axis-aligned square instead of the retired center-tremble pattern. Its source is polled at most every 5 ms, safely below the L0 256-frame/s admission ceiling; the end-to-end 60 Hz contract requires three consecutive one-second windows with at least 55 accepted events, 50 presented cursor moves, no drop/slow/error/backlog, at most 50 ms input gap/age, equal logical/presented coordinates, and at least 96 pixels of travel on both axes. The model proves both axes receive visible work and every accepted visible-motion epoch is eventually presented. |
| dvm-input-selftest | `driver-domains/linux/package/rustos-dvm-agent/src/rustos-dvm-agent.c` `input_scheduler_enter`/`input_scheduler_leave`/`input_selftest_start`/`input_selftest_emit_cycle`/`input_selftest_timeout_ms`/`stream_input_devices`; `tools/xtask/src/{build/mod.rs,kvm.rs}` source contract and `--exercise-input` | Matched: the KVM-only composite device advertises `BTN_LEFT` with bounded `ABS_X/ABS_Y`, so the ordinary pointer selector opens its own evdev description and exercises the production absolute path. After authenticated stream admission, the agent requires an initial normal policy, installs and reads back the exact 50/100 ms `RLIMIT_RTTIME`, then enters and reads back SCHED_RR priority 10. Exit restores and reads back both the saved policy/priority and RT limit; any admission rollback or restore mismatch terminates the agent instead of reconnecting. It sends one F12 proof before 6,000 pointer-only position cycles; every pointer cycle consumes one cumulative monotonic 15 ms deadline and missed slots are skipped. This keeps a bounded 90-second source window around the public 60-second proof while exercising a 66.7 Hz source above the 55 FPS acceptance target without making a 100 Hz synthetic producer the scheduler workload. TLC checks 45 distinct states spanning partial admission, successful rollback, restore, fatal restore, and hard-limit termination with no emission outside verified bounded scheduling. |
| dvm-absolute-pointer | `rustos-dvm-agent.c` report staging; `driver-domain-protocol`/`driver-domain-host` position frame; `services/inputd/src/{dvm_protocol.rs,main.rs}`; `services/uiserver/src/app/input.rs` | Fixed during this audit: absolute tablet X/Y remain staging state until one `SYN_REPORT`; the agent publishes only complete changed positions. L0 transports fixed bytes and inputd alone enforces the 1600x900 bound, distinct position kind, surface policy, and duplicate idempotence. TLC checks 74,772 distinct states with no partial publication, duplicate publication, mixed ownership, out-of-bounds cursor, or phantom UI motion. |
| devmgrd-sessiond-isolation | `services/devmgrd/src/main.rs` `serve`, `start_sessiond_ioctl_workers`, `reply_device_ioctl` | Fixed during this audit: sessiond-executed console ioctls are no longer called inline by devmgrd's sole receiver. A four-worker, 32-item bounded FIFO accepts them; saturation replies `EAGAIN` rather than holding unrelated device messages behind a recovering sessiond call. |
| vfio-release-authorization | `tools/hostd/src/main.rs` `verify_release_authorization`; `libs/driver-domain-host/src/lib.rs` `ReleaseAuthorization`, `VfioReleaseBinding`, and VFIO lease transitions | Fixed during this audit: irreversible device binding now requires a pinned-key detached signature, exact artifact and policy digest binding, durable evidence, and a fresh validity-window check before prepare, bind, and active-state persistence. Durable serialization accepts only schema 3 with fleet-policy evidence; schemas 1/2 are rejected, and an unsigned preflight snapshot cannot serialize as a lease. |
| dvm-commercial-lifecycle | `tools/hostd/src/{main.rs,runtime.rs}`; `libs/driver-domain-host/src/lib.rs` group reset/restore and live display/reset/DMA safety | Matched at source level: release inputs, the signature verifier, QEMU, artifacts, policies, and their canonical directory chains must have trusted ownership and reject group/world mutation; SHA-256 is computed in-process. Before any prepared lease or VFIO bind, activation validates the exact display-only policy, production QEMU digest, complete schema-8 bundle, writable `/dev/iommu`, an empty IOMMUFD IOAS allocate/destroy round trip, a soft memlock budget of at least 4 GiB, live sysfs evidence that the assigned group is neither `boot_vga` nor driving a connected DRM connector, and reset-method evidence whose maximum impact scope is contained by the complete lease. `bus`/`cxl_bus` is admitted only when every function on the affected bus is assigned; empty, missing, unknown, or escaping reset state fails closed. Binding additionally requires vfio-pci `disable_idle_d3=Y`; acquire clears and rereads PCI bus-master immediately after bind and reset does so before and after mutation, closing the observed identity-domain DMA window. For AMD, preflight parses the checksummed bounded ACPI VFCT and requires one exact BDF/vendor/device image with valid 0x55aa/ATOM headers and either an exact populated or wholly absent subsystem pair. Supervision rechecks the memlock limit before reset, uses a fixed 2 GiB guest-memory profile, validates the host identity, relocates only its VFCT BDF to fixed guest slot `0000:00:08.0`, recomputes the checksum, verifies the VBIOS bytes unchanged, fsyncs an owner-private full-table snapshot, and supplies it with QEMU `-acpitable` while pinning the VFIO function to that slot. The signed schema-3 device policy additionally binds `amdgpu` `1002:1900` and rejects an excluded storage/network function in the group. After durable prepare, hostd repeats display, reset-scope, DMA-bind, and exact-identity checks immediately before mutation; reset scope and bus-master quiescence are checked again around each hardware reset. QEMU is held behind a pre-exec gate until an owner-private fsync'd runtime record binds PID/start time and release inputs. Launch requires pre-reset plus IOMMUFD; readiness requires authenticated consecutive physical-display evidence. The DVM release selects `acpid` and verifies its fixed power-button `/sbin/poweroff` action. Supervised teardown negotiates a private owner-only QMP connection, requests `system_powerdown`, and requires actual QEMU exit within ten seconds; command acceptance is not evidence. TERM/KILL is bounded fallback, cannot count as successful supervision, and recovery uses it only after exact pidfd binding and a failed QMP/ACPI attempt. Post-stop reset still precedes original-driver restore, and reset failure retains VFIO quarantine. Unit tests exercise QMP ordering and path-injection bounds; TLC checks 155 distinct lifecycle states. Manual IOMMUFD runs proved the relocated VFCT, full amdgpu DRM/ring initialization, and qualitative physical scanout, but supervised graceful-shutdown, DMA-fault/reset/revoke, and the signed five-sample throughput capture remain required evidence. |
| dvm-release-bundle | `driver-domains/linux/scripts/{write-manifest,verify-release-artifacts,stage-release}.sh`; `tools/xtask/src/kvm.rs` `verify_dvm_artifacts`; `libs/driver-domain-host/src/lib.rs` `ControlContract::parse`; `tools/hostd/src/runtime.rs` `verify_artifacts` | Matched: schema 8 admits exactly 25 unique manifest keys and a six-key control contract, then packages them with kernel, rootfs, Buildroot config, kernel config, module-signing certificate, and source lock. All three verifiers hash the co-located companions; hostd derives every path from the admitted manifest directory, independently compares every control field, and launches with that verified parsed snapshot instead of reopening it. Staging refuses an existing destination, symlink traversal, and group/world-writable ancestors, verifies before and after copying, fsyncs the 0644 files and 0755 temporary directory, and publishes it by atomic no-clobber rename. TLC exhaustively checks 6,828 states spanning all component subsets and corrupt-copy/replacement/mutation outcomes. |
| dvm-display-driver-supply | `driver-domains/linux/sources.lock`; `package/rustos-dvm-nvidia-open`; `configs/rustos_linux_dvm_x86_64_defconfig`; `board/linux.fragment`; `scripts/verify-module-signatures.sh`; `board/overlay/etc/init.d/S48rustos-dvm-net`; `rustos-dvm-agent.c` display driver admission | Matched at source/build level for the current Blackwell target: one SHA-256-pinned official 580.173.02 release supplies the open `nvidia`, `nvidia-modeset`, and `nvidia-drm` modules and exact-version GSP images. UVM/CUDA and the proprietary kernel directory are not built. The kernel requires SHA-256 module signatures and signs every installed in-tree and external module. Schema-8 artifacts bind both the resulting certificate and the exact kernel configuration that proves enforcement; build verification extracts every installed module's detached PKCS#7 payload and verifies it cryptographically against that certificate. Boot selects only a kernel-produced display-class PCI modalias, completes NVIDIA modeset/DRM loading before relay start, and the control agent admits `nvidia` only as a DRM-bound display owner. The firmware package is explicitly non-redistributable; commercial redistribution authorization plus physical connector, IOMMU fault/reset/revoke, and sustained 60 FPS page-flip captures remain failed release evidence. |
| dvm-amdgpu-supply | `tools/hostd/src/runtime.rs`; `sources.lock`; `configs/rustos_linux_dvm_x86_64_defconfig`; `board/linux.fragment`; `scripts/verify-module-signatures.sh`; `S48rustos-dvm-net`; `rustos-dvm-agent.c` | Matched at source/build level for AMD PCI `1002:1900`: hostd binds the host VFCT VBIOS to exact PCI identity and an exact-or-absent subsystem pair, validates the ACPI checksum plus 0x55aa/ATOM image, relocates only its BDF to fixed guest slot `0000:00:08.0`, recomputes the checksum, proves the payload unchanged, snapshots the complete table privately, and supplies it through QEMU ACPI with the VFIO function pinned to that slot. The DVM contains the signed upstream `amdgpu.ko`, selects it only from the kernel PCI modalias, and fails image verification unless all thirteen target firmware payloads are regular files matching their source-locked SHA-256 digests. The corrected schema-8 artifact is rebuilt and signature-verified. Clean physical runs loaded DMUB, initialized DCN 3.1.4, passed gfx/compute/SDMA IB tests, registered DRM/fbcon, imported all three sources, and completed real fenced GPU/KMS frames after prime-completion v2 authenticated direct-DMA-BUF mode. An injected wrong VCN digest failed closed. TLC checked 458,755 distinct states and proves that missing identity, validation, guest-BDF relocation/checksum, private snapshot, QEMU supply, firmware, module signature, or modalias selection cannot reach KMS/relay readiness. Quantitative throughput and supervised reset/recovery remain separate unaccepted gates. |
| dvm-amdgpu-evidence | `libs/driver-domain-host/src/lib.rs`; `tools/hostd/src/{main.rs,runtime.rs}`; `rustos-dvm-agent.c`; `rustos-dvm-display.c`; `rustos-dvm-gpu-runtime.c` | Matched at source level: schema 3 fixes exact AMDGPU identity and signed performance bounds. Evidence ABI v2 atomically publishes one sequence-numbered one-second sample from completed DRM page-flip events and requires a read-only DMA-BUF source, GPU composition into a separate three-buffer GBM scanout pool, explicit fence, atomic KMS, zero CPU copy, no staged damage copy, frame rate, commit-to-event latency, and nonblocking commit time. The authenticated agent rejects malformed/private-state violations; hostd requires five distinct consecutive passing samples and rejects stale, restarted, wrong-identity, staged/CPU-copy, low-throughput, or high-latency evidence. TLC checks the finite abstraction. Assigned-GPU runs proved the qualitative direct path, but the captures did not satisfy the signed five-sample throughput gate; further FPS collection is user-deferred, so release readiness remains unaccepted. |
| driver-domain-fleet | `tools/hostd/src/main.rs` `verify_release_authorization`; `libs/driver-domain-host/src/lib.rs` `DriverDomainFleetPolicy` | Fixed during this audit: a signed release now binds a strict fleet artifact. The parser rejects CID, IOMMU-group, and PCI-function aliases across members, and activation requires the exact validated member. |
| gui-dvm-surface | `formal/gui-dvm-surface/GuiDvmSurface.tla`; `formal/dvm-atomic-scanout/DvmAtomicScanout.tla`; `tools/xtask/src/kvm.rs`; `kernel/io-manager/src/io/dvm_display.rs`; `kernel/io-manager/src/io/gui/{backend.rs,mod.rs}`; `kernel/compat/src/user/{sysops/device.rs,syscall/linux/device_broker_ops.rs}`; `services/uiserver/src/main.rs`; `driver-domains/linux/package/rustos-dvm-display/src/{rustos_dvm_ivshmem_uio.c,rustos-dvm-display.c,rustos-dvm-gpu-runtime.c}` | Source/model matched with qualitative physical scanout evidence but not complete physical certification. V3 gives exactly three page-aligned host-owned slots in one preallocated 128 MiB DMA-pinnable backing, read-only DVM mapping, bounded generations, module-mediated release, and a read-only DMA-BUF exporter. Prime-completion v2 authenticates exactly one backend mode, the kernel caches it, every submit must match it, and the direct path requires explicit linear-modifier EGL import. Clean physical runs proved all three EGLImage imports, real GPU composition, explicit render fences, and atomic KMS page-flip/out-fences. The virtio staged-copy path can never become physical success. The scanout model requires VFIO mapping before source authority and matches mode, acquire issuance/consumption, ordered source selection, source/output lifetime separation, fence-before-release, and evidence publication. The bounded KVM recovery lane now proves abrupt DVM exit, predecessor-confirmation revoke, context-epoch re-prime, idle-desktop re-invitation, and a fresh RustOS/DVM cohort reboot. A separate 60-window KVM run sustained at least 55 FPS; assigned-GPU quantitative certification remains a distinct hardware gate. |
| dvm-gpu-compositor | `formal/dvm-gpu-compositor/DvmGpuCompositor.tla`; `libs/driver-domain-protocol/src/lib.rs`; `services/uiserver/src/{gpu_scene.rs,gpu_runtime.rs}`; `kernel/io-manager/src/io/dvm_display.rs`; `driver-domains/linux/package/rustos-dvm-display/src`; `tools/xtask/src/{build/mod.rs,kvm.rs}` | Source/model and rebuilt packaged-QEMU evidence match for the pre-public-ABI slice. Applications retain the Wayland buffer contract; only uiserver can emit the bounded clear/solid-quad/textured-quad vocabulary. RustOS owns one three-entry monotonic context timeline and an asynchronous three-slot queue. A restarted DVM receives a new host-selected context epoch, old live slots are revoked, and stale uiserver work is rejected. The DVM measures fixed EGL/GLES program creation plus the initial fenced atomic KMS frame; its kernel module validates and publishes that exact prime record before readiness, and uiserver no longer fabricates a local duration. Runtime render and KMS completion use distinct explicit fences. The 16.667 ms frame target remains a measured acceptance limit, but the previous front and current epoch remain live until the distinct 50 ms hard timeout or an explicit context/device error. Rust and both C consumers are cross-checked by `xtask check` for version, magic, and prime offsets. TLC exhaustively checked 2,847,356 distinct states, including the target-miss window, measured current-epoch prime, queue bounds, hard timeout, revoke, and reset, without invariant or temporal-property error. The final 30-second QEMU capture admitted GPU composition and explicit fences, completed the private 120-frame proof at 128.705 FPS with 7.769/11.075 ms average/maximum GPU time, and retained the provider without context loss or compositor-offline markers. The relay now has no pre-command CPU-frame renderer or impossible zero-atlas direct-import branch; display readiness can follow only a validated command batch with GPU and present completion, and `xtask check` rejects those retired symbols if they return. This QEMU proof does not claim a public application 3D ABI; separate physical runs proved qualitative AMD zero copy while quantitative acceptance remains deferred. |
| dvm-gpu-proof-scheduler | `formal/dvm-gpu-proof-scheduler/DvmGpuProofScheduler.tla`; `driver-domains/linux/package/rustos-dvm-display/src/rustos-dvm-gpu-probe.c`; `tools/xtask/src/{build/mod.rs,kvm.rs}` | The pipeline prime remains under normal scheduling. The proof requires an initial normal policy, installs and reads back the exact 50/100 ms `RLIMIT_RTTIME`, then admits only the finite 120-frame measurement to SCHED_RR priority 8 below display/input. Success and ordinary failure restore and read back both the saved policy/priority and RT limit before evidence. Hard-limit or uncertain-restore termination leaves no evidence, and the long-lived health proof remains normal. xtask rejects loss of those source guards. TLC checked 18 distinct states and both restore-settlement/publication properties without error. Earlier QEMU evidence recorded the scheduler, limit, hard-action, and restored-normal fields; a new runtime capture is not claimed by this source-only hardening. |
| dvm-display-scheduler | `formal/dvm-display-scheduler/DvmDisplayScheduler.tla`; `driver-domains/linux/package/rustos-dvm-display/src/rustos-dvm-display.c`; `tools/xtask/src/{build/mod.rs,kvm.rs}` | The display relay remains normal-priority through setup and, only after host confirmation, requires an initial normal policy, installs/reads back the exact 50/100 ms RT bound, then admits SCHED_RR priority 9 below input. Partial admission cannot run the relay. Ordinary stop/failure may retry only after exact saved policy/priority and RT-limit readback; rollback/restore uncertainty is a fatal process exit, while the Linux hard limit also removes all process authority. xtask guards those source conditions. TLC checked 13 distinct states and stop settlement through either verified restoration or terminal failure. Earlier QEMU evidence proved normal successful admission; no runtime fault injection is claimed for the new fatal branches. |
| dvm-display-readiness | `formal/dvm-display-readiness/DvmDisplayReadiness.tla`; `rustos-dvm-display.c` process owner/publication/teardown; `rustos-dvm-agent.c` `display_relay_is_ready`; `tools/xtask/src/{build/mod.rs,kvm.rs}` | Fixed during this audit: the display process first owns a singleton lock. After a confirmed peer and completed GPU frame, it writes, fsyncs, and locks one fixed candidate inode before atomic rename to the ready path. Agent health already requires the exact payload plus a held inode lock. Ordinary relay failure now closes that lock before scheduler restoration; SIGKILL and the RT hard limit close all process locks. A duplicate process, partial/stale candidate, unlocked old path, and post-exit state therefore fail closed, while the fixed candidate bounds crash residue. TLC checked 306 distinct states spanning duplicate start, partial publication, atomic promotion, ordinary fault/restore, fatal restore, retry, and hard-limit/crash paths without error. |
| dvm-gpu-admission | `formal/dvm-gpu-admission/DvmGpuAdmission.tla`; `services/uiserver/src/{gpu_runtime.rs,gpu_scene.rs,render.rs,sys.rs}`; `libs/rustos-user-abi/src/device.rs`; `kernel/{io-manager,ps}` display-surface paths; `driver-domains/linux/package/rustos-dvm-display/src` | Source/model matched for bounded promotion and pacing. DVM scanout without a current GPU prime remains waiting and cannot be counted as software success. Atlas creation/mapping runs on a uniquely owned worker mapping while the UI thread keeps the existing CPU-presented surface live; initialization and the first GPU frame have separate terminal deadlines. The first completion uses the explicit five-second activation deadline, while only an already-active epoch uses the steady-state 50 ms hard recovery limit; host scheduling jitter can no longer contradict the first-frame contract. The DVM primes the full provider-stride atlas upload, fixed textured draw, EGL native fence, and atomic KMS present within the 500 ms setup budget; a clear-only prime cannot activate the model. Only that representative current-epoch prime, exact provider pitch, bounded mapping, ready retained scene, and completed GPU frame activate the path. Acceptance treats the primary+DVM bootstrap display snapshot and the later kernel-stamped display-ready milestone as ordered evidence, rather than demanding that the later GPU bit appear retroactively in the earlier snapshot. Each subsequent frame consumes one non-accumulating 15 ms cadence permit, phase-locked with the Wayland callback clock, and early calls receive `EAGAIN` backpressure. Provider revoke clears the epoch and requires a fresh prime. TLC checked 1,464 distinct states and both temporal settlement properties without error. The first paced QEMU capture exposed repeated provider loss and visible `display not available` because the 16.667 ms target was also the hard fence timeout. Post-separation packaged captures no longer show that marker or provider revoke; the 50 ms hard timeout preserves the live epoch while the separate frame-performance gate still rejects latency tails. |
| dvm-gpu-atlas-transport | `formal/dvm-gpu-atlas-transport/DvmGpuAtlasTransport.tla`; `libs/driver-domain-protocol/src/lib.rs`; `services/uiserver/src/{gpu_scene.rs,gpu_runtime.rs}`; `kernel/io-manager/src/io/dvm_display.rs`; `driver-domains/linux/package/rustos-dvm-display/src` | Source/model matched for transport contract 3, display commit ABI v5, and prime-completion ABI v2. The DVM authenticates exactly one staged-copy or direct-DMA-BUF mode before peer readiness; RustOS caches it, exposes it only to the private compositor, and stamps the same mode into every immutable submit record. In staged-copy mode each surface handle maps only one page-aligned atlas slot into uiserver with write-combine alias consistency; the pointer-free commit makes ring0 validate capability, generation, damage, and command metadata without copying pixels. Command pages and sibling slots remain inaccessible. Old, zero, unknown, ambiguous, unregistered, and backend/mode-mismatched values fail closed. Exactly three imported slots retain one mapping generation for the provider epoch, while each bounded command batch advances sequence/content epoch; first texture initialization is full, later damage is bounded/non-overlapping, disjoint history remains disjoint while only overlapping rectangles are unioned, and execution remains ordered. Read authority always names a currently executing batch, so a completed frame can release a slot and a later sequence can reuse it without reviving old authority. One shared sealed registry currently certifies only `virtio_gpu`/`virtual-staged` and `amdgpu`/`physical-direct`; the proof, relay, and KVM gate verify that class instead of embedding an AMD readiness condition, and ambiguous registered render nodes are rejected. Later drivers require a separate evidence-backed profile and registry entry. The packaged-QEMU run remains valid staged-copy evidence. Clean physical runs proved real zero-copy GPU/KMS slot reuse; after the aligned runtime/model fix, the latest operator-observed rerun no longer reproduced stale-slot flicker or the independent post-input freeze. This is qualitative visual evidence only; sustained physical rate remains user-deferred and unaccepted. TLC exhaustively checked 72,592 distinct states including stable provider-epoch mapping generation, executing-batch read authority, authenticated-mode equality, and registered backend/mode invariants without error. |
| gui-dvm-pixel-authority | `formal/gui-dvm-pixel-authority/GuiDvmPixelAuthority.tla`; `tools/xtask/src/kvm.rs`; `kernel/io-manager/src/io/dvm_display.rs`; `driver-domains/linux/package/rustos-dvm-display/src/rustos_dvm_ivshmem_uio.c` | Matched: the UC ivshmem BAR carries only versioned control/MSI-X state; a separate 128 MiB cacheable pixel region is writable only in RustOS QEMU and exposed read-only/ROM to the DVM. The KVM and hostd launch contracts require its complete backing to be preallocated on DMA-pinnable tmpfs/hugetlbfs before VFIO mapping. The Linux module requires byte-identical control and pixel headers, maps pixel pages WB read-only, and rejects `VM_WRITE`/`VM_MAYWRITE`. TLC proves a READY slot is complete and host-written, only one host snapshot writer exists, incremental construction requires an exact predecessor or complete contiguous history no longer than the fixed slot count plus an unchanged captured source, retained content is distinct from release authority, DVM write attempts do not change pixel epochs, newest selection is fresh, and loss of either plane revokes selection. Source publishes the generation only after a complete copy and valid record exist; missing history forces full copy, and an unexpected competing writer clears the touched slot and revokes the transport. |
| gui-dvm-install | `formal/gui-dvm-install/GuiDvmInstall.tla`; `kernel/io-manager/src/io/dvm_display.rs` | Matched: the GUI-DVM installer is a serialized, one-way capability admission. It maps the validated UC control BAR and BAR0 doorbell, requires the fixed cacheable pixel region to contain the identical V3 header and geometry, allocates exactly two permanent MSI-X vectors, and registers the provider once. Every validation, arm, or registration failure releases acquired device mappings before terminal rejection; a rejected or runtime-revoked transport has no retry or fallback provider path. |
| ivshmem-pairing | `libs/driver-domain-host/src/ivshmem.rs` fixed peer slots; `tools/xtask/src/kvm/{guest,layout}.rs` | Peer ID assignment is no longer inferred from process spawn order. The launcher pins RustOS as peer 0 before peer 1 starts. The broker retains each fixed slot's eventfd lease across QEMU process replacement and advertises retained disconnected slots during cohort reconstruction, so a logical peer never receives a new identity. Display replacement explicitly revokes the inherited confirmation and re-primes a new context epoch; block replacement uses an L0-signed zero-cursor successor generation. Reconnect itself is never readiness evidence. |
| ipc-reply-deadline | `kernel/ipc-runtime/src/ipc/{mod.rs,endpoint_priority.rs}`; `libs/rustos-user-abi/src/{performance.rs,syscall.rs}`; `kernel/compat/src/user/syscall/linux/{ipc_ops.rs,net_broker_ops.rs,syscalld_ops.rs,service_ops/ipc_helpers.rs}`; `kernel/ps/src/user/handles/{table.rs,transfer_registry.rs}`; `services/netd/src/main.rs`; `services/rootd/src/{main.rs,control_drain.rs}`; `services/runtimed/src/main.rs` | Matched: reply completion is one-shot; arm/recheck/commit, cancellation, and peer-close have explicit paths. Kernel-owned service calls are classified at every call site: readiness is capped at 16 ms, interactive policy at 100 ms, boot control at 5 s, and bulk data at the public 30 s ceiling. A caller may shorten but cannot widen its class, exact-epoch service grants bypass repeated rootd authorization, and stable endpoint discovery double-checks epoch plus endpoint without taking the global writer lock. Reply-bound AF_INET stream publication adds one invisible initial descriptor bound atomically to the exact live reply and netd process owner; ordinary reply completion moves it into the response, while cancellation and endpoint failure return it. A timely caller then validates the exact v7 token/domain/type/protocol/fd/status flags under the same deadline before installing one fd; rejection reclaims the invisible descriptor and netd discards its initial token once. AF_UNIX remains generic. Rootd and runtimed drain at most the shared 32-request already-queued control burst before returning to lifecycle, launch, catalog, and socket work; the kernel's 64-call endpoint admission ceiling is compile-time bound to at least two such bursts. This prevents a ready synchronous dependency chain from sleeping or re-entering unrelated policy after every request without turning the service loop into an unbounded drain. Immutable successful time-policy keys are cached per process and reset on fork/exec; clock-nanosleep keys include exact clock and absolute/relative mode. The capability-authenticated syscalld owner self-admits only its own exactly validated receive-loop backoff, so it cannot synchronously call its own endpoint and leak the 30-second IPC deadline as `ETIMEDOUT` to libc. Source conformance executes the ordering, stable-snapshot, deadline, cancellation, service-drain, self-dependency, and reply-bound AF_INET witnesses. |
| scheduler-wakeup | `kernel/ps/src/multitask/scheduler.rs`, `current.rs`, `irq.rs`, and `scheduler/runqueue/{wait,ipc_handoff}.rs` | Fixed and matched: wait reason kind, arm, block intent, and runnable intent share the owner word. Ordinary commit changes running+runnable+armed to non-runnable+disarmed with one CAS; a racing wake either clears the arm first so commit refuses sleep, or restores runnable intent after commit. Exact endpoint identity survives blocked and provisional direct-handoff custody until wake consumes the kind. The catalog is only the invalid-owner fallback. A running or armed task owns no published resumable frame because dispatch already consumed it; only a non-current blocked task is validated before publication. Arm/commit/cancel reject terminal, suspended, root, already-blocked, and unarmed states. Retirement disarms the exact HAL sleep waiter and clears every ready/block/wake field before slot reuse. IRQ-discovered corruption uses one allocation-free per-slot quarantine reason and defers complete teardown to process-context housekeeping. |
| smp-release-admission | `tools/xtask/src/formal_contracts/evidence.rs`; `tools/xtask/src/kvm{,/guest}.rs`; `kernel/ps/src/multitask/{irq,spawn}.rs` | Multi-vCPU launch no longer trusts checked-in booleans: it validates a fresh schema-bound PR verification run against the exact current source-tree hash and every recorded artifact digest. Runtime acceptance additionally requires exact online, idle-entry, first user-dispatch, clockevent, and reschedule-IPI markers from every requested CPU. Missing evidence fails the run rather than shrinking the claimed topology. |
| smp-ring3-qualification | `formal/smp-ring3-qualification/SmpRing3Qualification.tla`; `apps/smpqual/smpqual.c`; `libs/rustos-user-abi/src/syscall.rs`; `kernel/compat/src/user/syscall/linux/{debug_ops.rs,smp_qualification_ops.rs,proc_broker_ops.rs,proc_broker_ops/activation_batch.rs}`; `kernel/compat/src/user/sysops/console.rs`; `kernel/nucleus-core/src/debug/mod.rs`; `services/runtimed/src/{catalog.rs,main.rs,socket.rs,spawn.rs,kvm_smp_qualification.rs}`; `services/vfsd/src/{lib.rs,block.rs}`; `tools/xtask/src/{stage/mod.rs,kvm.rs,kvm/evidence.rs,kvm/evidence/smp_qualification.rs,kvm/guest.rs,kvm/layout.rs,options.rs}`; `driver-domains/linux/package/rustos-dvm-block/src/rustos-dvm-block.c` | The signed early-system catalog commits without private-storage access. Its private reconciliation is independently pending and retryable: unpublished storaged endpoint 0 or `-ENOENT` maps to `EAGAIN`; only a mounted private-file `NotFound` resolves absent. Candidate construction propagates loader error before clone/sort/publication, leaving ordinary entries unchanged; retries are bounded 250 ms through 5 s. The injector alone creates `LaunchEntry.private_smp_qualification`, and before UI-ready runtimed admits only the UI server or `qualification_contract_for_launch(entry) == Ok(Some(_))`; ordinary entries remain gated. `smpqual.elf` is an early-system allowlist member, preventing a DVM-volume executable substitution. Qualification forces the DVM block provider; QEMU attaches its exact backing read-only, create and rotate sign `READ_ONLY` headers, writable predecessors are refused, readiness requires RustOS/DVM/READ_ONLY, and the Linux relay compares `BLKROGET` with that header before publishing readiness. Runtimed binds the suspended exact executable before `PROC_ACTIVATE` and retires failures. Proc broker derives `qualification_required` from that exact executable and keeps the binding's SESSIOND owner/epoch and process/MM identities kernel-only. The binding-lock-held FSM enforces deadline, exact worker/TID/CPU/work, Ready-before-Start, Start-before-Finish, and Complete-after-all-Finish; endpoint identity is revalidated before output. The host admits only the exact 3N+1 kernel-stamped frames in five seconds. The v6 success record names v5 as predecessor and seals the formal record, RustOS images, actual attached DVM block-disk path/bytes/SHA-256, signed `system/boot/early-system.img#apps/smpqual/smpqual.elf` bytes/SHA-256, DVM kernel/rootfs, and private contract; prelaunch rechecks each artifact before unique non-overwriting publication. The bounded smoke path completes immutable host hashing/copy capture before its bounded input relay, then spawns both guests, and only afterward opens the readiness boot timestamp/deadline; that host work cannot consume a guest or relay readiness budget, while the interactive unbounded relay remains separate. FNV detects transport corruption/interleaving only; it is not authentication. The model's fairness property is finite local phase delivery, not a scheduler-rate claim. |
| clocksource-deadline | `kernel/hal/src/arch/{acpi.rs,clock.rs,rtc.rs}`; `kernel/hal/src/hooks.rs`; `kernel/ps/src/multitask/{current.rs,scheduler.rs,irq.rs}` | ACPI publishes the HPET GAS correctly. Invariant TSC plus calibrated frequency remains available for CPU-local TSC-deadline events, but raw TSC is a global source only for one CPU; every SMP topology requires validated 64-bit HPET until per-CPU offset/skew rendezvous is implemented. RTC remains calendar-only. PIT services absolute clocksource deadlines, so coalesced edges cannot slow elapsed time. Both deadline construction and the outer sleep completion loop read the same monotonic `ticks()` domain. Sleep uses the scheduler task id and arm/register/recheck/commit to reject a raced wake. |
| scheduler-admission | `formal/scheduler-admission/SchedulerAdmission.tla`; `services/runtimed/src/{main.rs,spawn.rs}` | Fixed during this audit: launch-catalog `weight` is untrusted policy input. Runtimed clamps every non-UI launch to the ordinary fair-share ceiling; only the exact `services/uiserver/uiserver.elf` path receives the source-pinned UI System weight. A path lookalike and an arbitrarily large registry value cannot enter the strict band. |
| scheduling-context-budget | `formal/scheduling-context-budget/SchedulingContextBudget.tla`; `services/rootd/src/{main.rs,scheduling_policy.rs}`; `kernel/executive/src/boot.rs`; `kernel/ipc-runtime/src/{api.rs,ipc/mod.rs}`; `kernel/ps/src/{api.rs,multitask/{current.rs,spawn.rs,scheduling_api.rs,scheduler.rs},multitask/scheduler/{scheduling_context.rs,ipc_donation.rs,donation_ledger.rs}}`; `kernel/compat/src/user/{console_host.rs,process/mod.rs,syscall/linux/{ipc_call_admission.rs,ipc_ops.rs,scheduler_ops.rs,proc_broker_ops/scheduling_context_grants.rs,service_ops/ipc_helpers.rs}}` | Rootd's immutable service manifest owns each domain/budget/period policy and publishes an exact requester/path grant; direct bootstrap consumes that grant once, while rootd itself receives an explicit bounded boot context. Production user-slot and process-spawn surfaces reject missing temporal authority. Deadline domains are admitted conservatively only while every selected CPU remains at or below 90% utilization. Every synchronous call reserves one bounded reply charge token before blocking. Nested passive-server calls retain the root caller identity; dispatch charges that context and its domain. A multithreaded server bills the latest live reply token, terminal settlement restores its predecessor, and cancellation or owner exit revokes descendant custody. Exhaustion records the context identity through the snapshot, consumed time, budget/period, and exact reply; a missing or stale timeout endpoint takes one non-retrying throttle action until the already-bounded refill. Context/domain credit remains conserved and separate counters plus snapshot ABI v2 expose all accounting without granting authority. |
| ipc-priority-inheritance | `kernel/ps/src/multitask/{scheduler.rs,current.rs}`; `kernel/compat/src/user/syscall/linux/ipc_ops.rs` | Fixed during this audit: a System caller reserves bounded donation capacity while still runnable, before endpoint enqueue can block it. Publication replaces that reservation with one live reply and one exact worker; process-owned endpoints select one eligible worker rather than promoting the process. Effective System class crosses nested calls only along exact live reply edges, while completion, timeout cancellation, enqueue failure, and either task's retirement revoke the exact record. Capacity exhaustion fails admission with no blocked caller. A saturated System lane is capped at two consecutive dispatches before one ready User turn. |
| ipc-priority-queue | `kernel/ipc-runtime/src/ipc/mod.rs`; `kernel/compat/src/user/syscall/linux/ipc_ops.rs`; `kernel/ps/src/multitask/{scheduler.rs,current.rs}` | Fixed and mapped: the compat boundary derives the endpoint lane from the live effective scheduler class before taking the IPC slot lock, so ring3 cannot self-promote through request bytes. System and ordinary calls retain FIFO order in separate preallocated lanes under one combined 64-call admission bound. At most two System heads bypass an ordinary backlog before one ordinary head is mandatory; failed receive capacity does not advance the burst, and cancellation removes the exact call from either lane. The source mutant collapses the burst and the TLA+ mutant permits an ordinary head to bypass an eligible System head; both exact witnesses must reject the change. |
| ipc-handle-transfer | `libs/rustos-user-abi/src/syscall.rs`; `kernel/ps/src/user/handles.rs`; `kernel/ipc-runtime/src/ipc/mod.rs`; `kernel/compat/src/user/syscall/linux/{ipc_ops.rs,net_broker_ops.rs,service_ops/vfs_socket.rs,service_ops/vfs_meta.rs}`; `services/netd/src/main.rs`; `kernel/ps/src/multitask/scheduler.rs`; `kernel/executive/src/boot.rs` | Ring3 sees only the canonical 24-byte little-endian tuple `(nonzero transfer_id, nonzero random nonce, nonzero batch_generation)`; Rust enums, padding, pointers, and internal descriptors never cross the byte boundary. The kernel binds each batch to source and receiver process generations, netd service epoch, channel generation/direction, and exact stream interval. AF_UNIX queues bytes and control in one segment; recvmsg collects only consumed-segment authority, ordinary read returns it for release, and close/service restart settle all queued batches. Receive reserves invisible descriptor numbers and atomically publishes only after data/control/header copyout. Runtime user-address remap fault injection remains required evidence. |
| ipc-endpoint-ownership | `kernel/ipc-runtime/src/ipc/mod.rs`, `kernel/compat/src/user/syscall/linux/{ipc_ops.rs,net_broker_ops.rs}`, `kernel/ps/src/multitask/current.rs`, `kernel/ps/src/user/handles/table.rs`, `service_ops/{ipc_helpers.rs,vfs_socket.rs}`, `services/netd/src/main.rs` | Fixed during this audit: user receive/reply paths require the endpoint owner process, so service worker threads remain valid while foreign processes fail closed; process exit destroys process-owned endpoint authority, revokes queued/received work and installed process-local transfer authority, and the model now forbids later enqueue revival through the dead numeric endpoint. Every ordinary, transferred, `dup2`/`dup3`, and `F_DUPFD*` descriptor install now shares the same ceiling; full-table installs and `dup`/`F_DUPFD*` exhaustion return `EMFILE` rather than `EBADF`, and a transfer batch is capacity-checked before its first descriptor is installed. VFS response payload bounds are checked before any slice use, and a successfully opened remote VFS object is explicitly closed when metadata or local fd admission rejects publication. Socket-pair publication preflights room for both fds and the user output, rolls both descriptors back on any later failure; the reply-bound AF_INET path instead reclaims its non-visible descriptor on bind rejection before netd discards that one unpublished token. |
| authority-identity-lifecycle | `formal/authority-identity-lifecycle/AuthorityIdentityLifecycle.tla`; `kernel/ps/src/{multitask,user/handles.rs}`; `kernel/compat/src/user/syscall/linux/{proc_broker_ops.rs,net_broker_ops.rs,service_ops/vfs_socket.rs}` | Task IDs, process generations, local remote-VFS/socket tokens, prepare handles, and exec tickets now use checked monotonic allocation. Exhausted process generations retire the slot permanently; exec generation exhaustion rejects replacement before mutation. No authority identity wraps to zero or one and aliases a stale bearer. |
| capability-derivation-lifecycle/CapabilityDerivationLifecycle | `formal/capability-derivation-lifecycle/CapabilityDerivationLifecycle.tla`; `kernel/object/src/{identity.rs,handle.rs,api.rs}`; `kernel/ipc-runtime/src/ipc/{mod.rs,slab.rs}`; `kernel/ps/src/{multitask/process_table.rs,user/handles.rs,user/handles/transfer_registry.rs}` | `ObjectIdentity` requires a nonzero owner/kind/slot/generation tuple, while `CapabilityEpochs` keeps provider lease and derivation revoke epochs distinct. IPC tickets preserve the existing opaque integer wire tuple but carry an internal `Ipc/Transfer` identity. Generational endpoint and reply handles decode in-range `Ipc/Endpoint` or `Ipc/Reply` identity without treating well-shaped input as a live object; the operation still validates its exact slab generation. A process handle likewise adapts its one-based table slot and existing generation to `Ps/Process`, without replacing exact table liveness resolution. An exec reservation retains its exact process handle and exposes its pre-exec MM generation as a one-shot `Ps/LifecycleToken`; table authorization/consumption remains mandatory. The process-visible descriptor token pair remains compatible; only proven nonreusable console, remote-VFS, and AF_INET open-description tokens carry the optional `OpenDescription` adapter, while unproven provider tokens remain explicitly legacy. Typed descriptor derivation retains the same transfer/provider token and accepts only same-kind rights subsets; widening or kind substitution fails before publication. Existing registry checks still reject stale ticket nonce/batch and service epoch. |
| root-authority-publication | `formal/root-authority-publication/RootAuthorityPublication.tla`; `kernel/compat/src/user/syscall/linux/ipc_ops.rs`; `kernel/ipc-runtime/src/ipc/mod.rs` | Rootd bootstrap is a one-shot trust transition: its first successful non-reusable process owner is sealed for the boot, so revoke or exit never creates a public root-capability registration window. Every service registration proves that the numeric endpoint belongs to the publishing process. Non-root capability authorization records the live rootd publication epoch and rechecks it under the registry mutation lock before the endpoint commit point; rootd restart, revoke, or exit makes the prepared grant stale and returns `EAGAIN`. |
| service-call-authority | `formal/service-call-authority/ServiceCallAuthority.tla`; `kernel/compat/src/user/syscall/linux/ipc_ops.rs`; `kernel/ipc-runtime/src/ipc/mod.rs` | Successful service lookup records a bounded process-local grant for the exact service publication epoch. Both ordinary calls and calls with attached handles require that exact grant, while the endpoint owner remains valid without lookup. Revocation or republication changes the epoch, process exit deletes its grants, and a raw endpoint outside the live service registry is callable only by its process owner. Predictable monotonic routing IDs therefore cannot bypass rootd discovery policy. |
| runtime-control-authority | `formal/runtime-control-authority/RuntimeControlAuthority.tla`; `services/runtimed/src/{socket.rs,spawn.rs,main.rs}` | Runtimed accepts only a complete `SO_PEERCRED` record with a nonzero peer PID. Every request then revalidates the peer against the live uiserver service owner or the immutable `logical_admin` bit copied from the signed launch entry into the exact running-process record. Snapshot, watch, launch, and terminate require either role; UI-ready is uiserver-only. A held watch grants no authority the caller lacked at connect time, and runtimed parks at most `MAX_RUNTIME_WATCHERS` of them, answering past that cap immediately rather than pinning descriptors. Unknown, foreign, exited, and service-revoked peers fail with `EACCES` before dispatch. The nonblocking listener retains at most sixteen partial requests and advances each without busy-waiting, so a background client preempted after connect cannot invert scheduler classes or block the first catalog-policy drain. |
| deferred-process-activation | `formal/deferred-process-activation/DeferredProcessActivation.tla`; `libs/rustos-user-abi/src/syscall.rs`; `services/loaderd/src/{main.rs,commit.rs}`; `kernel/compat/src/user/syscall/linux/proc_broker_ops.rs` | Deferred commit records a bounded one-shot `(target_pid, requester_pid)` authority in ring0. ACTIVATE requires and consumes the exact pair before waking the target. Foreign callers and replay fail, loaderd restart does not erase the binding, and requester exit removes the binding and retires the suspended target. |
| loader-request-authority | `formal/loader-request-authority/LoaderRequestAuthority.tla`; `libs/rustos-user-abi/src/syscall.rs`; `services/{initd,loaderd}/src/main.rs`; `services/loaderd/src/commit.rs`; `kernel/compat/src/user/syscall/linux/proc_broker_ops.rs` | `SPAWN_EXEC` is admitted only for the live rootd, initd, or sessiond endpoint owner; `EXEC_TARGET` is procd-only. Initd publishes an identity-only endpoint before its first loader request. Loaderd checks the kernel-stamped sender at ingress and ring0 revalidates the same live service role before consuming prepare or exec-ticket authority, so restart/revoke during image loading fails closed. |
| proc-broker-session | `kernel/compat/src/user/syscall/linux/proc_broker_ops.rs`, `services/loaderd/src/main.rs`, process-exit paths | Fixed during this audit: process exit and signal termination purge owner-bound uncommitted `PROC_PREPARES`, and prepare publication rechecks the owner-exit marker under the same table lock after the external policy call. An in-flight request therefore cannot republish after cleanup. Commit remains terminal by design because it removes the handle before later validation. A zero session argument inherits the exact caller/target-thread session only; missing state returns `EINVAL`, never system-session authority. |
| commercial-service-envelope | `formal/commercial-service-envelope/CommercialServiceEnvelope.tla`; `libs/rustos-user-abi/src/syscall.rs`; commercial handlers under `services/{rootd,procd,storaged,devmgrd,inputd,vfsd,syscalld,netd,loaderd}/src/main.rs`, `services/runtimed/src/session.rs`, and `services/uiserver/src/sys.rs`; clients under `services/{initd,devmgrd}/src/main.rs`, `services/runtimed/src/spawn.rs`, and kernel commercial clients | Every commercial handler shares one ABI-level request-envelope validator, so wrong versions, reserved flags, and out-of-bounds path/payload lengths fail closed. A malformed-size synchronous request receives an explicit error response instead of dispatch or abandonment. Callers bind the response to the exact request header and reject reserved fields, nested-length/count overflow, truncation, timeout, and peer close. The finite composition model proves that only an exact request plus exact response reaches admission. |
| zero-trust-service-flow | `formal/zero-trust-service-flow/ZeroTrustServiceFlow.tla`; `formal/trust-boundaries.tsv`; `kernel/compat/src/user/syscall/linux/ipc_ops.rs`; `libs/rustos-{user-abi,svc-runtime}/src`; every published service receive loop | Every published service receives a kernel-stamped sender identity. Direct request subjects and loader requester identity must match it exactly. Explicit cross-service delegation revalidates the current live owner on each request and is never cached across restart. The owning subsystem still rechecks object rights and generation, and the caller admits only an exact request-bound response. `check-zero-trust-ingress.sh` fails when a new published service omits these ingress contracts. |
| zero-trust-subsystem inventory | `formal/zero-trust-subsystems.tsv`; `formal/check-zero-trust-subsystems.sh` | High-risk external parsers, all kernel DVM shared-memory consumers, host-side DVM vsock/QMP readers, and all service-local socket listeners are registered with source patterns for shape, authority, and lifecycle failure. Every row names a registered model with an executable source witness. Adding a `dvm_*.rs` consumer, host-control reader, or `accept4`/`UnixListener` service source without a contract fails formal selftest. |
| dvm-control-ingress and storage QMP | `libs/driver-domain-host/src/lib.rs`; `tools/hostd/src/runtime.rs`; `formal/zero-trust-subsystems.tsv` | The host accepts DVM control only on a secret-derived endpoint, from the exact expected CID, within bounded socket deadlines, and after a nonce- and contract-bound constant-time HMAC proof; frames and field sets are exact and bounded. Storage shutdown accepts QMP only through its private runtime socket, bounds message size/count, negotiates capabilities, and requires the exact command response ID before observing the exact child exit. Both parsers are mandatory zero-trust inventory entries with executable witnesses. |
| entropy-broker-boundary | `formal/entropy-broker-boundary/EntropyBrokerBoundary.tla`; `boot/boot-protocol/src/lib.rs`; `kernel/nucleus-core/src/multiboot2.rs`; `libs/boot-random/src/lib.rs`; `kernel/compat/src/user/syscall/linux/entropy_broker_ops.rs`; `services/{syscalld,netd}/src` | Boot rejects an absent/all-zero hardware seed. Boot-random now retains a one-time private ChaCha20 master stream and derives each child seed from master output instead of XORing a public monotonic counter into one fixed seed; use before initialization and reinitialization fail closed. Only Linux-syscall and network-policy capabilities may request a bounded inline copy. Syscalld retains getrandom flag/length policy and netd retains collision/admission policy. |
| exec-ticket | `services/procd/src/main.rs`, `services/loaderd/src/main.rs`, `kernel/compat/src/user/syscall/linux/proc_broker_ops.rs`, normal/signal process-exit paths | Fixed during this audit: authorize now requires an exact live PID/TID and rechecks post-publication exit; cancel/exec validate before remove; transition publication precedes target image replacement; non-final TID exit, exec sibling retirement, and process exit purge target tickets/transitions; loaderd aborts early rejected commit state. |

### Exact revoke and qualification-output witnesses

The DVM block revoke owner has a closed twelve-reason production vocabulary.
Its once-only guard records immutable reason, generation, flags, and four ring
cursors before pending-slot and readiness clear; the Required milestone binds
reason to `arg0` and generation to `arg1`. The first 64 KiB READ after a valid
FLUSH completion remains live. Qualification output is closed to four critical
names with qualification-local loss snapshots; a critical drop increments both
global and local counters, scheduler records remain one-attempt measurements,
and the xtask parser rejects any nonzero loss counter before acceptance.

## Discrepancies found and closed

1. `TRANSFER_OBJECTS` was owned by compat while endpoint removal and task exit
   occur in IPC runtime/process substrate. A handle-aware receive could dequeue
   descriptors, fail later user-output validation, and leave registry entries
   unreachable; caller exit also left queued request/reply descriptors alive.
   The registry now resides in `kernel-ps`; runtime returns detached descriptor
   batches, and compat/PS dispose of them on every terminal path.
2. `PROC_PREPARES` was owner-bound for authorization but had no owner-exit
   cleanup. A loader crash between `PREPARE` and `COMMIT` could retain bounded
   prepare slots and pinned mapping metadata. Both ordinary exit and
   signal-driven process termination now remove the owner's uncommitted state.
3. Exec authorization accepted any live TID without proving its requested PID,
   while cancel and exec-target removed tickets before comparing that pair. A
   malformed request could therefore authorize a mismatched target or destroy
   a valid ticket. Target-thread exit and exec sibling retirement also retained
   pending tickets/register handoffs, and the handoff was inserted after image
   replacement. Authorization now checks the exact live pair, removal follows
   comparison, transition insertion precedes replacement, and thread/process
   teardown purges target-bound ticket state.
4. `runtime-control` accepted any successful response opcode and negated every
   nonzero signed status. A stale/cross-RPC reply could therefore become a
   false launch/terminate success, while `i32::MIN` could overflow. Response
   identity and shape are now validated before success; server error envelopes
   retain their documented opcode-zero compatibility.
5. Scheduler retirement removed a task without disarming its exact sleep
   waiter or clearing every wake/block bit. Retired tasks could therefore
   retain bounded timer-table capacity and stale wake authority. The first
   hardened arm/commit/cancel guard also inverted the scheduler's runqueue
   convention: a dispatched task is `ready=false`, so every service reply wait
   failed immediately with `EINVAL` and core endpoints could not register.
   Retirement now revokes the waiter before slot cleanup; the wait transaction
   accepts only the current nonblocked task, consumes a raced wake's transient
   ready mark, and rejects terminal or suspended lifecycle states.
6. Direct-map permission updates validated and split page tables one 2-MiB
   batch at a time. A later split-capacity or range failure could return
   `false` after earlier batches had already changed kernel permissions.
   Updates now validate and pre-split the complete range under one writer
   transaction before changing any permission; an impossible post-prepare
   failure is fail-stop rather than a partially successful return.

## Remaining boundaries outside this audit

- The signed post-cleanup QEMU capture source-matches the retired-path guards:
  GPU/display readiness followed only staged-copy GPU composition with explicit
  fences and three scanout buffers, and no legacy/direct-import or
  display-unavailable marker occurred. It remains performance-blocking rather
  than release evidence: the private proof reached 110.389 FPS, but settled
  relay samples were 50.730--59.105 FPS and WayClick was 35.894--41.776 FPS.
  uiserver cannot yet include aggregate Wayland/AF_UNIX readiness in its wait;
  the general cross-provider wait-set ABI is a separate required boundary.
- Public IPC calls intentionally retain blocking ABI semantics; only internal
  policy-service waits are deadline-bounded.
- The scheduler distribution model proves the one-System/one-User bounded
  reservation only. It does not prove multicore CPU-time distribution or
  hardware interrupt latency; target measurements remain release-blocking.
- ELF/PE byte parsing and page-table lifecycle now have finite abstractions and
  mapped source tests. Kani also proves the compositional decoder,
  segment/section, entry, single-relocation, and single-import contracts. Full
  multi-block corpus fuzz/native launches and target TLB behavior are
  independent release evidence and remain open.
- **Release-blocking hardware gap:** supervised physical DVMs require a
  non-identity QEMU IOMMUFD/VFIO address space, bounded DMA apertures,
  complete-group reset, and fail-closed revoke. Target fault/revoke/reset and
  guest-kernel captures are still absent. RustOS ring0 no longer has a native
  physical-storage DMA path; source state machines and TLC runs still do not
  substitute for the remaining hardware proof.
- No enabled topology advertises a trusted physical display or trusted
  human-input device. The trusted-UI model verifies the fail-closed admission
  rule and source provenance only; an in-band DVM display overlay cannot
  authenticate to a human, so privileged-prompt admission remains blocked.

## 2026-08 SMP transaction hardening

- `user-stack-growth/UserStackGrowth` binds the release eager-map profile to
  every usable page plus one permanent unmapped guard before task publication.
  The dormant lazy-growth exception route was deleted; a future lazy profile
  requires a new deferred-fault owner and refinement before source admission.
- `exec-address-space-transaction/ExecAddressSpaceTransaction` binds an exact
  scheduler target reservation to ProcessState-only staging, Scheduler-only
  publication, ProcessState-only final visibility, an exact `exit_pending`
  latch, and old-bundle retention. It rejects either lock nesting direction,
  target retirement before token settlement, and visible half-exec state. Its
  source-level publication invariant fault is a reachable fail-stop rollback
  state, so the old publication-failure assertion is no longer vacuous.
- `ipc-transfer-authority/IpcTransferAuthority` advances its committed stream
  frontier only by provider-accepted bytes and binds one opaque transfer batch
  to the exact receiver-set epoch, service epoch, channel direction, and that
  accepted stream position;
  FD visibility requires complete copyout and every terminal owns the batch
  exactly once.
- `dvm-transport-lifecycle/DvmTransportLifecycle` binds activation state and
  epoch in one packed CAS word, publishes mapping before exact Active commit,
  and binds every display/input shared-memory dereference to an exact mapping
  claim. Drain may win from Activating or Active; reset/unmap cannot complete
  until all in-flight claims have quiesced.
- `page-table-map-transaction/PageTableMapTransaction` binds every published
  leaf to a reverse commit log, and every published intermediate table to
  exactly one ownership ledger. A failed mapping returns only after exact leaf
  restoration and one shootdown-before-reclaim edge; it keeps its tables,
  because an exception-time installer that took no lock may already hold a leaf
  inside one. Only retirement frees a table frame.
- `robust-futex-owner-death/RobustFutexOwnerDeath` now distinguishes shared
  backing and exact private keys, including non-private anonymous fallback and
  kernel-generated robust/clear-child wake equivalence.
- `gpu-submit-transaction/GpuSubmitTransaction` binds compiler timeline
  checkpointing to transport acceptance; rejection restores the checkpoint and
  requires a complete next-atlas replay.
- `acceptance-profile-publication/AcceptanceProfilePublication` binds an
  initial registry miss to one demoted 30-second watcher and exactly one atomic
  profiler announcement. These are source/formal gates, not QEMU performance
  evidence.

## Shared-RAM cache and AP PAT conformance

Matched to `DvmBlockStartup`: block admission requires a prefetchable non-I/O
BAR, RustOS `map_shared_write_back`, a Linux `IORESOURCE_PREFETCH` resource,
and its explicit WB `remap_pfn_range` cache flags. Matched to `DvmInputRing`:
the host producer uses `MAP_SHARED`, while RustOS requires the same
prefetchable WB aperture. Matched to `DvmNetworkRing`: RustOS may install
before Linux, but only through the exact prefetchable WB aperture and atomic
control-word access; the relay becomes ready only through its dedicated WB UIO
mapping, never raw PCI `resource2`, and both sides use Acquire/Release cursors.
The global MMIO interval registry rejects mixed
cache modes before direct-map/high-window selection and rejects a range that
straddles the direct-map boundary; registry and per-page metadata reserve
before their respective mapping/PTE mutations. `PhysicalMappingCacheMode::WriteBack`
is the empty x86 page-table selector. Matched to `CpuOnlineLifecycle`: every
AP rejects an initial non-WB PAT0, initializes PAT2=UC/PAT4=WC, and exactly
reads back the full expected PAT MSR before `OnlineParked` and
`smp-ap-private-ready`. The source gate also rejects a legacy boolean
`mmio::map` API. These clauses deliberately exclude display pixels.
Permanent APIC MMIO instead retypes its preexisting direct-map leaf UC before
APIC/PIC configuration; generic high-window mapping rejects below-limit
physical addresses. Source conformance whitelists direct-map cache
inspection/update owners and requires display pixels to use the io-manager WC
mapper rather than a raw PTE update.
